# 当前收口总账（2026-08-22）

## 版本边界

- 当前维护发布版本：`v4.8.5`。
- 当前分支：`codex/ai-upgrade`。
- 最新正式 Tag 提交：`c04732900ee257f8bae02ad25cc1559dd2643bb8`；Tag 对象、Actions、资产和 SHA-256 见[v4.8.5 发布收尾记录](./release-v4.8.5.md)。v4.8.3/v4.8.2/v4.8.1 发布后文档回填提交保持历史记录。
- v4.8.5 代码、文档、发布配置、Windows x64/ARM64、macOS Universal 和离线 Web 资产均已完成正式 Release；私有仓库不以 Pages 作为入口。
- 正式证据：Release [v4.8.5](https://github.com/7752777/counselor-desk/releases/tag/v4.8.5)，Actions `32578966917` 全部 job 成功，Windows/macOS/Web 三份哈希清单已上传；具体资产大小和摘要见[发布收尾记录](./release-v4.8.5.md)。v4.8.3/v4.8.2/v4.8.1/v4.8.0 历史资产不移动。
- GitHub 仓库 `7752777/counselor-desk` 已设为私有；私有仓库不等于终端、数据库或备份已经加密。

### v4.8.5 发布完整性维护

- 单文件离线构建把 `assets/app-icon.svg` 内联为 data URL，用户从磁盘双击打开时不再产生外部图标资源失败。
- `tests/browser-file-route-smoke.js` 直接打开构建产物，以 `file://` 在 `1440x920`、`1024x768`、`390x844`、`360x800` 渲染 232 次主路由，检查抽屉、Escape、页面错误和窄屏横向溢出。
- `tests/v48-remote-backup-e2e.js` 使用本地自签名 HTTPS/WebDAV 测试服务器验证连接、上传、下载、删除和 404 失败；这不等于第三方 WebDAV 服务商互操作通过。
- `pnpm test` 现在明确调用 `pnpm test:cwb-ai` 和 `pnpm test:v48`，避免 v4.8/AI 定向测试只存在于独立脚本而未进入发布主门禁。
- v4.8.5 正式工作流 [#32578966917](https://github.com/7752777/counselor-desk/actions/runs/32578966917) 的 Tests、Windows、macOS、Offline Web 和 Draft Release job 全部成功；v4.8.4 的失败候选 Tag 保留但不作为当前发布。

### v4.8.3 界面收口维护

- 学生台账记录级 AI 动作固定在学生行的 sticky 操作列，稳定宽度并允许长文案换行。
- 学生分页在 `390×844` 和 `360×800` 下保持下一页按钮可见；资料列表在窄屏退化为单列，操作按钮不再跑到视口外。
- 真实 Chromium 检查覆盖 `1440×920`、`390×844` 的学生台账、分页和资料列表；无页面 JavaScript 错误和文档级横向溢出。
- 本批不新增数据迁移、不改变同步、备份、附件、AI 授权或业务事实。

### v4.8.2 可靠性维护（历史维护）

- 局域网客户端状态保存现在串行化，异步保存失败会恢复最后一次成功的队列、冲突和游标快照，并通过 `waitPersistence()` 暴露真实完成边界。
- 学生、普通业务和自定义集合仓储写入失败时恢复内存数据；内存批量替换先完整验证全部记录，避免坏记录清空已有数据。
- 新增 `tests/v48-sync-persistence.js`，验证单次失败回滚、后续成功保存和排队快照顺序；相关同步、断网恢复、附件、桌面和管理 UI 定向测试随本版重新执行。

历史记录中的“已完成”只在其明确属于对应正式交付范围时才代表用户可用；带有“候选、工作区、未发布”的内容保留为过程审计。当前版本的判断顺序是当前源码和测试、当前构建配置、发布记录、历史审计。

## 已完成能力

### 业务与效率

- 学生台账筛选、排序、分页、派生字段缓存、5,000 名学生组合筛选优化。
- 稳定 `student_id` 关联、历史学号兼容、画像和跨模块时间线。
- 学生照片、家长、家庭地址、导师、班主任、住宿、紧急联系人等档案字段。
- 成绩、谈话、任务、资助、奖惩、纪律、心理、预警、帮扶、就业、住宿、党团、班委、活动和工作留痕。
- 通讯录、班级课表、科研课题、家校联系、活动参与统计、宿舍排宿/调宿。
- 班委自定义角色、多人任职、任期、班委考核。
- 查课、查寝、随机点名、宿舍异常、证书和活动照片附件。
- 实用工具、班级综合分析、学业趋势、工作统计摘要和待确认工作记录草稿。
- CSV/XLSX/HTML/DOCX/PDF 导出、模板中心、打印工作包和来源回链。

### 数据与存储

- 浏览器 IndexedDB、Electron SQLite、独立附件仓、单文件离线 HTML、备份恢复、交换包和手机工作包。
- v8/v9/v10/v11 迁移边界保持只增不删，业务关系继续以 `student_id` 为主键。
- 照片、证书、活动和业务附件使用附件 ID 引用，支持容量检查、哈希去重、失败回滚和孤儿附件清理。
- Electron 仓储健康检查、诊断错误码、版本化密钥、恢复包、可选安装目录和数据目录迁移已随 v4.8.0 交付。
- 局域网主机、一次性配对、二维码、HTTPS/证书指纹、设备暂停/恢复/撤销、离线队列、幂等、字段级冲突和分块附件传输已随 v4.8.0 交付；真实设备长期压力仍按限制抽查。

### AI

- provider、用途授权、每日额度、连接 readiness、取消/超时、relay、SSRF、防重定向和响应大小限制。
- 当前学生、班级、事项、日期、科研、住宿、课表和跨模块来源上下文。
- 默认脱敏、字段级一次性授权、来源核验、建议状态机、人工确认、建议转任务/谈话/留痕。
- 学生摘要、谈话 briefing、任务拆解、通知识别/改写、成绩与预警辅助、就业/竞赛、证书识别、心理语音转写和工作总结。
- AI 输出二次脱敏；内部 `student_id`、记录 ID、附件 ID、审计 ID、密钥和完整模型响应不进入出站或普通备份。
- AI 审计、草稿、建议、来源的原子保存和失败回滚；`CWB.ai.health()` 运行健康报告。

AI 不自动修改学生事实、心理/危机、纪律、资助、预警、奖惩、住宿分配或审批状态。

### UI 与交互

- 移动端抽屉导航、底部快捷栏、sticky 顶栏、分组搜索和常用栏排序基础。
- 桌面端左导航、中央工作区、可折叠右侧上下文面板基础。
- 统计条、筛选栏、状态标签、空状态、加载状态、错误重试和统一按钮层级基础。
- 学生台账表格/卡片/照片模式；Electron 已用合成学生数据验证卡片刷新和恢复。
- 业务按钮成功提示等待真实保存，失败保留输入、恢复内存并提供重试。

## 2026-08-22 本轮已落地维护

- 局域网离线队列按实际能力显示“桌面工作区队列”“IndexedDB 队列”或“兼容离线队列”。未启用 IndexedDB 时明确显示“未使用数据库级加密”，不再制造安全错觉。
- AI 通知预览和解析等待审计、来源、草稿、建议完成持久化后才返回；通知解析的建议改为最终统一提交。
- 敏感授权创建、授权消费、建议反馈、批量查看、语音授权和证书授权均等待真实保存结果；失败会回滚或保留重试状态。
- 当前修改新增 AI 输出保存失败、通知解析保存失败、局域网无 IndexedDB 文案和同设备多工作区隔离回归。
- 跨端 JSDOM 恢复夹具等待时间从 5 秒调整为 15 秒，以避免慢磁盘加载误报；这不是放宽产品逻辑，也不替代真实设备测试。

### 2026-08-22 AI 入口与批量审阅再次收口

- 清理了两套重复的证书识别实现。当前 `ACTS['ai-certificate']` 只指向带附件逐次授权、`persistOutput:false`、证书草稿和人工确认保存链路的 `startAiCertificateRecognition`；旧入口不再有机会绕过授权或直接写入奖惩档案。
- 建议中心“批量驳回”此前只改变内存状态，刷新后可能恢复；`aiReviewSuggestions()` 现在把批量状态、批量审计和失败回滚纳入同一 `persistAiMutation()` 事务，按钮等待真实保存完成后才清理选择并显示成功。
- 新增批量审阅成功和持久化失败回滚回归；`node tests/ai-contract.js`、`node tests/ai-source-integrity.js`、`node tests/interaction-continuity.js`、`node tests/v47-polish-ui.js` 和构建后预览均已重新通过。

本节记录的是发布前 AI 入口维护过程；相关能力已随 v4.8.0 交付，真实设备和服务商限制仍按本账和发布收尾记录执行。

## 2026-08-22 真实浏览器界面验收

在本地候选页面使用真实浏览器完成了以下视口检查：`1440x920`、`1280x800`、`1024x768`、`390x844`、`360x800`。

- `1440x920`：左侧导航、中央主工作区和右侧当前上下文三栏正常显示；首页统计条、今日待办、快捷动作和 AI 当前页入口可见，右侧上下文可折叠。
- `1280x800`：右侧上下文保留窄版宽度，页面没有横向溢出；`1024x768`：右侧上下文默认收起，可由按钮打开，遮罩、`aria-expanded`、Escape 和焦点返回均正常。
- `390x844`、`360x800`：导航抽屉、遮罩、底部五项快捷栏、顶部 sticky 栏可操作；抽屉打开时 `aria-hidden=false`，关闭后恢复为 `true`，Escape 和点击遮罩会关闭并把焦点还给菜单按钮。
- 导航搜索在分组折叠时仍能显示匹配项；移动端文档和页面均未发现横向滚动。
- 学生台账卡片模式实际显示学生卡片、班级、宿舍和业务摘要。验收时发现卡片模式错误插入两条表格滚动代理，已修复为仅表格模式插入；修复后代理数量为 `0`，卡片仍完整渲染。
- 顶部栏滚动后保持 `position: sticky` 且顶部位置为 `0`。

本节是本地真实浏览器 DOM/截图验收，不等同于 Electron 实机、真实手机、二维码扫码、证书信任或 macOS 验收。欢迎设置弹窗在完成/关闭后不会阻断移动抽屉；首次设置是否应再次提示仍由本机 onboarding 状态决定。

## 2026-08-22 交互回归纠偏

为匹配 AI 预览“等待审计与来源真正保存后再返回”的新公共语义，已更新 `CWB.ai.notice.preview()` 的页面入口和历史测试调用；通知页不再把 Promise 当作结果直接渲染。`pnpm test:cwb-ai`、`node tests/v45-teacher-feedback.js` 和 `node tests/v47-polish-ui.js` 已通过。

`node tests/interaction-continuity.js` 已不再卡在删除来源记录的 5 秒等待断言，改为 15 秒慢持久化夹具上限。随后定位到通知预览已经改为“先持久化来源/审计、再返回结果”，原测试却在预览前开启保存失败注入，导致预览阶段提前失败；测试现已调整为预览先成功、仅对确认事务注入失败。当前实际通过 `node tests/interaction-continuity.js`，并覆盖 AI 建议接受、通知确认、证书确认、工作总结、模型删除、业务表单和调宿等失败回滚与重试路径。

本次调整只修正了回归夹具的时序假设，没有放宽产品错误处理；通知预览仍必须等待来源和审计真实落盘，确认失败仍必须恢复建议和业务草稿。

## 当前验证证据

本轮修改后已通过：

```text
pnpm lint
node --check src/core/cwb-v48-ui.js
node --check tests/ai-output-health.js
node --check tests/v48-lan-ui.js
node scripts/build-release.js output/v4-preview.html
node tests/v48-lan-ui.js
node tests/ai-output-health.js
node tests/interaction-continuity.js
pnpm test:cwb-ai
node tests/v47-polish-ui.js
```

近期同一候选批次还通过了 `pnpm test:v48`、局域网配对、同步冲突、学生身份/批量/表单、派生分析、跨端恢复、备份记录和桌面备份调度等定向测试；AI 定向套件本轮重新通过。

当前代码状态下已重新通过完整 `pnpm test`（退出码 `0`）。测试覆盖导入/导出、AI、Electron/SQLite、附件、浏览器 IndexedDB、迁移、备份、交换包和 10,000 条大名单性能；本次性能样本约 `4.76s`，最大事件循环间隔约 `175ms`。JSDOM 输出的 `https://c.local` 资源加载错误仍是既有外部 origin 模拟噪声，相关断言通过。

`pnpm lint`、`pnpm build:release`、`pnpm check:public`、`pnpm check:secrets`、`pnpm test:release` 和 `git diff --check` 已在完整测试之后作为 v4.8.1 最终发布批次重新执行并通过；GitHub Actions run `32565590122` 又在 Tag 的干净环境中重复验证并成功发布资产。

## 发布后限制与持续验收

### 代码和文档收口

1. 持续审计新增 AI 保存入口和旧式 `save('custom')` 调用，重点补充无 IndexedDB 和真实 Electron 仓储边界。
2. 持续维护本总账与数据契约、迁移备份、隐私、架构、开发、用户手册、README、CHANGELOG、客户反馈、AI 审计、局域网记录和 v4.8.1 发布收尾文档的一致性。
3. 逐步完成桌面、局域网手机、WebDAV 和跨端附件的真实环境抽查；这些不阻断当前 v4.8.1 正式资产，但不得被自动化测试冒充为实机证据。

### 必须使用真实环境验证

- Windows 安装、自定义安装盘、数据目录迁移、损坏 SQLite/WAL 和密钥回滚。
- macOS 安装、迁移、附件读取和恢复。
- 手机真实扫码、HTTPS 证书指纹、断网重连和设备撤销。
- 多设备长时间同步、附件大数据量恢复和逐集合恢复抽查。
- 真实 WebDAV/HTTPS 服务商互操作。
- 真实麦克风权限、长音频转写和真实模型质量。
- 正式签名、公证、安装包和受控分发验证。

### 产品边界

- 重点学生 PIN 锁仍是“仅防误看”，不是数据库级加密。
- 浏览器关闭期间不能后台写文件或执行同步；网页端只保证本地快照和手动加密备份。
- 自动发现、自动信任自签名证书、云端明文实时同步不在当前发布范围。
- 尚无脱敏固定样本的 AI 事实准确率、中文质量、图片识别质量和教师满意度基线。
- 公开网页来源仍需用户主动触发和人工核验；没有后台全网爬取和语义向量知识库。

## 下一步执行顺序

1. 持续补充剩余 AI 保存链路和无 IndexedDB 边界回归。
2. 每个跨模块批次完成一次安全/持久化综合审查，并回链长期文档。
3. 继续用真实浏览器验收 `1440×920`、`1280×800`、`1024×768`、`390×844`、`360×800`，重点检查三栏、抽屉、底栏、sticky 顶栏、焦点、Escape、无横向滚动和卡片模式。
4. 发生代码、迁移或公共接口变更时，使用新的维护版本重新执行：

```text
pnpm test
pnpm lint
pnpm build:release
pnpm check:public
pnpm check:secrets
pnpm test:release
git diff --check
```

5. v4.8.1 已完成 Tag、Release 和正式分发包；下一次发布必须在新版本号、新 Tag 和新资产证据上完成同样门禁，不移动历史版本。

## 维护规则

以后新增功能必须同时提供：数据集合或字段说明、`student_id` 关联规则、迁移/备份/交换边界、敏感字段和 AI 出站规则、成功/失败/取消/重试行为、定向测试、真实验收需求和文档回链。任何“页面有按钮”都不能单独作为功能完成证据。

## 2026-08-22 IndexedDB 旧工作区阻断已修复

真实浏览器验收中发现：已有工作区点击通知 AI“本地预览重点”时，因物理 IndexedDB 仍停留在版本 5，新增 object store 没有创建，出现“指定 object store 不存在”，保存状态变为“保存失败，待重试”。

修复内容：

- `src/core/v4-runtime.js` 将物理 IndexedDB 版本从 `5` 增量到 `6`；工作区协议 `schema_version:8` 和业务 `data_schema_version:11` 不变。
- 升级只创建当前集合 manifest 缺少的 store，不清除旧记录，不生成新密钥，不覆盖旧数据库。
- `tests/v8-canonical-idb-browser.js` 增加真实 v5 旧库夹具，验证旧学生记录保留及 AI、同步、备份、内容推送新 store 创建。

修复后已通过 `node tests/v8-canonical-idb-browser.js`、`node tests/v40-idb-migration-browser.js`、`node tests/v8-browser-contract.js`、`node tests/v8-portable-history.js` 和 `pnpm test:cwb-ai`。本地真实浏览器重新加载已有工作区后保存状态为“已保存”；填写通知样例并点击“本地预览重点”后结果区正常渲染，没有保存错误。

这条阻断已经关闭，但它发生在全量门禁之后，因此之前的全量绿灯不能覆盖本次修改。当前仍必须重新执行最终全量测试、构建、公开面、密钥扫描、Release 契约，并完成 Windows/macOS、真实手机、WebDAV 和附件恢复验收。

完整的功能/美观/稳定性/AI/互通性总账见[当前全量状态与后续提升路线](./comprehensive-status-and-roadmap-2026-08-22.md)。

## 2026-08-22 修复后完整测试 checkpoint

IndexedDB 物理版本修复、同步学生稳定 ID 约束和相关回归加入后，重新执行完整 `pnpm test`，退出码为 `0`。本次通过范围包括：全量回归、v4.8 定向回归、桌面 Electron、AI、局域网、学生身份/导入、迁移、备份、附件和交换包。

10,000 条性能样本约 `4.76s`，最大事件循环间隔约 `175ms`。该结果是当前机器和测试夹具的性能证据，不是低配设备上的 P95 承诺。

本次构建生成 `output/辅导员工作台.html`，当时仍是工作区候选产物；随后 v4.8.0 和 v4.8.1 已由正式工作流完成 Tag、Release 和受控分发资产。

## 2026-08-22 AI 公共写入契约收口

本轮继续审计 AI 公共保存入口，确认建议创建、反馈、采纳、驳回、查看、批量审阅、转任务/谈话/留痕、敏感授权和通知确认均有统一的持久化承诺。返回对象提供非枚举 `ready` Promise 和 `persistence_state`；调用方可使用 `CWB.ai.awaitMutation()` 或对应 `*Async` 方法等待真实保存。页面入口已从只观察全局保存 Promise 改为等待返回对象本身。

AI 定向套件 `pnpm test:cwb-ai` 已通过，新增公共等待和失败回滚断言；最终完整门禁和正式资产已由 v4.8.0 发布工作流完成。

## 2026-08-22 最终自动化门禁回填

在 IndexedDB v5 到 v6 兼容修复、AI 公共写入等待语义和审计契约全部纳入当前工作区后，已重新完成一次最终自动化门禁：

```text
pnpm test                 # exit 0
pnpm lint                 # exit 0
pnpm build:release        # exit 0
pnpm check:public         # exit 0
pnpm check:secrets        # exit 0，检查 318 个仓库文件
pnpm test:release         # exit 0
git diff --check          # exit 0
```

本次完整测试覆盖全量回归、AI 治理和保存回滚、Electron/SQLite、浏览器 IndexedDB、v8/v11 迁移、备份、附件、手机交换包、局域网实现和 10,000 条导入性能；`pnpm build:release` 重新生成了离线 Web 产物。JSDOM 中的 `https://c.local` 加载失败仍是既有外部 origin 模拟噪声，相关测试断言已通过。

这组结果关闭了“当前代码没有最终自动化门禁证据”的发布前代码项。Windows/macOS、Web 正式资产和校验清单已上传到 v4.8.1 Release；真实手机二维码/HTTPS 信任、长期多设备同步、真实 WebDAV、照片/证书/活动附件实机恢复、签名/公证仍是发布后限制。

## 2026-08-22 最新权威状态（本文件末段优先）

本段覆盖前文阶段性 checkpoint。当前正式维护版本为 `v4.8.1`；`codex/ai-upgrade` 上的源码、测试和文档已与正式 Release 基线一致，后续文档回填不移动 Tag/Release。GitHub 仓库保持私有，正式安装包和离线 Web 分发物以 v4.8.1 Release 为准，Pages 不作为入口。

### 本轮新增修复

- 修复工作区大批量 `replace` 的重复深拷贝：`mutate()` 已提供私有命令副本，运行时不再对同一替换数组再次完整复制，同时保留危险键校验、状态隔离、历史记录和持久化前复制。
- 5,000 条记录替换性能断言由此前约 `202.2-222.7ms` 的偶发失败恢复为 `PASS v8-workspace`，没有放宽 `200ms` 业务阈值。
- 移动端学生台账表格模式继续保留紧凑备用卡片，避免全局移动端 `.tw` 隐藏后出现空白页面；桌面端仍显示真实表格，卡片/照片模式不受影响。

### 最新自动化门禁

本次修复后最后一次完整回归和发布门禁均为退出码 `0`：

```text
pnpm test
pnpm lint
pnpm build:release
pnpm check:public
pnpm check:secrets
pnpm test:release
git diff --check
```

`pnpm test` 覆盖全模块回归、导入导出、AI 治理与失败回滚、Electron/SQLite、浏览器 IndexedDB 旧库升级、v8/v11 迁移、备份、附件、交换包和 10,000 条大名单性能。Web 资产已由正式 Release 工作流重新构建并上传；本地 `output/辅导员工作台.html` 仍不替代 Release 下载物。JSDOM 中反复出现的 `https://c.local` DNS 错误是外部 origin 模拟噪声，未导致测试断言失败；它不能当作真实网络验收结果。

### 最新真实界面与桌面证据

- 真实浏览器已验收 `1440x920`、`1280x800`、`1024x768`、`390x844`、`360x800`：三栏工作区、移动抽屉、遮罩、Escape、焦点返回、sticky 顶栏、导航搜索、底栏、无横向滚动和学生表格/卡片/照片模式均通过。
- Windows x64 移动修复后重新构建通过；打包可执行文件持久化烟测和 NSIS 隔离目录安装、卸载保留数据、显式删除数据烟测通过。
- Windows ARM64：正式发布工作流已完成重建、校验并上传，资产哈希见[v4.8.1 发布收尾记录](./release-v4.8.1.md)。
- macOS Universal：正式发布工作流已完成 DMG/ZIP 构建、挂载/打包烟测、校验并上传；包未签名、macOS 未公证。

### 下一阶段按优先级执行

1. Windows 实机补充自定义安装路径、工作区迁移、损坏密钥/SQLite/WAL、恢复口令、附件回读和失败回滚演练。
2. macOS 实机补充 Universal 安装、迁移、附件和恢复演练；签名/公证仍作为单独发布后改进项。
3. 真实手机完成二维码扫码、HTTPS 证书指纹人工核对、配对撤销、断网队列、重连、附件分块和多设备长时间压力。
4. 使用真实用户自有 WebDAV/HTTPS 存储完成加密备份上传、下载预览恢复、超时、删除和失败恢复；保留脱敏日志和服务商差异。
5. 建立脱敏 AI 质量基线：通知、谈话、成绩、就业、政策、证书和语音分别记录引用完整性、敏感字段出站、事实准确性、人工修改量和失败率。
6. 真实环境证据形成后回填 v4.8.1 发布收尾记录；涉及代码、迁移或公共接口的新改动创建 `v4.8.2` 或更高维护版本，不移动已发布的 `v4.8.1` Tag。

本轮没有进行源码哈希检查；哈希只在最终正式产物生成时执行。没有启动独立审查代理；本轮按高风险持久化修改由实现代理完成批次自检、定向验证、真实浏览器验收和一次最终全量门禁，后续在真实设备发布前仍需一次整体安全/架构审查。

## 2026-08-22 发布后维护候选：证书出站确认与恢复口令提示

v4.8.0 正式 Release 发布后继续执行 AI 与桌面入口抽查时，发现两处不会直接破坏数据、但会影响用户安全理解和恢复操作的细节，已在工作区维护候选修复：

1. 证书识别授权弹窗改为明确显示“确认发送证书图片给模型”。附件仍先归档到本地仓，必须逐次授权后才发送单个附件，结果先进入草稿，人工选择学生并核对字段后才写入获奖档案。`node tests/certificate-recognition.js` 通过。
2. 恢复向导导出/导入均明确要求至少 12 位恢复口令；导出增加二次输入确认、`minlength` 和处理中状态，失败保留当前表单和重试入口；导入错误口令不会替换当前数据。`node --check src/core/cwb-v48-ui.js`、`node tests/v48-core.js`、构建后 `node tests/v48-management-ui.js` 通过。

本节对应的是 v4.8.1 维护候选，现已完成冻结并发布为 v4.8.1；不改变 v4.8.0 历史 Tag、Release、Windows/macOS/Web 正式资产或其哈希。后续维护代码应创建 v4.8.2 或更高版本并重新计算资产哈希。

# v4.9.0 商业发布清单

> 当前适用口径：v4.9.1 前瞻版。本文记录 v4.9.0 方案的历史门槛；当前前瞻版已经把四档商品码、友情 AI 托管码和更新权益隔离落到服务端，但仍未达到正式签名商业发布门槛。

本文是发布前的事实清单。当前状态为候选，任何未打勾项目都阻止正式商业交付。

## 代码与测试

- [x] 许可证解析、Ed25519 校验、四档权益和离线宽限纯函数。
- [x] Electron `safeStorage` 专用许可证状态与原子写入。
- [x] AI 入口统一守卫和商业模式锁定 UI。
- [x] 更新清单版本、签名、哈希、下载取消和失败状态。
- [x] 独立发码服务本地契约、设备上限和撤销。
- [x] PostgreSQL 参数化 store、只增量 schema 迁移、订单幂等、支付事件幂等、邮件 outbox、管理员路由和外部 KMS signer 契约。
- [x] 更新清单生成/签名工具和商业发布工作流的签名凭据 fail-closed 检查。
- [x] Electron 下载后接入 Windows Authenticode、macOS codesign/spctl/pkgutil 的平台签名校验；未知平台和缺少校验器时拒绝安装。
- [x] 更新安装状态写入独立状态文件；安装器异常、版本未更新和更新后健康检查失败会进入恢复点回滚路径，并保留失败目录。
- [x] 客户页、订单访问令牌和受保护 `.cwb-license` 下载接口；普通订单查询不返回激活码。
- [x] 激活文件/二维码导入只填充候选，不自动激活；设备解绑和二维码降级提示有专项契约测试。
- [x] 桌面商业配置构建器、公钥/HTTPS 配置 fail-closed 检查和订单访问令牌强制校验。
- [x] `v4.8.5` 数据协议读取不变，许可证不进入业务备份。
- [x] 2026-08-23 已在候选代码和文档收口后重新执行 `pnpm test`、`pnpm lint`、`pnpm build:release`、`pnpm check:public`、`pnpm check:secrets`、`pnpm test:release`、`pnpm test:commercial` 和 `git diff --check`；全部通过。该结果不替代真实生产服务、平台签名、公证和 Windows/macOS 实机证据。

## 外部发布前置

- [ ] 真实 PostgreSQL 授权库、KMS/HSM 私钥托管和公钥轮换。
- [x] 源码适配器已提供管理员认证、限流、审计、退款/撤销、订单幂等和邮件 outbox；[ ] 真实管理员身份、支付、邮件和告警服务仍待部署。
- [ ] 支付 webhook 验签，不能以客户端回调作为支付成功依据。
- [ ] 真实生产公钥、授权服务地址和更新 feed 注入商业构建，并在目标平台确认 fail-closed。
- [x] 发布工作流已在凭据缺失时拒绝构建；[ ] Windows 代码签名证书和 macOS Developer ID/公证仍待配置并实测。
- [x] 更新 runtime 已实现清单、哈希、下载取消、安装状态持久化、失败回滚和旧目录保留契约；[ ] 更新 CDN、真实安装器断点/失败重试和跨重启恢复仍待实机。
- [x] 客户购买/订单/许可证文件交付页面和订单访问令牌边界已实现；[ ] 真实支付、邮件、域名、TLS、限流告警和售后流程仍待部署。
- [ ] Windows/macOS 安装、升级、三设备、换机、退款、断网 30 天和时钟回拨实测。

## 发布顺序

完整测试 → 生成候选产物 → 真实平台安装/升级/恢复 → 产物签名与公证 → 生成 SHA-256 和签名清单 → 创建 Draft Release → 校验全部附件 → 公开 Release → 交付客户更新地址。任一步缺少证据都停留在 Draft，不移动 `v4.8.5` Tag/Release。

## 当前不能对客户承诺

当前工作区不能承诺支付后自动发码、云端实时同步、不可破解 DRM、签名安装包、macOS 公证、后台静默更新或生产授权服务已可用。AI 模型费用始终不包含在产品买断价内。

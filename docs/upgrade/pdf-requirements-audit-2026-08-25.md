# `1.pdf` 参考需求逐项审计（2026-08-25）

> 当前适用口径：v4.9.1 前瞻版已将前瞻活动更新为四档商品码加一枚友情 AI 托管码；本文中的旧“两枚长期活动码”仅是审计时点记录，不作为当前商品规则。

## 1. 审计范围

本次审计以 `C:\Users\22797\Desktop\1.pdf` 为输入。文件共 18 页，已完成逐页文字提取，并按页面中的功能场景、操作控件和视觉结构逐项对照当前工作区候选代码。

参考文件只用于提取产品体验和信息架构，不作为本项目业务规则、数据来源或品牌素材。参考产品的名称、文案、图标、数据和具体实现没有复制。

### 证据等级

| 等级 | 含义 |
| --- | --- |
| 已落地 | 当前源码已有对应行为，并有定向测试或可复核代码证据 |
| 部分落地 | 主要流程已实现，但仍依赖真实服务、浏览器能力或人工视觉验收 |
| 待外部验收 | 代码已有接口或适配器，但不能用本地契约测试代替真实设备、生产服务或平台验证 |

## 2. 18 页功能对照

| 参考页 | 参考场景 | 提取到的关键功能 | 当前对照结论 |
| --- | --- | --- | --- |
| 1-2 | 宿舍查寝与系统设置 | 楼栋、房间、查寝批次、异常、统计、校历和教学周 | 查寝记录、异常处理、楼栋/房间统计已接入；真实校历来源和学校配置仍需维护者录入 |
| 3-4 | 班级查课与量化考评 | 今日课程、正在上课、查课打卡、随机点名、积分维度、排名和规则 | 查课、点名、量化规则/明细、排名和人工确认流程已接入；学校正式规则仍不能由 AI 自动确认 |
| 5-6 | 工作台与工具箱 | 问候、KPI、今日课表、待办、风险图表和外部工具入口 | 首页摘要、今日待办、课表、快捷工具、风险聚合和安全外链已接入 |
| 7-8 | 日程与待办 | 未完成/已完成、逾期、学生关联、快速创建和日期分组 | 任务快速创建、学生关联、起止日期、逾期、时间轴和甘特视图已接入 |
| 9-10 | 通知 AI 与课堂点名 | 原文/结果双栏、风格选择、复制、重新生成、点名结果和大屏准备 | 通知解析/改写、草稿、复制、失败重试、转任务/留痕和点名记录已接入；真实模型能力依赖配置 |
| 11-12 | 就业与竞赛 | 求职导航、简章解析、防骗红黑榜、竞赛资源、报名成员和模板 | 就业资源、防骗台账、竞赛资源/报名/成员/附件和安全 URL 校验已接入 |
| 13-14 | 谈话与学生档案 | 谈话登记、风险评估、历史、全量筛选、照片和档案字段 | 学生台账、照片、稳定 `student_id`、谈话、历史、筛选和上下文入口已接入；语音转写仍需独立音频模型真实验收 |
| 15-16 | 党员与处分 | 阶段统计、流程日期、状态筛选、解除、重点学生和导出 | 党团流程、处分、附件、状态、时间轴、导出和人工确认已接入 |
| 17-18 | 学业分析 | GPA、挂科课程、预警、班级均值、学生下钻和帮扶入口 | GPA/挂科/预警/班级聚合、学生下钻和帮扶联动已接入；缺失数据保持“未记录” |

## 3. UI 与交互逐项对照

### 已落地

- 桌面端采用固定左侧导航、中央主工作区和可折叠右侧上下文区。
- `>=1280px` 显示右侧上下文，`1024-1279px` 默认收起，`<=900px` 退化为导航抽屉、单列和底部快捷操作。
- 导航支持分组折叠、搜索、恢复、钉住常用项和自定义分类；自定义分类刷新后保留。
- 页面统一使用标题、统计摘要、筛选、主操作、结果区的顺序，表格与移动卡片保留同一业务动作。
- AI 页面支持平台 AI、自有 API Key 和高级自建服务三种配置方向；平台模式不要求用户填写共享模型密钥。
- AI 敏感用途以可读复选框呈现，敏感类别默认关闭，发送前逐次授权。
- AI 调用日志只保留用途、模型、状态、范围、来源数量、耗时和审计关联，可回到原业务页面。
- 失败和取消状态不会丢掉页面上下文；当前进程保留可重试文本，持久化 UI 状态不保存 Prompt、API Key 或完整响应。
- 任务支持开始日期、结束日期、截止日期、优先级、列表、时间轴和甘特视图；只有截止日期的任务显示为单日任务，日期倒置会按时间顺序展示。

### 部分落地或待外部验收

- 参考 PDF 的真实视觉页面尚未在本环境全部渲染为 PNG 对照。环境中没有 `pdftoppm`、`pdftocairo`、`mutool`、`gs` 或可用的 ImageMagick PDF 渲染器；本次完成了文字提取和源码/运行时检查，视觉像素级对照需要在有 Poppler 的环境补做。
- 平台 AI 的真实 Relay、学校域名、生产限流和第三方模型质量不由本地 JSDOM 测试证明。
- 真实手机摄像头二维码、移动麦克风、Windows/macOS 包签名、公证和局域网跨设备恢复仍需实机验收。

## 4. 统一通用兑换码边界

前瞻体验阶段允许配置两个长期活动兑换码：

1. 普通永久更新版：完整基础功能，不解锁 AI。
2. 永久 AI 增强版：解锁 AI 入口，但仍要求用户配置自己的模型 API Key，并自行承担模型服务费用。

同一个活动码可以给多位前瞻体验用户使用。服务端只保存活动码哈希，不把明文写入源码、客户端、日志、业务备份或 Git；首次兑换时按工作区签发独立 Ed25519 许可证，因此每个工作区仍有独立设备列表、刷新、撤销和审计。活动码泄露时可以暂停整个活动，已经签发的工作区许可证可以单独撤销。

这是一种前瞻体验权宜方案，不是不可破解的 DRM，也不适合替代正式购买许可证。活动码由 `services/license-server/scripts/generate-redemption-codes.cjs` 在受控终端生成，部署配置只接收哈希。

## 5. 本轮定向证据

已通过：

```text
node tests/pdf-requirements-ui.js
node tests/ai-workflow-ui.js
node tests/redemption-code.js
node tests/redemption-service.js
node tests/redemption-http.js
node tests/redemption-client.js
node tests/navigation-structure.js
node tests/ai-provider-readiness.js
node tests/ai-relay.js
node tests/ai-relay-license.js
node tests/mobile-navigation.js
pnpm lint
git diff --check
```

本页不把这些定向结果写成正式商业发布证明。最终代码状态仍需执行全量测试、构建、公开面检查、密钥检查、发布测试和真实平台验收。

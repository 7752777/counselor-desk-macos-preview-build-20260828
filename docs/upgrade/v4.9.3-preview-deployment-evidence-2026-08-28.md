# v4.9.2 前瞻版公网更新与 r16-final 交付证据

更新时间：2026-08-28

## 结论

`license.windsky.store` 的授权服务、下载中心和 preview 更新入口当前可达。2026-08-28 已在本机完成 v4.9.2 r16 前瞻交付包及各平台产物的生成与校验；本轮已用部署密钥登录目标机并将公开产物暂存到 `/home/ubuntu/cwb-v492-r16-upload`，但公网目录仍返回上一轮同名文件。提升到 Nginx 目录和更新 API 还需要目标机 root 权限及管理员发布凭据。本文只记录可复核的公网入口、本地产物关系和限制，不记录激活码、API Key、服务器密码、管理员密钥或更新签名私钥。

## 公网入口

| 用途 | 地址 | 验证结果 |
| --- | --- | --- |
| 授权服务健康检查 | `https://license.windsky.store/api/v1/health` | HTTP 200 |
| 四档产品目录 | `https://license.windsky.store/api/v1/products` | HTTP 200 |
| 购买与交付页 | `https://license.windsky.store/customer` | HTTP 200，页面标题和版本标识已切换为 v4.9.2 |
| preview 更新清单 | `https://license.windsky.store/api/v1/updates/latest?channel=preview` | 匿名 HTTP 401；10/40 元档 HTTP 403；20/60 元档 HTTP 200，Ed25519 签名有效 |
| v4.9.2 下载中心 | `https://license.windsky.store/downloads/v4.9.2/` | HTTP 200 |
| Windows x64 更新包 | `https://license.windsky.store/downloads/preview/counselor-desk-4.9.2-x64.exe` | HTTP 200；公网 `Content-Length` 92,541,202，仍是上一轮文件，不是本地 r16 |
| Windows ARM64 更新包 | `https://license.windsky.store/downloads/preview/counselor-desk-4.9.2-arm64.exe` | HTTP 200；公网 `Content-Length` 86,974,603，仍是上一轮文件，不是本地 r16 |
| macOS Universal 更新包 | `https://license.windsky.store/downloads/preview/counselor-desk-4.9.2-mac-universal.zip` | HTTP 200；公网 `Content-Length` 196,917,553，仍是上一轮文件 |
| macOS Universal DMG | `https://license.windsky.store/downloads/preview/counselor-desk-4.9.2-mac-universal.dmg` | HTTP 200；公网 `Content-Length` 197,516,411，仍是上一轮文件 |
| 完整前瞻交付包 | `https://license.windsky.store/downloads/v4.9.2/counselor-desk-v4.9.2-preview-package.zip` | HTTP 200；公网 `Content-Length` 631,905,302，仍是上一轮文件；本机 r16-final 为 631,674,730 bytes |
| 购买与激活速查 | `https://license.windsky.store/downloads/v4.9.2/counselor-desk-v4.9.2-activation-guide.pdf` | HTTP 404；当前固定文件名尚未部署 |

preview 更新入口和下载中心目录可通过 HTTPS 访问，但本轮探针确认其中的同名包仍是上一轮文件，且固定的购买与激活速查 PDF 地址返回 404；不能把当前公网目录宣传为已部署 r16。更新清单不是公开下载目录：匿名请求会被拒绝，只有已激活且拥有 `perpetual_updates` 权益的设备可以读取。

客户端更新地址固定为：

```text
https://license.windsky.store/api/v1/updates/latest?channel=preview
```

桌面端启动后低频检查，用户也可以在“版本更新”中手动检查。v4.9.0 及以上、且带有 preview 更新配置的桌面包会先提交已激活设备凭据；一次性测试许可证已实际验证：10 元普通版和 40 元 AI 增强版返回 `403 LICENSE_UPDATE_NOT_ENTITLED`，只有 20 元普通永久更新版和 60 元永久 AI 增强版可以读取该清单。当前已经是 v4.9.2 时不会重复下载；r16 与现有服务器清单版本号相同，不能依靠软件内更新把旧 r15 替换成 r16。

## 本轮产物

| 产物 | 本地校验 |
| --- | --- |
| Windows x64 | 92,546,109 bytes；`8536dbd79f94cd4f69cb1bac5cb141ba4e5eaa9040a7b1ff5c4e8615aebe9d1e` |
| Windows ARM64 | 86,979,493 bytes；`b851df769db0b74cb1cad1663d420edc0eacbb9347b86ae69d487270e87050b9` |
| macOS Universal ZIP | 196,930,953 bytes；`6bb3a1a485a4608c5b01838269b7ff36038a23744ec3dbf167df23192421576a` |
| macOS Universal DMG | 197,492,876 bytes；`7c8d5566e1e35b76772df0c7be01e0b88de578c4b9b7d0c733a4b0aea8e43b01` |
| 唯一离线网页 | 14,707,658 bytes；`74242bb9f1f4278c2049e6d59d1b8ccc6d9bcc65afc3aaab6c8ec5a1acab3ebe` |
| 完整前瞻交付 ZIP（本机 r16-final） | 631,674,730 bytes；SHA-256 `b546792219dce1c98a539b4880030cdfb5062ebfe6da9da6f52189397a94d67a` |
| 完整产品手册与功能说明书（各 20,828,107 bytes） | `00b470c75993f29829e68137b5488f23e890230a69d4b9e0b7b26d3e1e3e9cbd` |
| 购买与激活速查 PDF（13,194,958 bytes） | `5b43489aca1bc96a65214148cb71cfddd9df9667d33afb590758d03f3a044f8c` |

本机 r16-final 的包大小和 SHA-256 已与包内逐文件清单比对；公网同名文件尚未与本机 r16-final 一致。完整前瞻交付包包含网页端、三份 PDF、安装说明、更新元数据和脱敏导入样表；真实激活码和任何密钥均不在包内。

## 2026-08-28 服务器暂存与发布阻塞

| 项目 | 结果 |
| --- | --- |
| SSH 到 `82.156.201.93` | 已通过 `ubuntu` + 部署密钥建立 BatchMode 连接；主机返回 `VM-4-6-ubuntu` |
| 目标目录 | `/var/www/counselor-downloads/v4.9.2` 与 `/var/www/counselor-downloads/preview` 存在，但均为 `root:root`、当前账号不可写 |
| 暂存目录 | `/home/ubuntu/cwb-v492-r16-upload` 已上传公开安装包、交付 ZIP、三份 PDF、网页、更新元数据和原子发布脚本 |
| 暂存完整性 | 12 个大文件大小和 SHA-256 与本地产物逐项一致；暂存不改变公网内容 |
| root 权限 | `sudo -n` 不可用，root 直登未授权；需要服务器控制台/管理员提供 sudo 权限或执行发布脚本 |
| 更新 API 发布 | 需要服务器保管的更新签名私钥和管理员 API Key；二者不在本机、不写入仓库或聊天 |

暂存目录中的 `publish-preview-v4.9.2.sh` 会先备份当前同名文件，再原子替换 preview 与 v4.9.2 静态文件，随后使用服务器签名器签署 `manifest-v4.9.2-preview-unsigned.json`，调用 `/api/v1/admin/updates/publish`，最后打印目标文件哈希。获得 root 和管理员权限后，在服务器上执行：

```bash
sudo -n bash /home/ubuntu/cwb-v492-r16-upload/publish-preview-v4.9.2.sh
```

脚本从 `/opt/counselor-license/secrets/staging.env` 读取部署环境变量；不需要把任何密钥写入命令行。执行后必须重新做下方公网复核，确认下载大小、SHA-256、签名清单和 404 文件均已更新。

## 更新边界

- 当前 preview 包的清单签名和 SHA-256 校验有效，但平台包使用明确的 `unsigned-preview-v1` 诊断标记。
- Windows Authenticode、macOS Developer ID 签名和公证尚未完成，不能把 preview 包宣传为正式稳定商业版。
- 更新不会删除 SQLite、附件仓、工作区数据或局域网配对配置；安装失败的恢复契约已由自动化测试覆盖，但跨重启实机回滚仍需 Windows/macOS 验收。
- 网页在线版通过刷新获取新版本；单文件离线 HTML 需要重新下载新文件，浏览器关闭后不会自动替换本地文件。
- 支付、自动邮件发码、正式 KMS/HSM、正式商业 CDN、退款/换机售后和真实设备验收仍是正式商业发布前置条件。
- 公众号当前只保存自动回复和文章草稿，不直接群发；后台已核对主推文草稿和“工作台/前瞻版”关键词规则处于保存状态。文章和自动回复的本地源稿分别为 `docs/wechat-launch-article-v4.9.2.md` 与 `docs/wechat-auto-reply-v4.9.2.md`。本轮不把公众号操作状态当作服务器部署证明。

## 复核命令

```powershell
Invoke-WebRequest https://license.windsky.store/api/v1/health
# 匿名请求应返回 401；不要把真实许可证凭据写进脚本、日志或文档。
try { Invoke-WebRequest 'https://license.windsky.store/api/v1/updates/latest?channel=preview' } catch { $_.Exception.Response.StatusCode }
Get-FileHash output/desktop/counselor-desk-4.9.2-x64.exe -Algorithm SHA256
Get-FileHash output/desktop/counselor-desk-4.9.2-mac-universal.zip -Algorithm SHA256
Get-FileHash output/desktop/counselor-desk-4.9.2-mac-universal.dmg -Algorithm SHA256
```

以上命令只验证公网可达性和最终产物完整性，不会读取任何真实兑换码或服务器秘密。

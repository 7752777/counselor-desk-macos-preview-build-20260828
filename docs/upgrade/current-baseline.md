# 现状全景与差异基线

更新时间：2026-08-25

v4.4.6 的历史正式上线证据见[发布收尾记录](./release-v4.4.6.md)；v4.5.0 老师反馈逐条实现、数据迁移、浏览器验收和已知限制见[老师反馈全量审计](./teacher-feedback-audit-2026-08-18.md)。v4.6.0 日常工作与业务协同批次见[本轮审计](./next-optimization-audit-2026-08-19.md)和[发布收尾记录](./release-v4.6.0.md)。v4.7.0 发布前的收口修复、局部测试与长期限制见[收尾查漏补缺审计](./closeout-audit-2026-08-19.md)，历史发布证据以[v4.7.0 发布收尾记录](./release-v4.7.0.md)为准；当前 AI 收口逐项状态见[2026-08-20 AI 全面收口审计](./ai-comprehensive-audit-2026-08-20.md)。

全版本、AI、UI、仓库私有化、发布证据和未完成事项的单一对账入口是[全量开发计划对账](./full-plan-reconciliation-2026-08-19.md)；带有“候选/未发布”字样的段落保留当时审计语境，当前事实以本文件的 v4.8.5 权威状态和[发布收尾记录](./release-v4.8.5.md)为准。

当前工作区的功能、界面、稳定性、AI、互通性和后续路线另有一份按证据分层的[当前全量状态与后续提升路线](./comprehensive-status-and-roadmap-2026-08-22.md)。该文档明确区分 v4.8.5 正式代码、自动化证据和仍需真实环境核验的限制；不把局部自动化结果扩大为手机、服务商或签名验收。

当前工作区还包含 v4.9.0 商业候选。授权核心、AI 解锁、生产服务适配器、更新状态机和候选交付页已落在源码并有契约测试；生产启动器现在要求显式生产标记、PostgreSQL TLS、精确 HTTPS CORS、HTTPS 强制和数据库哈希管理员 Key。逐项范围、最新局部验证和未完成外部证据见[v4.9.0 实施对账与发布前审计](./v4.9.0-implementation-reconciliation-2026-08-23.md)。

2026-08-25 PDF/UI 收口：`1.pdf` 共 18 页，已完成文字提取和功能逐项对照；AI 配置分为平台 AI、自有 API Key 和高级自建服务，敏感用途默认关闭，调用日志支持失败重试和原页面回链；导航支持历史、分组、自定义分类和常用钉住；任务支持起止日期、时间轴和甘特图。新增 `pdf-requirements-audit-2026-08-25.md` 与 `ai-navigation-task-workflow-2026-08-25.md`。当前前瞻商品体系为四档服务端活动兑换码，分别对应 10/20/40/60 元档，另有一枚友情 AI 托管码；明文不进入客户端，兑换后每个工作区独立签发许可证，更新权益由 20 元/60 元档严格隔离。`node tests/pdf-requirements-ui.js`、`node tests/ai-workflow-ui.js` 和 `pnpm test:redemption` 已通过。PDF 页面 PNG 视觉对照因当前环境缺少 Poppler/同类渲染工具尚未完成，真实 Relay、支付、签名和跨平台实机验收仍是候选发布前置条件。

2026-08-23 使用者视角维护补丁见[使用者视角持续打磨审计](./user-centered-polish-audit-2026-08-23.md)：首次使用改为设置资料、导入名单、首次备份三步可执行路径；空状态补充下一步入口；保存失败和 AI 未授权/未配置/缺少密钥状态使用用户可理解的错误说明和重试入口。该补丁未新增迁移，真实 Electron、HTTP 预览和手机视口仍需继续验收。

2026-08-24 长期使用体验继续收口：首次设置保存等待真实持久化后才关闭，失败保留输入并支持重试；设置完成后不再连续弹出今日问候，问候改为首页内联提示，可换句、稍后再看或关闭；首次引导将“先体验示例”作为主动作，明确成绩和其他资料可以后续补录。AI 工作台新增文本、图片、语音三类能力状态矩阵，并在连接表单中补充中转地址/令牌配置说明。该批次未新增数据迁移，真实 Electron、手机麦克风、生产模型和签名发布仍未验收。

2026-08-24 长期补录与关联可见性继续收口：学生台账根据非示例学生的真实缺口派生最多三条下一步建议，联系方式/住宿可直接进入现有批量编辑，成绩、照片和旧关联进入对应模块；不把缺失字段当作零，也不新增重复事实表。学生详情新增稳定 ID、仅学号快照和涉及模块摘要，旧快照提供人工关联维护入口。首次引导步骤、完成、跳过和重置均等待真实设置保存后才跳转或刷新，失败恢复原状态。新增回归见 `tests/v48-student-import-ui.js`；录音仍需真实服务商、Electron 和手机设备验证，v4.9.0 仍为候选。

2026-08-24 持续编辑可靠性补充：工作节点、政策、素材、竞赛和模板等旧表单已统一等待真实写入，失败会恢复列表和编辑对象本身，用户可直接重试；新增 `pnpm test:transactional` 验证新增/编辑失败回滚和重试对象连续性。此前一次 45 秒限时运行未完成属于旧的运行时观察；在本批代码稳定后重新执行 `node tests/interaction-continuity.js` 已通过，当前不能再把它列为未通过证据。语音能力的真实前置仍是独立转写模型、服务商 `/audio/transcriptions`、可用额度、录音 MIME 和麦克风权限；文本接口可用不代表录音可用。

2026-08-24 长期变更映射补充：学生主档案、进行中事项快照和历史事实已明确分层。单人编辑、批量编辑和学生增量导入会同步带稳定 `student_id` 的未完成任务、待确认工作记录草稿、未结束住宿/查寝异常以及仍在进行的任职/就业/竞赛快照；已完成任务、成绩、谈话、奖惩、资助、心理、查课、点名和住宿轨迹不自动重写；只有学号快照没有稳定 ID 的旧记录仍需人工核对。失败保存和撤销会恢复联动记录。新增 `pnpm test:long-term`；本批没有使用暴露在聊天中的 API Key，也没有宣称目标中转站、真实麦克风、商业授权或发布门禁已经验收。

全局设置、学生筛选方案和学生台账列视图同样接入对象级事务保存；失败时恢复原设置引用和嵌套偏好，成功后才关闭窗口并提示。长期使用审计新增了设置偏好也必须具备刷新/重启一致性的约束。

2026-08-24 配置持久化补充：远程备份地址、认证模式、测试状态和上传状态统一通过 `persistSettingsMutation()` 保存；自动备份计划在桌面密钥设置、工作区设置或调度器失败时恢复原计划。学生台账列视图切换也等待设置写入后再刷新，失败保留原选择。该批验证通过 `pnpm lint`、`node tests/v40-backup-schedule.js`、`node tests/transactional-array-mutation.js`、`node tests/interaction-continuity.js`、`node tests/v48-student-import-ui.js`、`node tests/v47-polish-ui.js`、`node tests/ai-voice-contract.js` 和 `git diff --check`。

2026-08-24 长期偏好与统计视图补充：首次功能导览、欢迎体验重置、每日问候已读状态和首页自定义统计视图都使用同一排队的设置事务。写入失败时恢复原设置对象和已打开界面的引用；功能导览仍会打开，但会明确提示“已读状态未保存”，不会悄悄掩盖失败。领导统计视图不再并行写浏览器设置与工作区适配器，消除了两条写入链路在桌面仓储异常或连续操作下产生不同结果的风险。定向验证为 `node tests/leadership-dashboard.js`、`node tests/mobile-navigation.js`、`node tests/v39-features.js`、`node tests/v47-polish-ui.js` 和 `pnpm lint`；真实浏览器已通过 58 个主路由 × 5 个视口，离线 HTML 已通过 232 次路由渲染 × 4 个视口。正式商业发布仍需独立完成真实 Electron、目标转写服务、手机麦克风和签名安装验证。

本轮继续收口首页和轻量状态链路：示例数据状态优先提供“导入真实名单”，首页待办首屏严格限制为最紧急 5 条；首页折叠和路由状态采用合并排队的工作区设置写入，避免 UI 状态写入阻塞通知预览等业务操作。相关回归见上述审计和 `tests/v47-polish-ui.js`；真实 Electron、HTTP 预览和手机视口仍需继续验收。

2026-08-24 优先级收口后，普通编辑、批量/带附件删除和基础记录更新已统一到持久化分区队列；自定义集合按整体 `custom` 分区串行化，减少连续录入、切页和失败回滚时的覆盖风险。Electron 商业候选增加启动后约 30 秒、随后每 6 小时的低频自动更新检查，发现后只提示用户，不静默安装；更新仍要求签名清单、SHA-256、平台签名、安装前恢复点和重启后仓储健康检查。新增 `desktop/update-scheduler.cjs` 和 `tests/update-scheduler.js`，定向更新与桌面契约已通过。当前正式维护发布仍为 `v4.8.5`，工作区 `4.9.0` 仍未取得真实更新 CDN、Windows/macOS 证书、公证、跨重启设备证据和微信支付商户配置，不能把源码候选写成完整商业 Release。

同批次还修复新增就业意向、就业联系和业务档案的学生选择连续性：表单不再默认选中名单第一人，保存时按当前选择写入稳定 `student_id` 和学号快照。`tests/interaction-continuity.js` 已覆盖从选择学生到保存的跨页面链路。

本轮继续收口学生关联表单：请假、考勤、预警、帮扶、奖助勤补、重点学生、心理、毕业生、住宿、假期去向和评优申报统一保存稳定 `student_id`；成绩、奖惩、活动和工作留痕 v4 表单补充学生下拉与当前选择复核。保存失败会恢复原数组并保留表单，重试不重复新增；新增回归覆盖传统考勤表单失败回滚和稳定 ID。该批次不新增迁移，真实 Electron、HTTP 预览和手机视口仍需继续验收。

本轮学生导入体验继续收口：学生台账把名单增量更新与课程成绩录入分开引导；导入预览显示稳定学生 ID、当前学号或历史学号的实际匹配依据，以及本次真正会写入的字段。期末只有“学号、姓名、分数”的文件会更新已有学生综合成绩，不会因缺少培养层次重复建档；空白字段默认保留。对应运行时契约和定向回归见上述使用者视角审计及 `tests/v48-student-import-ui.js`。

本轮全量收口以[全量收口审计](./full-plan-closeout-audit-2026-08-23.md)为准：任务、v4 业务记录和文件批量操作已统一等待真实持久化并在失败时回滚；示例数据已扩展到通讯录、就业、文件目录和活动参与关系。当前分支仍是商业候选，真实支付、KMS、代码签名、公证、客户 CDN、手机实机和外部 AI 视觉/音频验收尚未完成。

2026-08-23 维护补丁见[学生增量导入、样例数据与批量操作审计](./student-import-and-demo-audit-2026-08-23.md)：期末只含“学号、姓名、分数”的文件不再因缺少培养层次而新建重复学生；示例数据扩展到跨模块体验场景；四类业务档案补齐批量状态更新、删除和失败回退；通用业务 CSV、通讯录、课表、就业资源和量化积分导入已补自然键更新、文件内去重、冲突预览和仓储失败回滚；AI 适配器增加 Responses API，并已用固定脱敏请求核验当前中转站的文本能力。视觉和音频仍以服务商明确支持和真实配置测试为准。用户聊天中暴露的 Key 不作为验证凭据，必须撤销后重新生成。

本文件区分“源码中已经存在的能力”和“参考界面或需求中希望增加的能力”。`v4.8.5` 是当前代码冻结版本，`v4.8.3`、`v4.8.2`、`v4.8.1`、`v4.8.0`、`v4.7.1`、`v4.7.0` 及更早版本作为历史兼容基线保留；GitHub 仓库已改为私有，发布附件和证据以当前版本收尾记录为准，参考图是体验研究来源，不替代真实 Release、Actions、Pages 和附件证据。

## 当前发布事实

- 当前维护发布：`v4.8.5`；`v4.8.3`、`v4.8.2`、`v4.8.1`、`v4.8.0`、`v4.7.1`、`v4.7.0`、v4.6.0、v4.5.0 及更早版本作为历史兼容版本保留，旧 Tag/Release 不覆盖。
- `v4.8.4` Tag 保留为未通过 GitHub Actions 门禁的候选，未生成正式 Release 或下载资产；它不被移动，v4.8.5 是本次通过门禁后的正式维护版本。
- v4.8.5 维护内容：单文件离线包内联网页图标；真实 `file://` 运行 232 次主路由渲染 x 4 视口；新增本地 HTTPS/WebDAV 备份端到端传输测试；完整 v4.8 与 AI 套件进入主 `pnpm test`；不新增迁移。
- Release：[`v4.8.5`](https://github.com/7752777/counselor-desk/releases/tag/v4.8.5)；正式附件、Actions 和资产哈希见[v4.8.5 发布收尾记录](./release-v4.8.5.md)。
- 兼容性：v4.8.5 不新增迁移，继续使用工作区协议 `schema_version:8`、业务 `data_schema_version:11` 和同步协议 `sync_protocol_version:1`；v4.8.3/v4.8.2/v4.8.1/v4.8.0 数据可直接读取。

- v4.8.0 历史发布提交：[`63c429d`](https://github.com/7752777/counselor-desk/commit/63c429df5f877ab5a91476f18b8dc468c3edcbc6)。
- v4.8.0 历史 Release：[`v4.8.0`](https://github.com/7752777/counselor-desk/releases/tag/v4.8.0)；仓库为私有，下载需要仓库权限。
- 发布门禁：[Actions #32553621535](https://github.com/7752777/counselor-desk/actions/runs/32553621535)；Tests、Windows x64/ARM64、macOS Universal、Web、直接上传、资产核对和正式 Release 全部成功。
- 本地发布门禁：完整测试、v4.8 定向测试、桌面定向测试、构建、lint、公开面、密钥扫描、Release 契约和差异检查均通过；v4.8.5 资产哈希见[v4.8.5 发布收尾记录](./release-v4.8.5.md)。
- Pages：从允许的 `master` 分支触发 [#32421073455](https://github.com/7752777/counselor-desk/actions/runs/32421073455) 时，GitHub 返回当前计划不支持私有仓库 Pages；前一次 [#32421035179](https://github.com/7752777/counselor-desk/actions/runs/32421035179) 仅因分支环境保护拒绝。仓库保持私有，Pages 不作为当前入口。
- 正式附件和 SHA-256：以 [v4.8.5 发布收尾记录](./release-v4.8.5.md)及 v4.8.5 Release 附带的 Windows/macOS/Web 三份校验清单为准；Windows/macOS 包未签名，macOS 未公证。

## 2026-08-21 v4.7.1 发布核对

- AI 定向安全、v4.7 UI 和优化回归均已通过；本轮新增的联系方式授权、学号快照范围校验、来源快照字段、IPv6 来源边界、直连响应上限、取消竞态、Anthropic 视觉转换、消息结构清洗和建议质量反馈见[AI 全面收口审计](./ai-comprehensive-audit-2026-08-20.md)。
- v4.7.1 已从 `41a2218` 创建 Tag 并完成远端发布；Tag、Release、桌面附件、Pages 限制和最终 SHA-256 均以[本版本发布收尾记录](./release-v4.7.1.md)中的实际证据为准，不能借用 v4.7.0 记录。
- 仓库私有化已由 GitHub 实际状态确认；历史 Release 和 v4.7.1 附件需要仓库权限。私有仓库不能替代终端、备份、附件仓和数据库级加密。

## 当前技术与数据事实

- 工作台是本地优先系统，可作为网页、单文件离线 HTML 和 Electron 桌面端运行。
- 浏览器侧以 IndexedDB 和 schema v8 作为持久化协议兼容边界；v4.8.0 业务数据版本为 v11，物理 IndexedDB 从 v5 增量到 v6，保留 localStorage 兼容与只增不删迁移路径。
- Electron 侧使用 SQLite 仓储、IPC 桥和加密附件目录。
- 学生及大部分业务记录通过学号和稳定 ID 关联，学生时间线已经能够汇总多个业务集合。
- 已有导入预览、字段映射、冲突处理、批量操作、导入回滚、备份、交换包、便携 HTML、附件和版本历史能力。
- v4.5.0 新增 `v4_contacts`、`v4_class_schedules`、`v4_activity_participants`、`v4_league_cases`；学生档案扩展导师、班主任、家长、居住、房东、照片和历史学号字段。学生名单默认合并更新，覆盖模式先建立恢复点并显示删除数量。
- 活动支持学期、学生选择、照片/附件和按学生/学期去重统计；工作留痕支持查课查寝结构化字段和照片/附件；资料与平台支持卡片/列表模式；谈话支持日期和学生筛选；统一导出支持 CSV/XLSX/DOCX/打印 HTML/PDF 工作包。
- 党员流程主记录为 `v4_party_cases`，团员流程按 `2023-08-22` 国家基线保存核心节点、学校附加节点、来源和审计；空日期不自动补今天。
- 附件二进制只存 IndexedDB、Electron 或便携附件仓，写入前做容量检查，失败回滚；重点学生锁明确为防误看，不是数据库加密。
- 已有本地 AI 模型配置、跨模块上下文、字段级脱敏授权、调用审计、统一建议中心、工作留痕草稿、通知改写、来源检索和预警辅助分析入口；模型可配置用途授权、每日成功调用额度、图像能力和可选中转地址，输出均须人工确认。上下文按当前学生、班级、日期区间、当前页面、当前事项和选定来源组合，默认使用稳定 `student_id` 并移除姓名、学号、联系方式等敏感出站字段；明确授权的敏感分类只对当前请求恢复对应字段，内部 `student_id` 始终不出站。建议保存于 `v4_ai_suggestions`，同时保存模型快照、审计编号、来源和风险等级；来源保存于 `v4_ai_sources`，授权保存于 `v4_ai_consents`，可按状态、关键词、用途、风险等级和来源筛选，可查看、采纳、驳回并转任务、谈话或工作留痕。生成、草稿、来源和人工确认之间会保留建议 ID、草稿 ID、生成审计、模型/provider、用途、来源 ID、上下文范围和确认审计；任务、谈话及通用 v4 规范化器会保留这些字段。没有稳定 `student_id` 的建议不能转为谈心谈话；重复确认会被拒绝。当前结果可批量标记已查看但不能批量采纳。失败或取消的请求可无密钥重试，工作台显示当日调用、成功、失败和引用来源统计。外部来源增加已核验、需要重新核验和核验失败状态；核验失败的来源保留历史记录但从新上下文排除，重新核验成功后恢复引用，目录和引用详情显示 URL、标题、抓取时间、最近核验时间和片段。学生档案、谈话详情、任务和业务记录编辑入口会记录当前页面、事项 ID 和稳定 `student_id`，学生摘要与谈话 briefing 可从详情直接生成。内置业务页面均有统一的当前页 AI 动作入口，详情页另提供专属动作；开发服务提供同源 AI 中转，避免浏览器 CORS 拦截；静态部署需要单独运行受控 HTTPS 中转端。证书识别以待确认草稿保存，确认后才以稳定学生 ID、附件引用和人工确认审计写入奖励档案；通知本地预览也保存带原文哈希的本地来源和审计。
- 当前就业资源种子数据约为 80 个官方或可信入口，带来源、分类、地域、适用对象、核验状态与本地收藏筛选基础；就业意向和联系全过程已形成独立记录流程，并以稳定学生 ID 优先、学号兼容的方式关联画像。

2026-08-20 AI 收口补充：直连模型响应执行 4 MiB 默认、8 MiB 硬上限，并在非法 JSON、响应超限或取消竞态时只记录安全失败/取消状态；图片请求必须选择 `supportsVision` 模型，Anthropic 消息按其视觉协议转换；公共 `CWB.ai.run()` 过滤未知消息结构。建议详情新增“有帮助 / 需要修改 / 不适用”反馈，结果只写 `v4_ai_audit`，`CWB.ai.metrics.summary()` 返回 `feedback_total`、`feedback_by_value` 和 `feedback_by_purpose`。这些修改已随 v4.7.1 Release 交付，历史 v4.7.0 不包含。

## v4.6.0 本轮实现事实

- 新增 10 个集合：`v4_dorm_buildings`、`v4_dorm_rooms`、`v4_dorm_batches`、`v4_dorm_assignments`、`v4_dorm_transfers`、`v4_committee_role_catalog`、`v4_committee_evaluations`、`v4_family_contacts`、`v4_worklog_drafts`、`v4_research_projects`。
- v8 到 v9 为只增不删的迁移：补齐集合数组和 v4.6 标记，保留旧记录、稳定 `student_id`、学号快照、附件引用和 v8 恢复能力。
- 实用工具默认不保存输入；排宿先预览、人工确认后更新学生宿舍快照；调宿记录追加历史，不覆盖历史位置，历史记录只能作废不能硬删除。
- 班委角色字典和考核等级保存在工作区；家长联系与个人通讯录严格分离；谈话、家长联系、任务完成、查课查寝、活动和 AI 通知转化只生成待确认工作记录草稿。
- 科研项目阶段保留变更历史，阶段任务按项目/阶段去重；证书材料只保存附件 ID，二进制继续进入附件仓；班级分析只生成派生聚合，不复制事实数据。
- Electron 打包和 Pages 部署清单已补齐 v4.6 运行时；`v4.6.*` 标签已纳入 Release workflow 触发范围，Windows/macOS/Web 产物和 Pages 已由对应 Actions 验证。
- v4.6.0 的实现、风险和验收要求以[本轮审计](./next-optimization-audit-2026-08-19.md)为准；发布提交、Actions、Pages 和 SHA-256 以[发布收尾记录](./release-v4.6.0.md)为唯一事实源。

## 2026-08-19 维护收口

公开 v4.6.0 之后，本工作区修复了业务记录误用自身 ID、非法日期归一化、排宿确认竞态、停用房间调宿、浏览器内嵌集合清单漂移、奖惩手工附件缺口和学生台账模式切换不生效等问题。调宿历史改为作废标记，奖惩附件删除会检查引用并写审计；这些维护修复随 v4.7.0 一并公开，未移动 `v4.6.0` Tag。对应定向测试和限制见[收尾查漏补缺审计](./closeout-audit-2026-08-19.md)。

维护收口还补齐了学生与家校电话默认脱敏、完整号码访问锁和审计、班级分析个人明细保护、记录级 `term` 筛选、活动参与去重、学生时间线补全，以及工作记录草稿来源变化后的哈希刷新闭环。来源已删除时草稿仍不可归档；这些边界已进入 v4.7.0 公开基线。

## 2026-08-19 工作区 UI 收口审计（已并入 v4.7.1）

当前工作区在公开 v4.7.0 之上继续做了一批纯 UI 收口：首页改为“今天先处理最重要的事”工作台，统一 v4.7 页面包裹、标题层级、统计条、筛选条和视觉密度，桌面端补齐更明确的右侧上下文区，学生台账把常用筛选和展示模式前置，窄屏压缩顶部状态区并保留完整产品名。首页又完成重复内容纠偏：旧版 today/KPI 不再输出，趋势、职责、学生结构和热力图放入默认收起的“数据概览”，另加四项紧凑数据维护状态。详细逐项核对见[UI 收口与历史需求查漏补缺审计](./ui-redesign-closeout-audit-2026-08-19.md)。

本节保留 2026-08-19 当时的开发核对语境；相关 UI、AI 来源链和交互收口已随 v4.7.1 提交、Tag、Release 和远端门禁交付。Pages 不能因私有仓库计划限制部署，不影响有权限的 Release 和离线包交付。

## 2026-08-19 AI 来源链补丁审计（已并入 v4.7.1）

- 本地来源目录使用稳定来源 ID，能够回链 `url`、`source_url` 或 `official_url` 等实际来源地址，并保存业务集合、记录 ID、`source_fingerprint` 和 `source_updated_at`。
- 建议状态查询和工作流运行同时检查 `record` 与 `local` 来源；业务资料被修改或删除后，建议显示需要复核并阻止转任务、谈话或工作留痕。
- 统一出站脱敏继续移除稳定 `student_id`、本地记录/附件/审计索引和来源指纹；这些字段只服务于本机追溯，不进入模型上下文。
- 普通生成、通知降级和证书识别草稿保存 canonical `purpose`；`CWB.ai.run()` 同时复核 `context.sources` 和直接 `sourceRows`，并在消费敏感授权前完成来源校验。
- 本补丁后的 `pnpm test:cwb-ai` 已通过；文档修改完成后的 `pnpm lint`、构建、公开面、密钥扫描、发布契约和最终差异检查仍是发布前门禁。
- 该补丁已进入 v4.7.1 Tag、Release 和正式桌面/离线产物；GitHub 私有化和 Release 权限控制不等于终端、备份或数据库级加密。

## 八个模块盘点

| 模块 | 源码中已存在 | 已有基础但需补齐 | 当前明确新增/目标 |
| --- | --- | --- | --- |
| 工作台 | 学生总览、任务、重点学生、心理筛查、危机跟进、统计、工作留痕与 AI 草稿入口；面向领导的可编辑统计视图（仅数值聚合、保存视图与 CSV 导出） | 当前学期统一汇总、风险分层展示、画像的全量字段编排 | 继续按业务口径补齐可汇总字段 |
| 学生信息管理 | 学生台账、导入映射、筛选、分页、批量修改、照片、导师/班主任、家长/居住、画像和时间线 | 学校字段口径、批量覆盖前的业务复核 | 保持稳定 ID、历史学号和附件关联 |
| 培养跟踪 | 党团发展档案、班级/组织角色、成绩、课表、学业预警、帮扶、毕业生基础台账、综测/学期学业/处分/资助档案、就业意向与联系记录 | 学校流程节点和材料完整性口径 | 团员发展采用国家基线加学校附加节点；就业资源约 80 个入口 |
| 奖惩助贷 | 奖励/处分基础记录、资助、考勤、住宿、请假和附件基础能力 | 处分解除状态与文件、贫困认定和临时补助口径、统计维度 | 证书识别草稿、人工确认后写入获奖档案 |
| 工作记录 | 任务、谈话日期/学生筛选、班会/活动、查课查寝、政策、材料、学习和工作日志等入口 | 学校对留痕分类和打印格式的口径 | AI 周报、月报、学期总结和通知转留痕草稿 |
| AI 智能 | 模型连接配置、跨模块上下文、字段级授权、建议中心、来源引用、草稿生成、通知改写/重点识别、预警辅助分析、请求取消/重试、用量统计、统一就地入口和记录级动作 | 用真实业务资料持续验收建议质量；不提供批量采纳和自动事实修改 | 通知主动导入闭环已完成；密钥轮换仍需账户持有人在外部服务完成 |
| 资料与平台 | 政策、材料、文件版本、竞赛、模板、通讯录、班级课表和列表模式 | 学校资源分类、打印口径和来源维护 | 资源入口集中维护，模板和导出说明持续同步 |
| 系统管理 | 院系/专业/班级等组织信息、版本、备份、迁移、诊断和数据安全能力 | 管理入口的集中性和升级提示 | AI 权限、调用额度、日志和模型启停管理 |

## 当前主要入口和存储边界

现有导航包含 `home`、`students`、`talks`、`tasks`、`worklogs`、`stay`、`leave`、`honor`、`pleave`、`attend`、`node`、`orgs`、`party`、`league`、`rewards`、`activities`、`warn`、`help`、`grades`、`grant`、`focus`、`psych`、`graduate`、`policy`、`material`、`comp`、`tpl`、`learning`、`contacts`、`schedules`、`ai`、`audit` 和 `bridge` 等入口。

学生画像的扩展必须沿用现有学生号/稳定 ID 关联。新增记录至少需要明确：所属业务集合、学生引用、发生时间、状态/等级、正文或数值字段、附件引用、创建/更新时间、schema 版本和审计要求。具体字段以对应现有记录定义为准，不在本基线中凭空改名。

## 参考 UI 的统一结论

已检查的参考界面覆盖证书识别、工作总结、党团发展、就业意向与联系、资源收藏、工作台/学生画像、风险建议、通知改写、模型配置权限、处分详情、班会记录和竞赛资源等场景。

后续实现必须逐场景复核以下内容：

- 页面层级：工作台概览、列表/筛选、详情/画像、编辑/确认弹窗。
- 操作边界：新增、编辑、批量处理、导入、附件、导出、AI 生成、人工确认和撤销。
- 状态呈现：空数据、加载、识别中、草稿、已确认、失败、无权限和敏感字段脱敏。
- 视觉一致性：沿用现有浅色工作台配色、紧凑信息布局、表格/标签/按钮体系；参考图用于补齐信息结构，不替换现有成熟交互。

## 本轮实现状态

- P0 移动端已完成：首次欢迎设置和每日问候不遮挡导航；抽屉层级、遮罩、Escape、返回焦点和 `aria-expanded` 状态已处理；折叠分组搜索、全部展开和恢复导航已接入；业务档案、就业意向和就业联系移动卡片保留业务操作与记录级 AI 动作，长摘要不会挤压操作区。
- P0/P1 AI 基础设施已完成：统一上下文构建和预览、稳定 `student_id` 关联、默认出站脱敏、字段级一次性授权、三类 AI 集合及备份/迁移/桌面/手机边界已接入。
- v4.5.0 老师反馈批次已完成：学生档案扩展和合并导入保护、党员日期、团员发展、通讯录、课表、活动参与统计、查课查寝、资料列表、统一打印导出和 AI 通知草稿闭环均已接入；新增集合已进入统一迁移边界。
- 移动端反馈已实测：欢迎设置不遮挡导航，抽屉和常用栏可用，分组搜索可找折叠模块，`390×844` 和 `360×800` 无横向溢出，顶部栏滚动后保持吸附。
- P1 建议中心闭环已完成：建议状态机、状态/关键词/用途/风险/来源筛选、查看/采纳/驳回、当前结果选择、批量标记已查看、失败无密钥重试、当日用量统计、转任务/谈话/工作留痕、来源引用和审计已接入。
- P1 核心工作流已完成统一入口映射：所有内置业务页面均显示当前页 AI 动作，学生、任务、谈话、成绩、帮扶、预警、心理、资助、就业、资料、班团、组织、业务档案等记录均补充记录级动作，并绑定当前事项、集合和稳定 `student_id`；三类跨页面动作已加入回归测试。建议质量仍需用真实业务资料持续验收。心理、预警、纪律、资助和奖惩仍严格限制为建议或人工核验，不自动写入事实。
- 来源生命周期和移动建议中心本批已补齐：来源目录显示核验状态、引用回链和重新核验入口；窄屏建议中心支持独立筛选、来源状态提示和可换行的查看/转化操作；移动抽屉补充 `aria-modal` 状态。
- AI 请求交互已补齐：请求进行时只保留取消入口；敏感授权弹窗取消不会发送请求并会清理待确认的 `running` 状态；刷新后残留的未完成状态也可由“取消请求”清理。该路径已加入 UI 回归断言并完成内置浏览器真实验收。
- 当前开发目录的 relay、上下文、建议中心、移动端、数据安全边界和老师反馈批次已在 `v4.5.0` 工作区完成；公开提交、Release、Pages、附件大小和 SHA-256 证据已回填到[发布收尾记录](./release-v4.5.0.md)。开发目录中的 `output/辅导员工作台.html` 仍是本地构建，不替代 Release 附件。
- v4.4.6 的公开附件、三份 SHA-256 清单、Actions 和 Pages 证据已回填到[发布收尾记录](./release-v4.4.6.md)；产品代码发布基线固定在 Tag `v4.4.6`，后续文档维护提交不移动该 Tag。
- AI 请求和公开来源抓取已有客户端超时与取消清理；异常 HTTPS 来源即使携带 `verified` 快照，也会在进入上下文前重新通过公开地址格式校验，失败时隔离为“需要重新核验”。
- 模型未配置/API key 缺失会留下安全失败审计但不会消耗敏感授权；视觉模型返回数组化文本内容时会统一提取文本，避免证书识别和图片草稿出现 `[object Object]`。

## 计划完成矩阵

| 批次 | 当前状态 | 事实边界 |
| --- | --- | --- |
| P0 移动端导航与 AI 基础边界 | 本地已完成 | 欢迎弹窗不再阻塞导航；抽屉、焦点、搜索、稳定 `student_id`、脱敏、一次性敏感授权和三类集合边界已接入并通过定向/完整测试。 |
| P1 统一建议中心 | 本地已完成 | 建议状态流转、来源引用、审计、人工确认、失败重试、查看筛选和任务/谈话/留痕转化已完成；高风险建议和失效来源仍受人工门禁。 |
| P1 核心工作流融合 | 基础闭环已完成，质量验收持续 | 业务页面已有统一当前页入口和记录级动作；各页面的专属指令、真实业务资料效果和学校业务口径仍需持续人工验收。 |
| P1 受控知识来源 | 本地已完成 | 本地资料优先；公开网页只能由用户明确提供 HTTPS 地址并受来源、大小、超时、重定向和私网限制；不做后台全网爬取。 |
| v4.5.0 老师反馈收口 | 正式发布已完成 | 15 条反馈已逐条接入并完成定向、完整回归、桌面包烟测、Pages 和公开附件验收；真数据库加密、家校沟通独立台账、字典中心和真实大规模设备压测仍不属于本批自动完成范围。 |
| v4.6.0 日常协同增强 | 已正式发布并部署 | 实用工具、住宿专项、班委考核、家长联系、工作记录草稿、科研课题、证书附件、班级分析、首页摘要和 Electron 卡片资源边界已接入；Tag、Release、Pages、跨平台正式包和 SHA-256 已回填。公开版本后的维护修复记录在收尾审计中，尚未形成 v4.6.1。 |

## 后续实施顺序

1. 使用脱敏真实业务资料继续验收 v4.7 的宿舍批次、家长联系、科研阶段、证书附件和班级聚合；统一入口、记录级动作和当前学生/事项/日期范围上下文已经完成。
2. 继续做来源维护和建议中心批量查看的真实资料验收；系统已完成来源核验失败隔离、重新核验和移动端基础转化闭环。
3. 撤销并轮换此前已经暴露过的外部 API 密钥；新密钥不得写入源码、文档、备份、日志或 Git。此项需要外部服务账户操作，本地代码无法代替。
4. 使用真实业务资料持续验收各页面专属指令与建议质量；继续维护来源核验、建议人工审核和学校业务口径，不把质量验收误写成自动事实更新。

v4.7.0 的完整 `pnpm test`、构建、lint、公开面、密钥扫描、发布契约和差异检查均已通过；远程发布门禁 [Actions #32211212549](https://github.com/7752777/counselor-desk/actions/runs/32211212549) 以及 Pages [Actions #32212075408](https://github.com/7752777/counselor-desk/actions/runs/32212075408) 也已通过。响应式浏览器在 `390×844`、`360×800` 下的移动验收和桌面五种视口验收已完成；密钥撤销/轮换仍属于账户持有人必须完成的外部操作。

## 移动端与本地地址

移动端的 `127.0.0.1` 指向手机自身，不能访问电脑上的 `4173` 开发服务。仓库私有后原 Pages 当前返回 404；手机应使用有权限访问的受控离线包、电脑局域网地址，或直接打开 `output/辅导员工作台.html`。`http://127.0.0.1:4173/` 只适合在运行服务的电脑本机验收。

### 外链与窄屏卡片收口

- 政策智库和就业资源的外链统一经过 `CWB.utils.safeExternalUrl()`：只接受绝对 `http`/`https` 地址，拒绝危险协议、相对地址、控制字符、过长地址和用户名/密码；原始字段仍保留用于人工修正和导出，未通过校验的记录只显示“网址待核验”，不会生成可点击链接。
- 就业资源卡片的操作区允许换行。动态插入的记录级 AI 动作在 `360×800` 和 `390×844` 下不会把卡片撑出视口，也不会造成页面横向滚动。
- 新增 `tests/external-url-safety.js`，覆盖 URL 协议/凭据/长度边界、政策和就业页面渲染，以及窄屏动作区换行约束；内置浏览器已复核现有政策和就业资源链接均为合法 `http/https`。

## 数据与 AI 安全边界

- AI 默认只接收脱敏后的业务上下文；姓名、学号、联系方式、心理、纪律、资助等敏感内容需要明确授权。
- AI 结果统一作为草稿、建议或待确认结果，不直接改变预警、处分、资助、心理结论或学生档案。
- 证书识别必须经过学生匹配和人工确认后才写入获奖档案。
- 周报、月报和学期总结只能基于已有记录生成，不补造事实。
- API 密钥不进入导出包或普通备份；调用、授权、失败和额度变化需要可审计。
- AI 中转只转发必要的模型请求，不接受客户端任意请求头；服务端要求显式来源、可选访问令牌和允许模型域名清单，拒绝 DNS 解析到内网或特殊地址、非 HTTPS 目标、重定向和超大响应，密钥不写入日志、审计或备份。

## v4.7.0 参考工作台批次（历史已正式发布）

参考产品九页功能拆分、差距矩阵和视觉规则见[参考工作台功能与界面审计](./reference-product-feature-and-ui-audit-2026-08-19.md)。本批以 `v4.6.0` 为公开基线，新增 v10 查课、点名、查寝、量化考评、工具箱、就业防骗、竞赛和学业分析集合，并把桌面端高频页面整理为统计条、筛选区、主操作、结果区和可折叠上下文区；现已成为 `v4.7.0` 公开基线。

`v4.7.0` 已完成核心运行时、集合清单、v10 增量迁移、网页/离线/Electron/Pages 入口、定向与完整测试、五种视口验收、平台附件、Tag、Release 和 Pages 部署；真实证据以[发布收尾记录](./release-v4.7.0.md)为准。

本批新增集合：`v4_class_checks`、`v4_roll_call_sessions`、`v4_dorm_inspections`、`v4_dorm_exceptions`、`v4_assessment_rules`、`v4_assessment_entries`、`v4_tool_links`、`v4_employment_safety`、`v4_competition_resources`、`v4_competition_entries`。所有学生关联继续使用 `student_id`，二进制继续使用附件仓，AI 结果只进入建议或草稿。

## 2026-08-19 AI 加固候选

当前工作区在历史 `v4.7.0` 之上新增了 AI 来源指纹和上下文范围门禁：业务来源发生修改或删除时，建议不能直接转化；公共运行入口会拒绝跨学生记录和未核验来源。通知识别在没有可用模型时直接本地降级，但仍留下草稿、来源哈希和 `fallback_reason` 审计；建议中心支持批量查看和批量驳回，批量采纳仍需人工确认；浏览器公开来源大小按 UTF-8 字节计算。详细证据见[AI 收口加固记录](./ai-hardening-followup-2026-08-19.md)。

这些修改只存在于当前 `codex/ai-upgrade` 工作区，尚未提交、推送、创建新 Tag/Release、生成新桌面/离线产物或部署 Pages。仓库已经私有，历史 `v4.7.0` 仍保留；Pages 入口因私有化当前不可用。

## 2026-08-20 AI 收口补充

当前工作区已完成一轮 AI 反向查漏补缺：每日额度只统计真实 `generate` 完成审计；身份授权按当前学生/班级/实际来源收窄；relay 来源 URL 清理令牌类查询参数；建议转化必须同时具备人工确认时间和确认方式；停用 provider 和取消连接测试具有明确审计语义；错误提示覆盖额度、授权、附件、视觉能力、非法 JSON 和响应过大等常见处理路径。

最终定向验证：`pnpm test:cwb-ai`、`pnpm test:v47`、`pnpm test:optimization`、`pnpm lint`、`pnpm build:release`、`pnpm test:release`、`pnpm check:secrets`、`pnpm check:public`、`git diff --check` 均通过；`test:optimization` 的 17 个串行单项也逐项通过。以上修改仍属于 `codex/ai-upgrade` 未发布候选，不代表历史 `v4.7.0` 已包含这些修复。GitHub 仓库 `7752777/counselor-desk` 当前为私有，历史 Tag/Release 保留；仓库私有不等同于本地数据库、附件、备份和终端已加密。

当前仍未完成的高风险/外部事项：数据库级加密和密钥生命周期、历史 API key 撤销轮换、真实模型质量评分、真实设备长期压力恢复测试、来源语义检索和内置浏览器人工截图触控验收。不得在发布说明中把这些事项写成已完成。

## 2026-08-20 交互连续性候选收口

当前候选又完成了业务按钮闭环核对：工具结果和科研阶段任务具备稳定来源键和重复点击保护；公共任务记录保留来源集合、来源 ID 和阶段；家校联系、科研项目和调宿附件保存失败会回滚未被引用的附件；删除业务记录会让来源工作记录草稿进入不可归档状态，并只清理确认后的孤儿附件。表单校验失败保留输入，允许直接修正重试。

右侧上下文面板已补齐焦点进入、`Escape`/关闭按钮退出、焦点返回和刷新状态恢复；Electron 学生台账已用 3 条合成学生验证卡片模式真实渲染、照片/宿舍/导师/班主任/家长关系显示和刷新恢复。对应实现和证据见[交互连续性与按钮闭环审计](./interaction-continuity-audit-2026-08-20.md)。

以上修改仍属于 `codex/ai-upgrade` 未发布候选。它们没有进入历史 `v4.7.0` Tag、Release、附件、Pages 或哈希证据；文档记录不能替代最终发布门禁。

## 2026-08-21 保存链路维护补充

在上述按钮闭环基础上，当前候选又修复了来源草稿的跨集合持久化边界：基础集合保存触发 `v4_worklog_drafts` 来源复核时，会把 `custom` 变化一起提交到 v8 工作区；删除来源前已经标记的“来源已删除”状态也会通过待同步版本保留到保存成功。工作记录来源指纹新增备注、查课/查寝事实、地点、处理措施、优先级和截止日期等字段；基础保存批量刷新的 `updated_at` 不再单独造成草稿过期，修改真实业务事实才要求重新核对。

保存 Promise 现在由统一队列追踪，失败返回可识别错误并保留确认框/表单重试入口；失败保存不会清除待同步标记，成功保存后才清除。该修复和 v8 工作区状态断言已通过 `pnpm test:interaction`、`pnpm test:v46`、`pnpm test:v47`、`pnpm test:cwb-ai` 与 `pnpm lint`。本节仍属于 `codex/ai-upgrade` 未发布候选；本次代码变化后的完整 `pnpm test`、构建、发布门禁和哈希尚未重新执行。

文档完成后的受影响验证已通过：`pnpm test:desktop`、`pnpm test:release`、`pnpm check:public`、`pnpm check:secrets` 和 `git diff --check` 均退出码 0。完整 `pnpm test` 首次暴露导入测试静态契约与当前事件分发器变量名不一致，修正 `tests/import-loop.js` 后重新执行退出码 0；`pnpm build:release`、lint、公开面和发布契约也在修复后通过。本轮没有生成正式发布附件，因此没有哈希检查。桌面测试留下的 `.tmp-electron-cards-root-sKjswo` 已加入忽略规则，只是合成测试缓存，不属于候选发布内容。

本轮全量验证补充：当前候选的完整 `pnpm test` 在修正导入器测试契约后退出码 0，覆盖 10,000 条性能样本、备份/附件/迁移/交换包和跨端回归。`v40-route-alias` 及部分 JSDOM 夹具仍会输出 `c.local` 外部脚本和 `window.scrollTo` 能力噪声，但测试断言退出码为 0；这不能写成真实浏览器人工截图验收通过。

## 2026-08-20 AI 记录级入口复核

在上一轮 AI 安全收口之后，继续反向比对页面按钮、集合映射和上下文日期字段，补齐了三处容易被误判为“已完成”的入口：班级课表周视图现在绑定 `v4_class_schedules`，住宿当前安排绑定 `v4_dorm_assignments`，随机点名已保存结果绑定 `v4_roll_call_sessions`。随机点名的上下文日期字段同步修正为规范化记录的 `date`。

本次修改已经通过 `node tests/ai-cross-module-audit.js`、`node tests/ai-record-actions.js` 和两个新增/受影响 UI 文件的 `node --check`。当前候选尚未正式发布，未创建新发布物或部署 Pages；完整门禁已在最终代码状态重新执行，不能沿用修改前的绿色记录代替本次验证。逐项完成/部分完成/外部依赖见[AI 实现逐项对账](./ai-implementation-reconciliation-2026-08-20.md)。

## 2026-08-20 最后一次用途收口与验证账本

当前候选又补齐了 AI 用途的可见性和失败边界：主工作台可以直接选择 canonical `warning_assist` 和 `certificate_recognition`；建议中心会将旧别名归并到 canonical 用途；未登记用途返回 `AI_PURPOSE_INVALID`，不再静默按工作总结执行。该改动仍只存在于 `codex/ai-upgrade`，不属于历史 `v4.7.0` Release。

全量 `pnpm test` 在修正 `tests/import-loop.js` 的事件分发器静态契约后实际退出码为 0；AI、v4.7、优化、lint、构建、公开面、密钥扫描和发布契约也在最终代码状态通过。首次运行的失败记录保留为审计轨迹，不再作为当前最终状态。

## 2026-08-20 AI 输出安全与运行健康

当前候选又完成了一次 AI 出站和运行状态收口：模型返回文本会在页面返回、草稿保存和建议保存前执行二次脱敏；`student_id`、业务记录 ID、附件/审计/请求索引始终不会因为模型回显而进入结果，未授权姓名、学号、手机号和邮箱也不会落库。明确授权当前范围的身份/联系方式后，只放行允许字段，内部关联 ID 仍然清理。生成审计新增 `output_redacted`，记录是否发生了这次本地清理。

AI 工作台新增 `CWB.ai.health()` 运行健康报告，检查核心治理脚本、用途合同、六类 AI 集合、页面级入口、启用模型、来源待核验和待审核队列；入口函数不存在时会明确报告缺口。`tests/ai-output-health.js` 已覆盖未授权回显、授权范围、草稿/建议结果、审计字段和健康卡片渲染。

本次完整 AI 定向门禁 `pnpm test:cwb-ai` 实际通过，包含 15 个用例；完整 `pnpm test` 也在修正导入测试契约后退出码为 0。当前工作区仍是 `codex/ai-upgrade` 未正式发布候选，GitHub `7752777/counselor-desk` 已确认是私有仓库；本次推送只更新维护分支，不创建新 Tag/Release、不部署 Pages。

## 2026-08-21 启动时序收口

真实 `4173` 页面验收发现内联 `boot()` 早于 `defer` 核心脚本，因此 AI 工作台首屏可能短暂显示脚本/集合缺口。当前候选已在 DOM 就绪后等待 `CWB`、AI 核心、AI 工作流和 v4.6/v4.7 UI 就绪，再绑定治理对象并重绘一次；未配置模型仍只显示配置提醒。`pnpm test:v47`、`pnpm lint` 和真实移动/桌面页面验收通过，修复仍未进入历史 Tag/Release。

## 2026-08-21 AI 模型可用性收口

AI 生成按钮现在使用 `CWBAI.providerReadiness()` 检查当前连接、启用状态、模型名、canonical 用途授权、视觉能力、当前会话凭据和当日额度；不可用时直接禁用并显示下一步配置动作。运行入口仍会再次检查，不能靠脚本绕过 UI。`secret_set` 只代表曾配置过密钥，普通浏览器还必须在当前会话取到密钥；Electron 最终仍以安全存储读取结果为准。

每日额度修正为按 `provider_id + purpose` 隔离，只统计成功生成，不统计连接测试、失败、取消和本地通知预览。`v4_ai_audit.provider_id` 是可选非敏感字段，旧审计按连接器和模型名兼容读取；readiness 是运行时派生状态，不新增集合、不改变 v8/v10 数据版本。详细规则与验收记录见[AI 模型可用性审计](./ai-provider-readiness-audit-2026-08-21.md)。`pnpm test`、`pnpm build:release`、`pnpm check:public`、`pnpm check:secrets`、`pnpm test:release` 和 `git diff --check` 已通过；当前候选已构建但尚未创建新 Tag/Release 或部署 Pages。

## 2026-08-21 v4.8.0 客户反馈执行计划

客户反馈已形成新的长期执行基线，完整方案见[客户反馈全流程执行方案](./customer-feedback-execution-plan-2026-08-21.md)。本节只记录当前事实和版本边界，不代表功能已经落地。

方案初始核对时确认的缺口包括：局域网数据中枢、Electron 解密错误诊断与恢复口令、自定义安装目录、按计划自动备份、`student_id` 优先导入、学生批量维护、动态分班、一生一表反向汇总、心理语音草稿和本地内容推送。本节保留该初始快照；当前执行状态以紧随其后的候选实现对账为准。

本轮锁定的架构决策是：Windows/macOS Electron 主机同期支持；局域网通过一次性配对码加入；离线变更进入队列；不同字段自动合并、同字段进入冲突收件箱；云端只保存端到端加密备份；部门首期只做本地政策/资料推送，不共享学生明细；数据版本采用 `schema_version:8`、`data_schema_version:11` 和 `sync_protocol_version:1`。

本节是 v4.8.0 发布前的规划基线历史。不得把计划文档或工作区候选代码直接当作设备/服务商验收证据；v4.8.0 正式 Tag、Release、安装包和离线 Web 资产现已由发布工作流交付，真实设备证书信任、跨端恢复和长期压力仍按发布后限制维护。

## 2026-08-21 v4.8.0 候选实现对账（阶段性快照）

当前工作区已经把一部分高风险基础设施从设计推进到候选实现；本段保留为当时的阶段性快照，后续页面和跨端契约收口见文末最新对账。Electron 版本化密钥与恢复包、仓储健康检查、错误码、可选择安装目录、数据目录迁移核心、局域网 HTTPS 主机、一次性配对、设备撤销、幂等/字段冲突、v10 到 v11 迁移、学生稳定 ID 优先导入、加密附件正式/临时分块、备份运行记录、远程加密包适配器核心，以及桌面运行期间每 60 秒的备份到期检查均已进入候选。相关定向验证见[客户反馈逐项审计](./customer-feedback-audit-2026-08-21.md)。

这些代码尚未进入正式 Release。此阶段快照中的局域网客户端连接、证书指纹校验、拉取、推送、离线队列、冲突收件箱页面（含主机值/本机值/手动编辑三种处理入口）、主机真实 SQLite 回写、恢复/迁移诊断页面、加密附件上传/下载、备份运行记录、远程加密包适配器核心、动态分班、学生字段中心、工作分类、内容推送/内容包、联合走访、心理语音页面和受支持 DOCX 生成/反向汇总页面均已保留。后续又补齐了远程备份配置/测试连接/上传/下载预览恢复/删除页面和学生批量编辑/归档/删除/撤销 UI；当前仍未完成手机真实 HTTPS 信任/自动发现、真实 WebDAV 互操作、Windows/macOS 实机迁移与回滚、多设备长期压力和真实设备逐集合恢复验收。任意 Word 自动识别和直接回写学生事实仍不支持。任何文档、页面或发布说明都必须继续区分“候选已实现”“定向验证通过”“实机验收通过”和“正式上线”。

本批同步主机新增可注入审计回调：配对码创建、配对请求/确认/拒绝、设备撤销、同步操作接收、冲突创建/解决和附件完成只记录操作类型、集合、记录 ID、设备、字段名、版本或哈希等最小元数据，不记录补丁值、令牌或附件明文。管理页面的删除字段、分班历史、内容发布/已读/撤回和冲突处理同样写入本地访问审计；保存失败会恢复内存快照并保留重试入口。

本批新增的桌面备份调度器每分钟只调用到期检查，不会每分钟生成文件；成功备份会写入最多 50 条 `v4_backup_runs` 最小运行记录，记录本身不会增加业务变更计数；浏览器关闭期间无法后台写文件，页面已显示这一限制。正式发布前仍需在 Windows/macOS 安装包中验证数据目录迁移、恢复口令、损坏数据库回滚和备份失败重试。

## 2026-08-21 v4.8 页面与 AI 执行回填

当前候选的心理页面已支持“录音 -> 一次授权 -> 转写整理 -> 人工编辑 -> 确认写入”链路；取消、失败、关闭和完成都会释放会话音频，群体主题只输出达到最小人数阈值的脱敏聚合。通知/资料推送已支持工作区、学院、年级、班级范围，以及导入失败回滚的内容包合并；工作分类中心保护系统分类，学校自定义分类可维护。

动态分班页面的联合走访弹窗按日期和班级读取历史班级、有效住宿和当日课表，课表缺失显示“未记录”，不把缺失当作空闲。模板中心已经可以校验受支持 DOCX、预览缺失字段、生成单人/批量 Word，并从内容控件预览反向结构化 CSV；任意 Word 文本识别、反向结果自动写回学生事实仍明确禁止。

本次新增的页面闭环定向证据包括 `node tests/v48-management-ui.js`、`node tests/ai-voice-contract.js`、`node tests/ai-voice-relay.js`、`node scripts/build-release.js output/v4-preview.html` 和 `node scripts/check-inline-js.js`。这批证据只证明当前工作区候选，不改变 `v4.7.1` 正式发布事实。

## 2026-08-21 v4.7.1 当前权威状态

以上 2026-08-20 至 2026-08-21 小节记录的是版本化之前的候选过程，保留其“当时未发布”表述作为审计历史。当前源码已版本化为 `4.7.1`，核心实现、AI readiness、保存队列、来源草稿、附件回滚、桌面卡片模式和真实浏览器视口检查均属于 v4.7.1 交付范围。版本号修改后的验证、提交、推送、Tag、Actions、Release、Pages 和最终产物哈希只以[v4.7.1 发布收尾记录](./release-v4.7.1.md)中的实际回填为准；不得继续把历史候选段落当作当前发布状态。

## 2026-08-21 交互连续性维护补充

当前维护工作区又完成一批不改变数据协议的高频操作收口：

- 首页摘要入口现在携带下一步筛选条件，逾期/在办任务、待回访谈话、重点/未解除风险学生、待确认草稿和今日查课都能直接进入处理队列；任务增加截止状态筛选，谈话增加回访状态筛选。
- 全局搜索覆盖 `v4_contacts`、`v4_family_contacts`、`v4_research_projects`、`v4_class_schedules`、`v4_worklog_drafts`、`v4_tool_links` 和 `v4_ai_suggestions`，命中后把查询词写入对应页面状态。搜索字段包含通讯录电话/其他联系方式和工作草稿来源摘要，仍只在本地执行。
- 工作记录草稿队列增加实时搜索、数量反馈、清除筛选、来源存在性提示和来源回链；来源已删除时不能继续用旧草稿确认正式留痕。来源回链只打开原记录，不绕过人工确认。
- 右侧“当前上下文”在选中学生后提供四个直接动作：新建任务、记一次谈话、记工作留痕和识别通知。前三个动作使用当前学生的稳定 `student_id` 预填表单；任务、工作留痕会同时带出姓名和学号，谈话保存时仍按稳定 ID/学号兼容恢复关联。未选中学生时按钮保持普通空白创建流程，不会误关联上一名学生。
- 当前学生区提供“清除当前学生”入口；清除会同步移除 v47/app 的学生选择、AI 目标事项和当前页学生选择器，后续新建任务、谈话或留痕不会继续带入旧学生。重新选择学生后，原有稳定 ID 预填链路仍可恢复。
- 四个上下文动作采用稳定的两列布局，窄屏继续由现有抽屉/单列规则接管；本轮补充回归覆盖了上下文清除、动作携带稳定 ID、任务/谈话/工作留痕预填和来源回链。该维护代码尚未重新生成 Release 产物。
- UX 测试夹具使用 `file:` 页面加载本地运行时，避免测试环境的外部 origin 错误掩盖真实 v4.6/v4.7 脚本；这只修正测试环境，不放宽生产页面的来源或安全校验。

本轮 `pnpm test:optimization`、`pnpm test:ux`、`pnpm test:interaction`、`pnpm test:v46`、`node tests/ai-cross-module-audit.js` 和 `pnpm lint` 均通过。上述代码变化尚未形成新的 Tag、Release、桌面/离线正式产物或 Pages 部署；`v4.7.1` Release 的正式范围和 SHA-256 仍以[发布收尾记录](./release-v4.7.1.md)为准。长期维护审计见[交互连续性与按钮闭环审计](./interaction-continuity-audit-2026-08-20.md)。

## 2026-08-21 工作留痕确认与驳回回滚维护

继续从“按钮点击后数据是否真的完成”检查待确认工作记录：

- 页面待确认工作记录使用持久化等待版确认/驳回动作，等待正式工作留痕和草稿状态的实际保存结果；保存失败会恢复确认前的草稿和工作留痕内存状态，调用方收到可重试错误，不会先提示成功。为保持旧集成兼容，`CWB.worklogDrafts.confirm()` / `dismiss()` 仍保留同步返回语义；需要等待落盘的调用使用新增 `confirmAsync()` / `dismissAsync()`，页面按钮已经使用后者。运行时没有统一等待辅助函数时仍兼容直接等待 Promise 的环境。
- 同一草稿重复确认沿用 `source_draft_id` 幂等更新，不会重复创建正式工作留痕；驳回和确认仍需要人工动作，来源已变化/已删除的草稿不能绕过来源复核。
- 业务档案和就业联系页面文案已改为明确说明：业务记录优先使用稳定 `student_id`，学号仅作当前快照和旧数据兼容，避免界面提示与实际关联规则不一致。

本次新增交互回归覆盖保存失败、状态回滚、恢复保存、重复确认不重复写入和驳回失败保留草稿。随后完整 `pnpm test` 已在这批代码状态下通过，包含 v8/v10、附件、Electron、10,000 条性能样本、交换包和浏览器存储回归；JSDOM 的 `c.local` 外部脚本加载提示仍是既有测试环境噪声。该维护代码仍属于当前工作区候选，不移动 `v4.7.1` Tag/Release，不生成新的正式产物；最终发布前需在代码稳定后重新执行正式发布门禁。

## 2026-08-21 AI 保存事务维护（当前工作区候选）

在上述按钮闭环基础上，AI 证书和工作总结又完成了一次跨集合保存收口：

- 证书识别草稿创建纳入 `v4_ai_drafts + v4_ai_audit` 快照；草稿或审计回链保存失败时恢复原数组，不留下仅有附件而没有可追踪草稿的状态。
- 证书人工确认只保存 `rewards + custom`，不再调用无分支参数的全量 `save()`；奖励记录、草稿确认状态和确认审计作为一个可重试动作处理。失败恢复后仍可用同一个 `ai_draft_id` 重试，成功不会重复创建奖励。
- AI 工作总结确认纳入 `worklogs + v4_worklog_drafts + v4_ai_suggestions + v4_ai_audit` 快照。建议采纳和转化不再各自提前落盘，统一等待 `worklogs` 与 `custom` 两个保存分支；保存失败时正式留痕、确认审计和建议状态同时恢复。
- `persistAiMutation()` 对单个保存结果和多集合结果统一提取 `{ ok:false, error }`，保留底层错误码供界面重试和审计定位。按钮可以显示具体失败原因，但不会把内部密钥、请求正文或敏感字段带入提示。

本批 `pnpm test:interaction`、`pnpm test:cwb-ai`、`pnpm test:optimization` 和 `pnpm test:v47` 均通过，`pnpm lint` 也通过。代码和新增测试仍是当前维护工作区候选，尚未进入 `v4.7.1` 正式产物；不得把本节定向验证当作新的发布证据。文档修改完成后还需执行一次 `git diff --check`，最终发布前按发布收尾记录重新执行完整测试、构建、公开面、密钥扫描、发布契约和产物核对。

## 2026-08-21 v4.8.0 发布前存储对账（历史）

本节是发布前阶段的收口事实，保留用于解释 v4.8.0 发布前过程；当前正式维护版本为 `v4.8.1`，分支 `codex/ai-upgrade` 的正式资产以私有 Release 为准，Pages 不作为入口。

- 学生台账已落地 `student_id` 优先导入和批量维护 UI：当前页/筛选结果选择、导师/班主任/家长/居住/紧急联系人/学业/关注字段批量编辑、归档、删除、差异确认和一次撤销。空白默认不覆盖，姓名不自动匹配。
- 远程备份已从适配器核心推进到 v4.8.0 页面闭环：HTTPS/WebDAV 配置、测试连接、上传当前加密 `.cwbk`、下载并预览恢复、删除远程备份；配置不保存 Bearer/Basic 凭据，上传成功后才更新本地状态。
- 新增跨端恢复契约测试，覆盖浏览器工作区、单文件离线 HTML、加密备份、手机工作包、交换包、自定义 v11 集合和附件 ID/内容回读。该测试证明脱敏夹具的协议/持久化契约，不等于真实 Windows/macOS/手机设备验收。
- 当前发布后的真实剩余风险是：Windows/macOS 安装与数据目录迁移、损坏 SQLite/密钥回滚、手机 HTTPS 信任/二维码或自动发现、真实断网重连和多设备长期压力、真实 WebDAV 服务商互操作，以及真实设备逐集合恢复抽查。

本节对应的代码文件、测试文件、迁移集合和发布门槛分别以[客户反馈逐项审计](./customer-feedback-audit-2026-08-21.md)、[局域网同步与存储记录](./lan-sync-and-storage-v4.8.0.md)、[v4.8.0 发布收尾记录](./release-v4.8.0.md)和[客户反馈全流程执行方案](./customer-feedback-execution-plan-2026-08-21.md)为准。候选状态不能替代最终完整门禁和发布证据。

## 2026-08-21 高风险边界最新状态

空数据目录参数已在桌面路径解析前拒绝，迁移激活已使用可测试事务辅助模块：候选仓健康检查失败会恢复原目录，原仓恢复失败则清空活动仓引用并返回 `DATA_MIGRATION_ROLLBACK_FAILED`。远程备份端点已限制为 HTTPS 基址下的同源相对路径，拒绝绝对 URL、查询参数和目录穿越；局部错误会保留端点错误码，不再统一包装成网络失败。局域网配对弹窗可以复制地址、配对 ID、配对码和证书指纹，并明确未实现二维码/自动发现。

本次变更属于发布前 v4.8.0 候选历史；定向测试证据见[客户反馈审计](./customer-feedback-audit-2026-08-21.md)和[v4.8.0 发布收尾记录](./release-v4.8.0.md)。Windows/macOS 实机、真实手机、真实 WebDAV 和长时间同步不因发布自动变成已验收，继续按发布后维护清单执行。

## 2026-08-21 内容权限与导出回链状态（发布前历史）

v4.8.0 的内容推送不再依赖页面默认身份或空范围：核心服务支持 `workspace_admin`、`content_editor`、`teacher`、`viewer` 四类本地策略标签，页面列表、已读和普通角色导出均传入当前工作区的学院/年级/班级上下文。只有工作区管理员拥有全量导出权限；内容编辑仍可发布、导入并撤回自己发布的内容，但导出按当前上下文可见范围处理。已读记录必须使用当前操作者自己的 `reader_id`，避免代写其他人的已读状态；角色变更写入最小审计。

工作留痕导出现在经过实际构建产物运行时验证：导出行包含来源集合、来源记录 ID、来源标题、来源状态和来源更新时间，包顶层包含 `provenance` 来源映射。该映射只用于人工回查，来源变化/删除仍必须重新核对。新增服务级和构建后运行时证据见 `tests/v48-content-export.js` 与 `tests/v48-worklog-export-runtime.js`。

本节记录发布前实现过程；内容角色不是账号认证。真实 Windows/macOS、手机 HTTPS、WebDAV 互操作和多设备长期压力仍是发布后限制，不能由本节替代。

## 2026-08-22 发布前候选状态与后续路线（历史）

当时发布前正式维护版本仍为 `v4.7.1`。工作区 `codex/ai-upgrade` 的 `v4.8.0` 候选已完成最后一次自动化门禁：`pnpm test`、`pnpm lint`、`pnpm build:release`、`pnpm check:public`、`pnpm check:secrets`、`pnpm test:release` 和 `git diff --check` 均为退出码 `0`。本轮还修复了 5,000 条工作区替换的重复深拷贝性能问题，`node tests/v8-workspace.js` 通过；随后 v4.8.0 和 v4.8.1 均完成正式发布。

真实浏览器已验收五个目标视口和三栏/抽屉/底栏/卡片模式；Windows x64 修复后构建、打包烟测和 NSIS 安装器烟测通过。ARM64 新包因 Electron 下载外部网络超时未完成，macOS 构建必须在 macOS 实机或 CI 执行。真实手机 HTTPS/二维码、长时间局域网同步、WebDAV 服务商、跨端附件恢复、签名/公证仍是发布阻断项。

功能、美观、稳定性、AI 和互通的完整清单与下一步分阶段执行表见[当前全量状态与后续提升路线](./comprehensive-status-and-roadmap-2026-08-22.md)；当时的发布状态以[当前收口总账](./closeout-status-2026-08-22.md)为准。该段是发布前历史，不代表当前 v4.8.0 尚未发布。

## 2026-08-21 v4.8.0 候选提交前门禁

本批实际通过：`pnpm test:v48`、`pnpm lint`、`pnpm check:public`、`pnpm check:secrets` 和 `git diff --check`。其中 `test:v48` 覆盖内容推送跨范围权限、同步规模、远程加密备份互操作契约及构建后工作留痕来源回链；密钥扫描检查 313 个仓库文件。该结果属于工作区候选验证，不等同于 Windows/macOS、真实手机、WebDAV、长期同步或附件恢复实机通过；v4.8.0 仍未创建 Tag、Release、正式安装包或 Pages 发布物。

## 2026-08-21 发布前候选最新维护状态（历史）

发布前正式版本仍为 `v4.7.1`；`v4.8.0` 当时是 `codex/ai-upgrade` 工作区候选，未创建 Tag、Release、正式安装包或 Pages 上线物。上一批 P0 改动之后，本轮新增同步事务回滚和客户端错误边界：状态文件落盘失败会恢复主机内存快照，Electron 同步仓储会恢复已有记录或删除未提交的新记录；客户端地址校验错误不再被吞成网络故障。

本轮新测试入口和证据见 [v4.8.0 发布收尾记录](./release-v4.8.0.md) 与 [客户反馈逐项审计](./customer-feedback-audit-2026-08-21.md)。真实 Windows/macOS 密钥/SQLite 损坏恢复、跨盘迁移、手机 HTTPS 信任、长时间断网、WebDAV 服务商互操作、附件大数据量恢复和最终全量门禁仍是发布阻断项。

## 2026-08-21 v4.8.0 同步交互最新回填

本候选又完成一批“实际按钮能不能走通”的收口：主机确认配对后，客户端可以通过请求 ID和设备 ID一次性领取令牌；设备管理页支持暂停、恢复和撤销；客户端连接成功后支持立即同步和 60 秒自动检查；网络失败时保留离线队列、游标、表单和错误码；健康状态、设备状态、`ui_state`和普通备份均不包含令牌或 `token_hash`。对应代码为 `src/core/cwb-v48.js`、`src/core/cwb-v48-ui.js`、`desktop/lan-sync.cjs`、`desktop/main.cjs`、`desktop/preload.cjs`和 `index.html`。

本回填已通过局部同步、桌面 IPC、构建后局域网 UI、管理 UI、inline lint 和安全边界测试。二维码/自动发现、真实手机证书信任、跨设备长时间断网、Windows/macOS 实机恢复当时仍未通过；这些内容随后以发布后限制写入 v4.8.0 Release，不得把这段历史状态当作当前版本判断。

## 2026-08-21 学生照片保存优化回填

照片上传与学生档案编辑现在遵循“附件先写入、学生引用一次提交、必要时才写清理标记”的路径。成功上传不再无条件进行第二次学生持久化；保存失败仍恢复内存对象并删除未被引用的新附件。`tests/photo-storage.js` 已加入成功上传单次学生写入断言，`node tests/photo-storage.js` 和 `node tests/interaction-continuity.js` 在本轮代码状态下通过。

该修改属于 `v4.8.0` 未发布候选，版本号仍为 `4.7.1`；之前的完整门禁不覆盖本轮代码，最终发布前必须重新运行完整门禁。JSDOM 持久化测试较慢是因为它执行真实 v8 状态校验，不代表已完成真实 Windows/macOS、手机或大附件压力验收。

## 2026-08-21 大名单 IndexedDB 契约与全量回归状态

浏览器端学生大批量原子替换使用 `records_students` 中的 `__cwb_bulk_students__` 内部载荷；仓储 `list/get/count` 会在刷新和重新打开后解包为逻辑学生记录。最终全量回归中的 10,000 条导入样本耗时 `4788.5ms`，处理 10,000 条，最大进度事件间隔 `9.4ms`，最大事件循环间隔 `176ms`；这些是本地浏览器夹具结果，不等同于所有低配设备的性能承诺。

本轮完整测试曾因旧断言期待 IndexedDB 原始行数为 1,200 而失败，已修正为验证原子载荷完整和仓储 reopen 后逻辑记录完整；受影响测试及修正后的完整 `pnpm test` 均已退出码 `0`。这只证明当时候选源码回归通过，不替代 Windows/macOS、手机和真实跨端恢复验收；该历史快照之后已创建 v4.8.0 和 v4.8.1 Tag/Release。

## 2026-08-21 当前候选最新对账：二维码配对已进入工作区实现

本候选已经补齐局域网配对二维码。桌面端“生成一次性配对码”现在优先调用 `lanSyncPairingQr()`，弹窗可显示二维码，并保留地址、一次性配对 ID、配对码和证书指纹的明文复制回退。核心 `CWBV48.createPairingQrPayload()` / `parsePairingQrPayload()` 对载荷字段做白名单校验：只允许版本、HTTPS 地址、工作区 ID、配对 ID、8 位配对码、指纹和过期时间；令牌、密钥、学生数据和附件数据不会进入二维码。

二维码不是自动发现，也不是证书自动信任。手机扫码后仍需人工核对 HTTPS 地址和证书指纹，由主机确认配对；二维码过期后必须重新生成。没有扫码能力或二维码生成失败时，四项明文复制入口仍可完成旧的手动配对流程。

本节对应的定向证据为 `node tests/v48-pairing-qr.js`、构建后 `node tests/v48-lan-ui.js` 和相关 `node --check`。在该历史快照时，真实手机扫码、手机自签名证书信任、局域网自动发现和多设备长期断网重连仍未完成；随后 v4.8.0 已正式发布，v4.8.1 是当前正式维护版本。

## 2026-08-22 当前事实总账（权威）

本轮最新按钮级收口：删除重复证书识别实现，保留唯一的附件逐次授权、延迟持久化和人工确认入口；修复建议中心批量驳回未真实提交的问题，并为批量成功和整批失败回滚增加回归。随后修复浏览器物理 IndexedDB 版本并重新完成最终自动化门禁；这些修改已随 v4.8.0 正式 Release 交付，真实设备和服务商抽查仍按发布后限制执行。

本文件的详细现状以[当前收口总账](./closeout-status-2026-08-22.md)为准。正式维护版本为 `v4.8.5`；`codex/ai-upgrade` 上的发布后文档回填不移动既有 Tag/Release，正式安装包和离线包仍以私有 Release 分发物为准。

本轮实际收口了 AI 建议、通知、敏感授权和局域网离线队列的持久化等待/失败回滚，修复了旧浏览器工作区缺少新 object store 的问题，并补充了无 IndexedDB 时不宣称数据库级加密、同设备多工作区隔离和跨端恢复等待的回归测试。修复后完整测试、最终构建、公开面、密钥扫描、Release 契约和差异检查均已通过；Windows x64/ARM64、macOS Universal、离线 Web 资产和发布校验清单已由正式工作流交付。手机/WebDAV 实机互操作、长期同步、数据库级加密和签名/公证仍是已知限制。

## 2026-08-24 高频记录链路收口补充

任务、谈话和回访现在以“来源记录 + 关联待办 + 工作留痕草稿”为一个长期使用单元：三者必须等待真实持久化后才提示成功，失败会恢复原状态并保留表单供重试。删除回访任务会清除谈话反向引用并标记待重新安排；删除谈话会保留回访待办并解除失效来源，避免后续工作随来源删除而丢失。本批通过 `node tests/interaction-continuity.js` 和 `pnpm lint`，但不改变“v4.9.0 尚未完成全量商业门禁、真实设备、生产 AI 和支付验收”的版本判断。

## 2026-08-23 v4.9.0 商业候选最新对账

当前维护发布仍为 `v4.8.5`；`codex/ai-upgrade` 工作区版本为 `4.9.0`，但尚未创建 `v4.9.0` Tag、Release、正式安装包或客户下载入口。v4.8.5 的数据格式、历史 Tag/Release 和私有仓库状态不变。

本轮商业控制面已经落到候选源码：Ed25519 许可证和四档权益、Electron `safeStorage` 独立存储、AI 统一守卫、设备绑定/撤销、签名更新清单、更新安装状态与恢复点回滚、Fastify/PostgreSQL/KMS 适配器、订单幂等、支付事件幂等、邮件 outbox、管理员审计，以及 `/customer` 客户订单页和受保护许可证下载。客户订单取件令牌只以服务端哈希保存，服务端有效期为 7 天；旧订单缺少过期字段时按创建时间兼容计算，过期返回 `ORDER_ACCESS_EXPIRED`，不通过可枚举订单号继续读取。

客户页已增加旧浏览器随机 ID 回退、产品目录 HTML 转义、取件有效期展示、无缓存和防点击劫持响应头。授权服务支持精确 CORS 来源清单；生产环境禁止 `*`、路径、查询参数、凭据和非本地 HTTP 来源，来源不在清单时返回 `LICENSE_CORS_ORIGIN_NOT_ALLOWED`。限流器同时限制来源记录数量，避免长期运行时因大量 IP 导致内存无界增长。

2026-08-23 已在新服务器 `82.156.201.93` 部署隔离的授权服务前瞻候选：专用 `counselor-license` 系统用户、PostgreSQL `counselor_license` 数据库、Node.js 20、systemd 服务和逐行库存许可证导出工具；服务仅监听 `127.0.0.1:8787`，公网仅经 Nginx 进入。已实际完成四档产品查询、临时库存生成、激活、撤销和测试文件清理。域名 `windsky.store` 已注册、实名通过并由 DNSPod 接管，`license.windsky.store` 已添加 A 记录，Let's Encrypt 证书已签发并部署，云安全组和 UFW 已放行 443，公网 HTTPS 健康检查、产品目录和客户页验收通过。部署记录见[授权服务前瞻候选服务器记录](./staging-server-deployment-v4.9.0.md)。这不等于正式商业上线；KMS/HSM、链动小铺官方回调、邮件、CDN、代码签名、公证和真实设备证据仍缺失。

本轮最后一次局部证据包括：`customer-page-interaction`、客户交付、授权服务 HTTP/CORS、生产服务适配器和相关 Node 语法检查均通过。最终全量测试、构建、lint、公开面、密钥扫描和 Release 契约仍须在候选代码稳定后统一重跑；真实 PostgreSQL、KMS/HSM、支付、邮件、CDN、Windows Authenticode、macOS Developer ID/公证、跨重启升级和目标设备恢复仍没有本地证据，不能写成已完成。

许可证、设备令牌、订单取件令牌、管理员 Key、Relay assertion 和模型 API Key 都不属于工作区业务事实；它们不进入 SQLite/IndexedDB 业务集合、备份、手机交换包、学生导出或普通日志。更新状态仅保存于 Electron `vault/update-install-state.json`，更新失败保留失败目录和恢复点，不生成新密钥覆盖旧数据。业务数据恢复成功不等于商业授权恢复成功，换机必须重新激活。

## 2026-08-24 长期使用与 AI 语音链路审计

本轮继续以“长期补录、修改、撤销和跨模块映射”为验收单位。学生关联索引现在同时覆盖稳定 `student_id`、当前学号和唯一历史学号；同一历史学号出现歧义时不自动选择。谈话记录和学生时间线不再按姓名自动兜底，姓名只保留为展示快照。学生档案保存前会显示变更字段、关联模块和记录数量，明确成绩、谈话、处分、资助、心理等过程事实不会被自动改写；保存后提示仍只有旧学号快照的记录，供后续人工补齐稳定 ID。

AI 语音链路已区分文本模型与转写模型：连接配置新增 `transcriptionModel` 和 `supportsAudioTranscription`，录音界面发送前检查 OpenAI-compatible 协议、语音用途授权、独立转写模型、密钥和浏览器 MIME。服务商返回 404、415、400 时分别提示音频端点不存在、格式不接受或请求被服务商拒绝；录音仍只在当前会话暂存，失败/取消/关闭不保存原音频。详细规则和已知限制见[长期使用数据联动与 AI 语音链路审计](./long-term-data-and-ai-audit-2026-08-24.md)。

本批定向证据：`node tests/ai-voice-contract.js`、构建后 `node tests/v48-student-import-ui.js`、三个 Node 语法检查和 `git diff --check` 均通过。真实中转站语音请求、Electron/手机麦克风、旧数据全量人工核对和完整商业发布门禁仍未完成；不能把本批自动化结果写成真实设备或生产 AI 已验收。

本批后续维护已补齐 AI 能力矩阵去重，以及活动、工作留痕、通讯录、课表、就业和业务文件编辑保存的失败回滚。活动和工作留痕的附件先写入、主记录后提交；主记录失败会清理本次新增附件，清理失败会进入诊断字段。追加定向验证为 `pnpm lint`、`node tests/v47-polish-ui.js`、`node tests/interaction-continuity.js`、`node tests/ai-voice-contract.js`，均通过。`v4.9.0` 仍未重新执行全量商业发布门禁，也没有新增真实 AI 服务商、Electron、手机或生产支付/授权证据。

2026-08-24 追加长期班级历史纠错：日期班级解析现在稳定处理同日多次变更，并在查询早于第一条变更时使用最早原班快照；动态分班页增加链路断裂检查。手工补录统一生成带 `student_id`、快照、来源和操作人的历史事实；存在更晚记录时不自动改写当前档案，删除历史时在事务内重新计算当前班级。该能力属于 v4.9.0 工作区候选，已通过 `node tests/v48-core.js`，尚未作为正式版本发布。

随后执行的 v4.8 受影响批次综合验证 `pnpm test:v48` 全部通过，覆盖同步、备份、恢复、学生导入、管理 UI 和桌面备份调度。该结果仍不覆盖真实 Electron/手机麦克风、目标第三方语音服务、生产支付授权、代码签名/公证，也不改变当前正式维护版本 v4.8.5 与 v4.9.0 工作区候选的版本边界。

## 2026-08-24 长期关联维护追加收口

当前候选新增学生台账“维护旧关联”入口，扫描谈话、成绩、住宿、奖惩、资助、就业、班委、家校联系、量化积分和竞赛等高频记录的稳定 `student_id`。有效 ID 不进入队列；缺少/失效 ID、歧义学号和仅姓名快照会明确展示原因。当前学号或唯一历史学号只能给出候选，姓名绝不自动合并。老师选择学生并确认后，系统只补写稳定关系及原记录缺少的快照，不改写已有业务事实；处理审计保存在 `v4_student_identity_conflicts`，与业务记录一同失败回滚。

本次局部证据：`node tests/v48-core.js`、构建后 `node tests/v48-student-import-ui.js`、`node tests/import-safety.js`、`node tests/interaction-continuity.js`、`pnpm lint` 和 `git diff --check` 均通过。先前完整 `pnpm test` 在本批开始时发现并修正了首次引导的过期文案断言；修复后未重复启动完整门禁，因为本批仍需在真实 Electron/手机与真实 AI 服务商验证完成后再统一执行。正式维护版本仍为 `v4.8.5`，`v4.9.0` 没有新增正式 Tag、Release 或生产 AI/支付/签名证据。

## 2026-08-24 长期维护按钮级收口

本次继续检查用户在开学建档、期末补录、改班级/宿舍、补照片、导入任职和维护就业资源时的长期行为：

- 学生只导入“学号、姓名、分数”时，稳定学号匹配会更新原学生的综合成绩；未出现在文件中的培养层次、电话等字段保留，课程化成绩仍进入成绩管理。
- 照片人工确认、班团任职导入/删除、就业资源编辑/收藏/删除/CSV 导入在兼容保存路径中等待真实写入；失败会恢复列表、队列或附件关联，成功提示不会早于持久化完成。
- 活动、工作留痕及其参与人、草稿和附件继续按事务边界保存或删除；学生主档案修改不会静默改写历史成绩、谈话、住宿、奖惩、资助和心理事实。
- 本批新增静态 UI 契约，防止上述按钮退回直接 `save()` 的旧写法。

本批实际通过：`pnpm lint`、`pnpm check:docs`、`pnpm check:secrets`、`node tests/v47-polish-ui.js`、`node tests/v48-student-import-ui.js`、`node tests/interaction-continuity.js`、`node tests/photo-storage.js`、`node scripts/build-release.js output/v4-preview.html` 和 `git diff --check`。这些是定向证据，不替代真实中转站语音、Electron/手机麦克风和 v4.9.0 商业发布门禁。

## 2026-08-24 长期使用追加验证

点名保存、活动/工作留痕删除、文件删除和就业资源收藏继续按“真实保存完成后才提示；失败恢复原状态、附件和反向引用”的规则收口。AI 设置页增加“检查语音”：发送本机生成的静音 WAV 验证服务商音频端点，不发送真实录音。该检查不能代替真实服务商转写质量和 Electron/手机麦克风验收。

追加通过：`node tests/ai-voice-contract.js`、`node tests/v47-polish-ui.js`、`node --check src/core/cwb-ai.js`、`pnpm lint`、`git diff --check`。当前正式维护版本仍为 `v4.8.5`；`v4.9.0` 工作区候选仍缺真实 AI 服务商、真实设备、支付、签名/公证和完整商业门禁证据。

## 2026-08-24 长期关联与删除事务追加

当前 v4.9.0 工作区候选进一步覆盖多人关系和删除失败恢复：学生详情的跨模块影响摘要识别 `student_id` 及活动参与、查寝、点名、一生一表等多学生 ID 数组；学生批量编辑、归档、撤销保留现有对象引用并在失败时原地恢复。查课、查寝、量化积分和竞赛报名的附件删除与业务记录提交现在等待同一事务完成，业务写入失败会恢复附件和来源状态。工具链接、就业风险和竞赛资源删除等待真实 `custom` 持久化后才显示成功。

本批已通过 `node --check src/core/cwb-v47-ui.js`、`node --check src/core/cwb-ai.js`、`node tests/transactional-array-mutation.js`、`node tests/student-relationship-impact.js` 和 `git diff --check`。正式维护版本仍为 v4.8.5；真实 Electron/macOS、手机、目标 AI 服务商和 v4.9.0 商业发布门禁仍未完成。

## 2026-08-24 长期数据更新与语音状态补充

当前候选新增学生资料的长期变更映射。单人编辑、增量导入和批量编辑的班级调整都会创建 `v4_student_class_history`，包含稳定 `student_id`、原/新班级、生效日期、来源和操作人；按日期走访/课表分析读取这条历史，而既有谈话、成绩、处分、资助、心理和住宿轨迹保持原始事实快照。相同学生、同日期、相同原/新班级不会重复记录；批量撤销和保存失败会同时回滚学生与班级历史。

学生保存前的影响确认扩展至家长、紧急联系人、联系方式、地址、居住、预警、自定义字段与照片。自定义字段采用稳定内容比较，导入列或对象键的排列变化不造成误报。首次引导同时明确示例不覆盖真实数据、AI 可以稍后配置、期末三列成绩表可以增量补录，且所有引导跳转必须在状态真实写入后执行。

语音功能真实状态不变：实现已通过 `MediaRecorder → /audio/transcriptions → 独立转写模型 → 人工确认` 链路和 relay 合同测试，不需要额外“音频解析库”；最终识别能力取决于目标服务商的 STT 端点、模型、额度和浏览器 MIME。静音端点探针和本机格式探针都不发送谈话内容，也不代表真实普通话、长录音或手机端已验收。

本批通过 `node tests/v48-core.js`、`node tests/student-relationship-impact.js`、`node tests/import-safety.js`、`node tests/v47-polish-ui.js`、`node tests/v48-student-import-ui.js`、`node tests/ai-voice-contract.js`、`node tests/ai-voice-relay.js`、`pnpm lint`、`pnpm check:docs` 和 `git diff --check`。构建后 `v48-student-import-ui` 在当前环境约 30 秒后正常退出并通过，运行时间较长但不是失败证据。正式基线仍为 `v4.8.5`，`v4.9.0` 未发布。

本轮又收口了长期使用中的学生导入边界：学生集合旧同步导入 API 现在明确拒绝写入并要求 `commitPreviewAsync`，统一使用稳定 `student_id`、当前/历史学号和人工核对规则；导入摘要增加待核对与班级历史数量，撤销快照同时包含学生和 `v4_student_class_history`。导览中的“备份与更新”直接进入备份页。定向验证通过 `node tests/import-safety.js`、`node tests/v47-polish-ui.js`、`node tests/v48-core.js` 和构建后 `node tests/v48-student-import-ui.js`。这些结果不改变当前正式维护版本 `v4.8.5` 与工作区候选 `v4.9.0` 的边界，也不替代真实设备和商业发布验证。

## 2026-08-24 AI 工作区可用性收口

AI 页面的上下文选择、模型连接、建议用途和操作区已由通用工具条改为专用响应式工作区，修复了生成 HTML 中的 `card-bd` 标签错误。1440×920、1024×768、390×844 的本地预览均确认能力矩阵只显示一次、模型/用途字段保持可读、移动端无横向溢出。模型状态继续严格区分“未激活”“未配置”和“可调用”，不因为文本连接可用就宣称语音转写可用。

本次仅改动 AI 页面结构、样式与页面契约，运行 `node tests/v47-polish-ui.js`、`node tests/ai-voice-contract.js`、`pnpm lint`、`pnpm check:docs` 和 `git diff --check`。未重复运行完整测试或发布构建：本批没有改变数据模型、仓储、同步或构建配置；完整商业门禁仍留待功能候选稳定后统一执行。

## 2026-08-24 长期体验追加

当前 v4.9.0 工作区候选已补充示例数据长期体验保护：`settings.demo_reference_date` 作为示例日期锚点，重新打开时仅平移仍带 `_demo: true` 的演示记录及其嵌套阶段日期，真实记录不会被改写。首次示例导航默认优先展示工作台、学生台账、谈心谈话和工作留痕，低频模块仍可搜索、展开或钉住。

AI 录音窗口新增浏览器录音能力判断和可执行下一步提示；服务商 HTTP 错误按凭据、接口、请求大小、音频容器和限流分别翻译。当前仍不能把静音探针或自动化契约测试当作目标中转站真实普通话质量、Electron/手机麦克风或正式商业发布证据。追加定向验证：`node tests/demo-data-coverage.js`、`node tests/ai-voice-contract.js`、`node tests/v47-polish-ui.js`、`node tests/mobile-navigation.js`、`node tests/import-safety.js`、`node --check src/core/cwb-ai.js`。

## 2026-08-24 v4.9.0 候选最终收口

本轮按“先把长期使用和更新做稳，再继续扩展 AI 语音”的优先级收口。AI 语音识别暂停新增开发；现有文本/图片/语音契约和安全边界保留，真实第三方转写服务、麦克风和识别质量仍未验收。

当前工作区已经具备：学生稳定 ID 增量补录和人工核对、学生关联维护、长时间使用中的真实保存等待与失败回滚、进行中事项快照联动、Electron 自动更新调度器、更新清单/哈希/平台签名检查、安装前恢复点、失败回滚、商业中心和授权服务候选契约。自动更新对普通用户的实际语义是“自动检查 + 用户确认下载/安装”，不是后台静默升级；浏览器/离线 HTML 仍需手动获取新文件。

本候选截至 2026-08-24 的门禁记录：`pnpm test`、`pnpm lint`、`pnpm build:release`、`pnpm check:public`、`pnpm check:secrets`、`pnpm check:docs`、`pnpm test:release`、`pnpm test:commercial` 和 `git diff --check` 均通过。该结果只证明源码与自动化检查通过；真实微信支付、支付 webhook、生产 PostgreSQL/KMS、邮件、CDN、Windows/macOS 签名/公证、跨重启升级和客户设备恢复仍没有证据。

因此正式维护版本仍为 `v4.8.5`，`v4.9.0` 仍是 `codex/ai-upgrade` 候选分支。后续只有在外部商业服务和目标设备验收完成后，才创建 `v4.9.0` Tag、私有 Release 和客户安装包；本次提交不移动、不覆盖任何历史 Tag、Release 或旧数据。

# 更新日志 · CHANGELOG

## v4.9.3 前瞻版联网与交付收口 · 2026-08-28

- 当前 v4.9.3 工作区候选状态：源码、文档和本地产物正在进行最终公网发布与跨平台验收；正式平台签名、公证和真实商业稳定版发布仍未完成。
- 将当前商业候选版本提升到 `4.9.3`，统一生成唯一离线网页版、Windows x64/ARM64、macOS Universal DMG/ZIP、完整产品手册和购买激活速查。
- 保持未激活样例只读、四档真实数据/AI/更新权益隔离、AI 覆盖激活和友情托管 AI 资格边界；10/40 元档不能获取软件内版本更新，20/60 元档可以获取。
- 将桌面端更新清单、下载包、清单签名和 SHA-256 校验统一到 `license.windsky.store` 的 `v4.9.3` preview 通道；客户端更新不会替换工作区、SQLite 或附件仓。
- 延续前瞻版边界：桌面包未完成 Windows Authenticode、macOS Developer ID 签名和公证，不作为正式稳定商业版本宣传；真实激活码、API Key、服务器凭据和签名私钥不进入 Git、安装包、文档或聊天。

## v4.9.2 前瞻版 r16 联网诊断收口 · 2026-08-28

- 将共享联网诊断核心加入 Electron 正式打包白名单，避免桌面安装包缺少 `src/core/cwb-network-diagnostics.js` 导致源码与产物行为不一致。
- 将请求 ID、阶段、状态码、耗时、请求/响应字节数和脱敏错误码贯穿网页、Electron、局域网主机、授权服务、AI Relay 和 Nginx；日志不记录激活码、API Key、请求正文、学生资料或附件。
- 交付包新增《联网诊断与部署说明》，完整产品手册补充客户端、网关、服务端和上游的逐层排障顺序；r16 产物必须从本轮 Windows/macOS/Web/PDF 同一代码状态重新组装。
- 继续保留前瞻版边界：版本号仍为 `4.9.2`，同版本修订包不能通过软件内更新替换旧客户端；Windows Authenticode、macOS Developer ID、公证、支付自动发码和真实设备全链路验收仍不冒充已完成。

## v4.9.2 前瞻版 Windows 公网重发修复（历史记录） · 2026-08-28

- 本节记录的是上一轮 Windows 公网重发，线上曾使用与当时产物匹配的 preview 文件；它不代表当前本机 r16-final 已经部署。
- r16-final 已在本机重新生成 Windows x64/ARM64、macOS Universal、唯一网页端和三份 PDF；当前公网同名文件仍需取得部署权限后替换，并重新签署 preview 清单。
- 由于所有修订仍使用 `4.9.2` 版本号，r14/r15 与 r16-final 不能通过软件内更新互相替换；公网更新前必须直接分发本机 r16-final 完整包。Windows Authenticode、macOS Developer ID 和公证仍未完成。

## v4.9.2 前瞻版桌面授权网络修复 · 2026-08-27

- 定位并修复桌面端在部分用户网络环境下把授权服务请求误判为 `LICENSE_SERVICE_NETWORK_FAILED` 的问题：商业授权请求现在由 Electron 主进程的 Chromium 网络栈发送，使用系统代理和证书信任配置，不再依赖打包 Node `fetch` 的网络路径。
- Windows x64/ARM64 已用当前源码重新打包，并通过打包后 Electron 健康请求、伪造兑换码服务端校验和启动持久化烟测；伪造码收到服务端明确的 `REDEMPTION_CODE_INVALID`，证明请求已到达授权服务。
- 既有 `r14` 包生成于本修复之前，包内仍是旧网络实现，不能继续作为本次问题的修复包分发。macOS Universal 需要在 macOS CI 或 macOS 实机重新生成后，才能替换现有 Mac 交付物。
- 增加 Electron 桌面网络实现的源码契约，防止后续构建遗漏该修复。真实激活码、API Key 和签名私钥仍不进入仓库、安装包或文档。

## v4.9.2 前瞻版 r13 · 2026-08-27

- 修正商业构建注入的许可证公钥算法标识错误。旧交付包可能把 Ed25519 公钥误写为无效值，导致服务端已正常签发的许可证在客户端验签失败；r13 的网页、Windows 包和已复核的 macOS Universal 包统一使用有效 Ed25519 公钥。
- 网页和桌面构建现在会在生成商业包前解析并验证每个许可证公钥，拒绝 Ed448、损坏的 SPKI/JWK 和其他非 Ed25519 值；对应产物检查也会再次验证包内公钥。
- 使用五类前瞻兑换码完成公网验收：四档权益、永久更新档位隔离、AI 覆盖激活和友情 AI 前置条件均符合预期。明文兑换码仍只保存在维护者受控桌面文件中。

## v4.9.2 前瞻版修复交付（2026-08-27）

- `r10` 直接交付修订包：重新生成 Windows x64/ARM64 NSIS 安装器和 macOS Universal DMG/ZIP，补齐桌面快捷方式与授权错误提示修复。由于仍使用 `4.9.2` 版本号，r9 已安装客户端不会把 r10 视为新版本；r10 仅通过新的完整交付包分发，下一次需要软件内推送时必须先提升版本号并重新签发清单。
- 补充安装后桌面入口：Windows NSIS 保持创建“学工智伴”快捷方式，应用首次启动会在快捷方式因系统策略未生成时补建且不覆盖用户已有图标；macOS 应用复制到“应用程序”后首次启动会创建桌面别名，DMG 临时挂载运行不会留下失效别名。
- 修正 Electron 授权错误映射：安全 IPC 未携带自定义错误字段时，前端会从受控错误消息提取明确的兑换或友情 AI 错误码，不再把“兑换码无效”等服务已响应的情况误报为“请联网重试”。
- 修复未激活工作区仍可通过普通业务入口新增、编辑、删除、导入或同步真实数据的问题。现在未激活时仅可浏览、筛选和查看内置样例，所有真实写入以及样例改写都在界面、工作区仓储和桌面端 SQLite/附件仓三层被拒绝，并显示可理解的激活提示。
- 明确四档权益：10 元普通版和 40 元 AI 增强版不含软件更新；20 元普通永久更新版和 60 元永久 AI 增强版才可从软件内获取后续版本。AI 商品码可以覆盖基础版，友情 AI 码只在已有 AI 权益后启用托管服务，不改变更新权限。
- 收口桌面端与唯一离线网页版的职责：两端各自保存授权状态，同一商品码需在各端单独输入；桌面端负责 SQLite、附件仓、备份恢复和局域网主机，网页端用于查询、临时录入和回办公室后的同步，不建立长期个人文件仓。
- 以 `4.9.2` 作为修复版版本号发布，保证已安装 `4.9.1` 前瞻版且拥有永久更新权益的设备能识别这次修复；历史 v4.9.1 交付物和数据格式保持兼容。

当前 v4.9.2 工作区候选状态：前瞻更新清单、Windows/macOS 包、唯一离线网页端、产品手册和下载中心已完成本轮构建与公网校验；平台代码签名、公证、支付自动化和真实设备跨重启验收仍未完成。

## v4.9.1 前瞻版收口（2026-08-26）

### r8/r9 授权边界与更新权限修复

- 修复 r7 前瞻交付包未激活仍可使用普通业务的缺陷：商业网页和 Electron 包现在只允许样例数据体验；真实数据、文件上传、导入、备份恢复和更新检查均在底层再次校验授权。
- 服务端兑换活动改为部署哈希白名单优先，当前只保留四个商品码和一枚友情托管 AI 码；数据库残留的旧活动不能绕过白名单继续兑换。
- 已通过公网临时许可证验收四档更新权限：10/40 元档返回 `403 LICENSE_UPDATE_NOT_ENTITLED`，20/60 元档返回带 Ed25519 签名的 v4.9.2 preview 清单；测试许可证已立即撤销。
- `r9` 重新交付包以新的商业网页、Windows x64/ARM64 和 macOS Universal 产物为唯一来源；未签名、未公证和支付自动化限制保持不变。

- 修正授权服务的更新清单接口：现在必须先通过已激活设备认证，且只有 20 元普通永久更新版、60 元永久 AI 增强版可以读取更新；10 元普通版、40 元 AI 增强版明确拒绝更新请求，避免四档商品权益串档。

当前 v4.9.1 工作区候选状态：前瞻更新清单、Windows/macOS 包、网页包、产品手册和下载中心已完成本轮构建与公网校验；平台代码签名、公证、支付自动化和真实设备跨重启验收仍未完成。

- 收口宿舍查寝、量化考评、竞赛资源和学生报名与分工的列表换行：正文与状态/操作区分离，窄屏时改为上下布局。
- 简化局域网同步入口为“启动主机、完成配对、开始同步”三步，并把高级连接参数留在高级区域；补齐文件与模板入口的真实文件上传权限说明。
- 统一三端工作方式说明：办公室优先使用桌面端作为数据主机，下班后网页端继续工作，手机端用于移动记录；网页/手机首次配对后，回到办公室同一局域网且页面打开或回到前台时自动 push/pull，页面关闭期间不伪造后台同步。
- 交付入口统一为一个离线网页版：电脑浏览器和手机浏览器共用同一个 HTML 文件，桌面端与网页端使用同一套四档激活和友情 AI 规则；删除“体验版/授权体验版”双入口的交付约定。
- 未激活工作区只允许样例浏览和样例快照操作；真实数据新增/编辑/删除、导入、换机包导入、备份恢复和文件上传继续经过业务层授权守卫。
- 新增独立 AI 对话入口：默认不附带学生数据，必须预览范围后发送；友情 AI 激活后隐藏普通模型配置并使用托管服务入口。
- 构建版本切换为 `4.9.1`，产品手册和前瞻交付脚本切换到 v4.9.1 文件名；Windows/macOS 包仍需明确标注未签名、未公证。
- 本轮公网复核：授权服务健康接口、产品目录、`/downloads/v4.9.1/` 和主要 Windows/macOS 包 URL 均返回 `200`；更新清单接口对匿名请求返回 `401`，对已激活设备按更新权益鉴权。清单包含平台架构、SHA-256、最低版本和 Ed25519 签名。preview 包明确标记为 `unsigned-preview-v1`，因此可以验证软件内更新链路，但仍不是完成平台签名和公证的正式商业包。
- 交付复核发现 Windows x64 包曾携带与其他平台不同的许可证公钥；本轮已用统一 `staging-primary` 公钥重新编译 Windows x64/ARM64，重新签名 preview 清单并替换服务器下载包，避免跨安装包出现签名校验不一致。
- macOS Universal 前瞻包已通过临时无历史公开构建镜像生成并完成双架构校验；构建完成后镜像已改回私有。更新清单、preview 下载目录和 v4.9.1 完整交付包已同步到服务器，清单哈希与实际文件一致；更新签名脚本同时支持异步 KMS signer。

## [Unreleased] · PDF 需求与前瞻版授权收口（2026-08-25）

- 本轮收口授权体验：正式签名许可证支持离线校验并记录激活模式，购买入口在支付未就绪时隐藏，AI 许可证可覆盖基础许可证；前瞻授权弹窗和客户页统一改为购买/资格核验后添加微信人工入群，不再展示群二维码。v4.9.1 最终交付包需重新生成并包含 macOS Universal DMG/ZIP；真实兑换码、托管 AI Key 和私钥仍不进入源码、文档或包内。
- 修复前瞻兑换码在桌面端和单文件 HTML 中被 `file://` CORS 拦截的问题：Electron 改由主进程受控 HTTPS 请求授权服务；授权服务仅对激活、兑换和友情 AI 三条公开路由允许 `Origin: null`，并保留限流与其他接口的来源拒绝；同时修复窄屏首页引导操作区文字挤压。
- 完成 `1.pdf` 18 页参考功能的逐项审计文档，补齐 AI 配置模式、用途授权、调用日志、失败重试、导航分组/自定义分类和任务时间轴/甘特图的用户与开发说明。
- 修复新版 AI 工作台因上下文栏标记变化而不显示失败重试状态的问题；失败请求现在能保留可重试入口，且 UI 状态不保存 Prompt、API Key 或完整响应。
- 修正 PDF 需求 UI 测试对任务起止日期字段的断言方式，增加 PDF 定向 UI 回归。
- 明确五类前瞻兑换码的实现：四档商品码分别对应 10/20/40/60 元产品，另有一枚友情 AI 托管码；跨工作区可兑换、同工作区复用独立许可证，更新权益由许可证字段隔离并纳入 `pnpm test:commercial`。
- 保持安全边界：不把用户模型 API Key、授权私钥或通用兑换码明文写入客户端、源码、日志、备份、文档或 Git。
- 查漏补缺：为缺少工作区 ID 的旧数据和示例工作区生成并持久化独立随机身份，商业授权不再回退到共享的 `local-workspace`；兑换服务增加活动产品归属校验，兑换码生成脚本被导入时不再打印明文。
- 新增第三类“友情 AI”兑换码闭环：已激活 AI 许可证的前瞻体验用户可单独兑换托管服务资格；资格按工作区幂等保存，服务端只发 15 分钟 Relay assertion，维护者 API Key 不进入客户端，并默认按许可证每日 30 次额度限制。增加托管资格表、路由、客户端弹窗感谢说明和 `managed-relay-redemption` 回归测试。
- 修复 Electron 许可证安全存储遗漏友情 AI 托管资格的问题：`managed_relay` 只保留资格编号、状态和时间等最小字段，重启后仍可恢复托管 AI 选择；新增安全状态编解码测试，原始 API Key、业务数据和完整 Relay 内容仍不会进入该文件。
- 新增 v4.9.0 受控下载中心：提供一个前瞻版离线网页、Windows x64/arm64 安装包、完整前瞻版 ZIP 和两份 PDF 手册；下载中心与授权 API、数据库和 AI Relay 分离，并完成公网 HTTPS 与 SHA-256 验收。
- 完成公众号外部交付配置：更新“学工智伴”简介和关注欢迎语，保存“工作台、购买、激活、安全、帮助”等快捷回复并补充友情 AI 说明；两篇用户向产品/购买文章已保存为草稿，未发表或群发。链动小铺商品和真实支付仍等待商家后台登录后落地。
- 扩充 v4.9.0 客户材料：产品手册新增桌面端/网页版/单文件离线 HTML 对比、许可证与工作区关系、换机恢复、期末增量导入和 macOS 交付边界，并按模块补充 30 余张虚构演示截图；新增一篇长篇公众号主推文和分篇素材，统一使用“前瞻版”称呼；前瞻版交付包目录更新为 `output/学工智伴-v4.9.0-前瞻版交付包-20260826-mac构建/`。本次只准备和保存草稿，不发表、不群发；macOS 仍未签名/公证，链动小铺商品和真实支付仍未完成。

## [Unreleased] · 长期使用维护

- v4.9.0 候选本轮优先收口长期数据维护和桌面自动更新，暂停继续扩展 AI 语音识别；当前候选仍不是正式商业 Release，真实微信支付、生产授权、下载 CDN、Windows/macOS 签名/公证和跨重启实机升级仍待外部配置与验收。
- 统一学生及业务集合的持久化分区队列：普通编辑、批量/带附件删除和基础记录更新使用同一写入顺序；自定义集合按整体持久化分区串行化，避免连续操作时后一次修改被前一次失败回滚覆盖。
- Electron 商业候选新增低频自动更新检查：启动约 30 秒后首次检查，随后每 6 小时检查一次；只检查和提示，不静默下载或安装。更新状态通过受限 IPC 回传，用户仍需确认下载和重启安装，原有清单签名、包哈希、平台签名、恢复点和失败回滚规则不变。
- 新增 `desktop/update-scheduler.cjs` 与 `tests/update-scheduler.js`，覆盖重复启动、调度停止、检查失败记录和单次运行防重入；当前仍未具备真实更新 CDN、Windows/macOS 签名证书和跨重启实机证据。

- 学生主档案新增“进行中事项快照联动”规则：单人编辑、批量编辑和名单导入会同步带稳定 `student_id` 的未完成任务、待确认草稿及其他当前快照；已完成任务、成绩、谈话、奖惩、资助、心理、查课和住宿轨迹等历史事实不改写，失败和撤销会一起恢复联动快照。
- 备份与迁移页面的右侧上下文现在显示正确的“备份与迁移”标签；学生导入解析提示在预览打开后显示真实行数，不再把“正在解析”留在用户面前造成状态误解。
- 新增 `pnpm test:long-term` 与 `tests/student-live-link-sync.js`，覆盖进行中事项更新、历史快照保护、旧学号快照不自动重绑；录音继续通过独立转写模型和音频端点诊断，不把文本聊天能力当成语音能力。
- 批量编辑学生现在先显示可同步的进行中事项数量，并允许关闭关联同步；批量撤销同时恢复学生、班级历史和进行中事项快照，若关联事项在撤销前又被修改则跳过该行并提示人工核对，避免用旧数据覆盖新工作。

- 学生导入旧同步入口不再绕过稳定身份规则：学生集合的 `commitPreview` / `commitCSV` 明确要求异步身份确认流程，避免期末补录或学号变更后重新建重复档案。
- 学生导入成功反馈增加待人工核对、匹配方式和班级历史数量，并在真实持久化后刷新页面；撤销动作同时恢复学生主表与 `v4_student_class_history`，失败时两侧一起回滚。
- 功能导览的“备份与更新”按钮直接跳转备份页面，减少首次使用时的错误路径。

- 首次功能导览、欢迎体验重置、每日问候已读状态和首页自定义统计视图统一改为事务保存：只有本机工作区确认写入后才更新界面；失败时恢复旧状态并提供重试，不再出现“刚调整的偏好刷新后回退”。
- 自定义统计视图取消浏览器设置与工作区适配器的并行双写，统一以单个设置事务保存，避免断网、桌面仓储异常或快速连续操作后两端各自保存一份不同配置。
- 工作节点、政策智库、工作素材、科创竞赛和模板库的新建/编辑统一等待真实持久化完成；失败会恢复原列表和编辑对象引用，重试不会丢失原编辑上下文或写出半条记录。
- 新增 `pnpm test:transactional`，覆盖数组事务在失败新增、失败编辑和原对象重试下的实际回滚行为；完整跨页面 `interaction-continuity` 已在本批重新执行并通过。
- 明确录音转写由独立语音模型和 `/audio/transcriptions` 端点完成，文本聊天成功不能推导出语音可用；AI 设置的“检查语音”只验证静音 WAV 探针，不代表真实普通话、方言、长录音或手机端识别质量。
- 全局设置、学生筛选方案和台账列视图纳入对象级事务保存；失败恢复原设置对象和嵌套偏好，只有真实落盘后才关闭窗口并提示成功。
- 远程备份配置、自动备份计划、欢迎设置、主题/背景、访问锁和导入字段映射预设也统一等待真实写入；自动备份计划在配置、调度或密钥保存失败时恢复原计划，避免长期使用中的半成功状态。
- 按长期使用场景继续收口高频维护：照片人工归档、班团任职导入/删除、就业资源编辑/收藏/删除/CSV 导入均等待真实持久化完成，失败恢复原列表、队列或附件关联，避免刷新/重启后出现半成功数据。
- 增加长期数据维护检查清单：每个按钮必须明确写入集合、反向引用、成功等待点和失败回滚范围；学生字段变化仍不自动改写历史成绩、谈话、住宿、奖惩和资助事实。
- 补充录音 AI 的真实边界：应用已调用 OpenAI-compatible `/audio/transcriptions`，但是否可用取决于服务商端点、独立转写模型、MIME 格式和额度；未完成真实中转站、Electron 和手机麦克风验收，不把测试桩结果当作生产可用承诺。
- 追加“学号、姓名、分数”期末补录回归，确认缺少培养层次等字段时更新原学生综合成绩，不新建重复档案。
- 修复 AI 能力矩阵重复渲染，文本、图片和语音状态各显示一次。
- 活动和工作留痕保存改为等待真实写入后确认；失败时恢复主记录、参与关系和表单状态，并清理本次新增附件。
- 通讯录、课表、就业意向、就业联系、班委岗位和业务文件新增/编辑复用统一数组事务保存，避免失败后残留或重试重复新增。
- 任务、谈话和回访改为等待任务、关联待办与工作留痕草稿全部真实保存后再提示成功；失败会恢复原状态并保留表单，重试不重复创建。
- 删除回访任务会清理谈话反向引用；删除谈话会保留回访待办并解除失效来源关联，避免长期使用时产生孤立或丢失的后续工作。
- 平台任务拉取等待本地任务真实落盘后才返回结果，减少切页或刷新时的状态错觉。
- 新增“学生关联维护”入口，集中扫描旧业务记录中缺少/失效的 `student_id`；当前学号和唯一历史学号只提供候选，姓名绝不自动合并，老师确认后只补稳定关联和缺失快照，不改写历史事实。
- 增加保存失败回归证据；本批自动化结果不等同于真实 Electron、手机麦克风或商业发布验收。
- 点名结果只有在结果记录和工作留痕草稿一起真实落盘后才更新界面，失败会恢复未保存的复核状态。
- 动态分班历史按生效日期、创建时间和记录 ID 稳定排序；早于首次变更的历史查询使用最早原班快照，同日多次变更不再依赖数组顺序。
- 新增班级历史链路检查和“待核对”状态；手工补录复用统一稳定 ID/快照格式，存在更晚记录时不静默改写当前档案，删除历史后在事务内重新计算当前班级。
- v4.8 受影响批次综合验证 `pnpm test:v48` 通过；该自动化结果不等同于真实 Electron/手机、生产 AI 服务商或商业发布门禁已完成。
- 活动、工作留痕删除统一等待主记录、来源状态、参与关系和附件清理完成；文件删除和就业资源收藏失败会恢复原状态。
- AI 设置页新增“检查语音”，使用本机生成的静音 WAV 检查服务商音频端点，不发送或保存真实谈话；文本聊天可用不等于语音转写已验收。
- AI 工作区修复生成 HTML 的 `card-bd` 标签错误；上下文、模型、用途和操作区改用专用响应式布局，避免三栏桌面宽度下控件挤成竖排。已用 1440×920、1024×768、390×844 本地预览检查，仍明确区分模型配置成功与真实服务商语音质量验收。
- 学生台账新增按当前真实资料派生的“下一步补录建议”，最多显示三项；联系方式、住宿可直接进入批量编辑，成绩、照片和旧关联跳转到对应模块，不把缺失资料当成错误或零值。
- 学生详情新增稳定 ID、仅学号快照和涉及模块的跨模块关联摘要；发现旧快照时提供人工维护入口，修改当前档案不会静默改写历史成绩、谈话、住宿、奖惩或资助事实。
- 首次引导的步骤跳转、完成、跳过和重置等待真实设置持久化后才执行，失败恢复引导状态，避免用户看到“已完成”但刷新后回退。
- 学生跨模块影响摘要补齐多人关系数组（活动参与、查寝、点名、一生一表等），并继续严格区分稳定 `student_id` 与历史学号快照；不扫描姓名或备注自动猜测身份。
- 学生批量编辑、归档和撤销改为原地更新与原地回滚，保持详情弹窗、编辑器和筛选状态引用；v4.7 查课、查寝、量化积分和竞赛报名删除等待业务与附件事务完成，失败自动恢复附件。
- 新增 `tests/student-relationship-impact.js`，验证多人关系识别和历史快照分离；`test:transactional` 已包含该回归测试。
- 学生主档案长期变更新增完整关联影响提示：联系方式、家长/紧急联系人、家庭与居住、预警、备注、自定义字段和照片均可见；自定义字段按稳定内容比较，导入列顺序变化不会误报。
- 单人编辑、学生增量导入和批量编辑修改班级时，会新增带生效日期的 `v4_student_class_history` 事实，供后续走访、课表和宿舍联查按日期读取；历史成绩、谈话、处分、资助、心理和住宿记录不被改写，撤销/失败会同步回滚班级历史。
- 首次引导进一步说明示例不覆盖真实数据、AI 可稍后配置、期末三列成绩表可增量补录且合并导入不删除缺席学生；体验、跳转、备份、恢复、跳过和完成均在状态真实保存后执行，失败提供重试。
- 示例工作区新增相对日期锚点：只平移仍标记为示例的任务、回访、课题阶段和自定义记录，避免用户隔几天回来后看到无意义的历史逾期；真实编辑记录不会被改动。
- 首次示例导航默认优先展开工作台、学生台账和谈心谈话，低频模块收起但仍可搜索、展开和钉住，降低长期使用中的侧栏噪声。
- 录音窗口补充浏览器能力检查和可执行下一步，HTTP 400/401/403/404/413/415/429 分别翻译为模型参数、凭据权限、接口、请求大小、音频容器和限流问题，避免把“文本能聊天”误认为“录音能转写”。

## v4.9.0 商业中心流程收口（候选维护）

- 工作台商业中心增加档位选择，购买入口会把所选产品带入订单页；用户可以先用样例数据体验，未配置正式支付时不会显示虚假的支付成功。
- 已激活用户选择更高档位时进入专用升级订单，授权服务验证当前许可证后按产品目录计算差价，保留升级来源和订单审计；新增服务端升级订单契约测试。
- 普通用户说明补充了当前实际要做什么、购买/激活/升级/更新/三端互通的操作边界。微信支付商户、邮件、KMS、签名和正式下载服务仍属于上线前置条件。
- 维护收口补齐学生三列成绩增量导入、通用业务导入自然键更新与失败回滚、跨模块脱敏样例，以及 Responses 文本协议核验；视觉和音频 AI 仍不作未验证承诺。

这里记录的是面向使用者与贡献者的产品演进，而不是开发过程摘录。版本状态以 [GitHub Releases](https://github.com/7752777/counselor-desk/releases) 中实际存在的附件、校验文件和说明为准。

## [Unreleased] · 后续维护

### 长期数据联动与 AI 录音收口（2026-08-24）

- 手工分班变更和删除现在会同步进行中事项的学生快照，并把学生、班级历史、任务与自定义记录纳入失败回滚边界；历史成绩、谈话、奖惩、资助、心理、查课和住宿轨迹不被改写。
- AI 音频传输增加底层能力守卫：必须配置独立转写模型、明确音频端点、用途授权和可接受录音格式，文本聊天可用不再被误判为录音可用。
- 通过长期关联、录音协议、UI 连续性、学生导入以及浏览器/离线多视口路由验证；真实服务商、Electron/手机麦克风和商业发布仍待实机验收。

### 使用者视角持续打磨（2026-08-23）

- 首次设置保存改为等待真实持久化完成；失败时保留输入、恢复内存状态并提供重试，避免“提示成功但刷新丢失”。
- 设置完成后不再连续弹出今日问候；问候改为首页内联提示，支持换一句、稍后再看和关闭，不阻断日常工作。
- 首次引导把“先体验示例”作为主入口，并明确名单、成绩和其他资料可以在后续工作中逐步补录。
- AI 工作台新增文本生成、图片识别、语音转写能力矩阵；模型、中转地址、独立转写模型、视觉声明和凭据分别显示可用状态。
- AI 模型连接表单补充中转地址和中转令牌配置，凭据不进入业务备份、导出和交换包。

- 首次使用卡片改为“设置个人信息 → 导入真实名单 → 完成首次加密备份”的可执行三步路径，并为桌面和移动端提供清晰的下一步按钮。
- 空状态统一支持说明、可访问状态和真实操作入口；保存失败优先按稳定错误码翻译，保留输入、重试和数据未改变提示。
- AI 当前页入口区分“未激活许可证”“未配置模型”和“缺少 API Key”，未授权时只展示激活入口，不发送模型请求。
- 学生台账增加“导入名单 / 更新”与“录入课程成绩”的分流引导；导入预览显示更新已有记录、新增记录、人工核对、身份匹配依据和实际写入字段。
- 期末仅含“学号、姓名、分数”的 CSV / Excel 会按当前学号更新已有学生综合成绩，不因缺少培养层次重复建档；空白字段默认保留，课程化成绩继续进入成绩管理。
- 新增[使用者视角持续打磨审计](./docs/upgrade/user-centered-polish-audit-2026-08-23.md)，记录本轮验证结果和下一批真实设备验收范围。
- 首页示例状态新增“导入真实名单”主入口，清空示例数据降为次级操作；首页待办严格只显示最紧急 5 条，其余进入完整任务列表。
- 合并首页折叠和路由 UI 状态的工作区写入，避免连续导航拖慢通知本地预览等高频操作；最新 UI 状态仍会持久化，失败仍保留恢复提示。
- 就业意向、就业联系和业务档案的学生下拉框增加“请选择学生”占位，保存前按当前选择重新绑定稳定学生 ID，避免新增记录误挂到名单第一人；连续性回归同步修正为按学生和年份定位记录。
- 请假、考勤、预警、帮扶、奖助勤补、重点学生、心理、毕业生、住宿、假期去向和评优申报等学生关联表单统一保存稳定 `student_id`；成绩、奖惩、活动和工作留痕表单补充学生选择与冲突提示。
- 高频学生表单保存失败会回滚内存记录并保留表单，重试不会重复新增；连续性测试新增传统考勤表单失败回滚、重试和稳定 ID 验收。
- 长期数据联动继续收口：当前/历史学号解析进入统一学生引用索引，同一历史学号发生歧义时转人工核对；谈话和学生时间线移除姓名自动兜底，避免同名学生串联。
- 学生档案保存前增加关联影响确认，显示变更字段、关联模块和记录数量，并明确历史成绩、谈话、处分、资助和心理事实不会被自动改写；保存后提示旧快照记录，方便逐步补齐稳定 `student_id`。
- AI 语音配置拆分文本模型与独立转写模型，增加服务商音频能力声明、录音前就绪诊断、实际 MIME/大小显示和 404/415/400 音频错误分类；未确认支持音频的中转站不会让用户误以为“文本能用就能转录”。
- 新增[长期使用数据联动与 AI 语音链路审计](./docs/upgrade/long-term-data-and-ai-audit-2026-08-24.md)，记录数据更新原则、AI 录音技术链路、验证证据和真实设备剩余风险。

后续修改将在新的维护版本条目中记录；当前 v4.8.5 发布状态以私有 GitHub Release、Tag 和实际产物为准。原 Pages 入口不作为当前可用性证明。

**当前 v4.9.0 工作区候选状态：** 已加入商业许可证、四档权益、AI 解锁、Electron 专用授权存储、`electron-updater` 运行依赖、签名更新契约、取消下载入口，以及 PostgreSQL/KMS/Fastify 授权服务候选、订单幂等、支付事件重放保护、邮件 outbox、管理员 Key、更新清单生成/签名和签名凭据 fail-closed 工作流。新增客户 `/customer` 购买/订单页、受订单令牌保护的 `.cwb-license` 下载、7 天取件令牌过期、精确 CORS、许可证文件/二维码导入专项测试、Windows/macOS 平台签名校验封装、更新状态持久化和恢复点回滚契约。尚未接入真实 KMS/PostgreSQL、支付 webhook、邮件/CDN、Windows/macOS 代码签名、公证和正式 Release；v4.8.5 正式 Tag、Release 和旧数据格式不移动。

本候选对外产品名称统一为“学工智伴”。四档建议价为普通版 10 元、普通永久更新版 20 元、AI 增强版 40 元、永久 AI 增强版 60 元；前瞻体验用户可按人工核验规则获得普通永久更新版赠送或 AI 升级优惠。产品内已提供购买、激活、更新说明、版权提示、开发者微信 `Windsky0823` 和反馈奖励说明；支付、邮件、生产发码、CDN、平台签名和公证仍未部署，不能把候选 UI 当作正式商业服务。

本轮候选进一步加入 1-30 天试用授权、最多 500 份的批量独立许可证、学校/学院工作区授权池和默认关闭的匿名运行指标白名单。真实生产服务、支付、邮件、KMS/HSM、平台签名、公证和实机升级证据仍是正式商业发布前置条件。

本轮最新收口补充：锁定 AI 入口覆盖旧通知路由并支持键盘打开激活；授权服务生产启动、直接迁移和管理员 Key 命令统一拒绝非生产标记、未启用 PostgreSQL TLS、空/非 HTTPS CORS、关闭 HTTPS和共享明文管理员令牌；授权服务健康接口增加数据库探测。详细对账见 [v4.9.0 实施对账与发布前审计](./docs/upgrade/v4.9.0-implementation-reconciliation-2026-08-23.md)。

本轮数据可靠性收口：任务完成/删除、v4 业务批量/单条删除和分类文件批量删除统一等待真实持久化；失败时恢复记录、工作留痕草稿和附件，不再先显示成功提示。学生样例扩展通讯录、就业、文件目录和活动参与关系；学生导入说明明确“学号、姓名、分数”是增量更新入口。定向回归新增任务失败回滚和文件批量整体回滚，当前状态与未完成商业条件见[全量收口审计](./docs/upgrade/full-plan-closeout-audit-2026-08-23.md)。

本轮商业闭环维护：工作台商业中心改为“体验 → 选档支付 → 获取许可证 → 激活 → 更新/互通”五步入口；新增受 HTTPS 校验的公开下载中心配置 `CWB_DOWNLOAD_CENTER_URL`，未配置时下载按钮保持禁用；授权服务新增部署侧支付适配器契约和订单“去支付”地址，支付成功仍只接受签名 webhook 或管理员确认。新增[普通用户购买、激活、更新与互通说明](./docs/customer-quickstart-v4.9.0.md)和[商业闭环与平台选型方案](./docs/upgrade/commercial-closed-loop-v4.9.0.md)，明确链动小铺适合试运营但官方 webhook/API 仍需核实，Cloud Studio 只作为开发/预览环境。真实支付、KMS、域名、CDN、签名、公证和实机证据完成前，仍不发布 v4.9.0 正式商业包。

本轮固定兑换码维护：新增“内测普通永久更新版”和“贡献者/老客户永久 AI 增强版”两类长期兑换活动。两段固定码只作为服务端兑换凭据，服务端只保存 SHA-256 哈希，并为每个工作区签发独立许可证；不再让用户共用同一张 3 台设备授权。客户端授权窗口可识别 `CWB-REDEEM-1...` 并在线兑换后按原签名校验、设备绑定和安全存储流程激活。新增增量 PostgreSQL 迁移、并发兑换串行化、活动审计和定向回归；真实明文兑换码尚未生成，仍须在受控服务器终端生成并放进部署侧密钥模块。

本轮服务器前瞻候选落地：在新服务器部署了隔离的 PostgreSQL 授权服务、systemd 运行单元、前瞻候选 Ed25519 签名器和链动小铺兼容的逐行库存导出脚本；本机已验证四档产品查询、临时卡密生成、激活、撤销和测试文件清理。服务仅监听回环地址，公网域名、HTTPS、正式 KMS/HSM、支付回调和正式下载入口仍未上线，详见[授权服务前瞻候选服务器记录](./docs/upgrade/staging-server-deployment-v4.9.0.md)。

## [4.8.5] · 2026-08-22 · 发布门禁时序维护

本版本是 v4.8.4 的兼容维护补丁，不改变 `schema_version: 8`、`data_schema_version: 11`、`sync_protocol_version: 1`、物理 IndexedDB v6、旧导入格式、附件 ID 关联或历史 Tag/Release。

- 修复远程备份管理界面验收在 CI 慢速 runner 上固定等待 60ms 导致的异步保存竞态；测试现在等待真实保存 Promise 和最终配置状态。
- 保留 v4.8.4 已完成的单文件离线图标内联、`file://` 路由验收、HTTPS/WebDAV 传输验收和完整 v4.8/AI 主门禁。
- v4.8.4 Tag 已推送但未通过发布门禁，未生成正式 Release 或平台资产；该 Tag 保留为不可覆盖的候选证据，v4.8.5 作为通过最终门禁后的正式维护版本。

**当前 v4.8.5 发布状态：** 正式 Tag、Release、Windows/macOS/Web 产物和 SHA-256 清单以[v4.8.5 发布收尾记录](./docs/upgrade/release-v4.8.5.md)及 GitHub 私有 Release 为准；v4.8.4 及更早历史资产不移动。

## [4.8.4] · 2026-08-22 · 发布完整性与门禁维护

本版本是 v4.8.3 的兼容维护发布，不改变 `schema_version: 8`、`data_schema_version: 11`、`sync_protocol_version: 1`、物理 IndexedDB v6、旧导入格式、附件 ID 关联或历史 Tag/Release。

- 修复单文件离线 HTML 的 `assets/app-icon.svg` 外部依赖，将网页图标内联并在构建时阻止未内联产物通过。
- 新增真实 Chromium `file://` 离线验收，覆盖四个桌面/移动视口、232 次主导航路由渲染、移动抽屉、Escape、页面错误和横向溢出。
- 新增本地自签名 HTTPS/WebDAV 传输端到端测试，覆盖连接、上传、下载、删除和 404 失败路径；文档明确不把它扩大为第三方服务商互操作证据。
- 将完整 v4.8 与 AI 定向测试套件、离线 `file://` smoke 纳入主 `pnpm test` / `pnpm test:release`，并使发布构建和文档检查按当前版本动态工作。

**v4.8.4 候选状态：** Tag 已保留，但发布门禁未通过，未生成正式 Release；正式使用请以 v4.8.5 Release 为准。

## [4.8.3] · 2026-08-22 · 跨端高频页面体验维护

本版本是 v4.8.2 的兼容维护发布，不改变 `schema_version: 8`、`data_schema_version: 11`、`sync_protocol_version: 1`、旧导入格式、附件 ID 关联或历史 Tag/Release。

- 修复记录级 AI 学生摘要被追加到学生表格最右侧的问题；AI 操作现在固定在学生行的 sticky 操作列，并为长文案提供稳定换行空间。
- 修复 `390×844`、`360×800` 窄屏下学生台账分页控件被裁切的问题，下一页按钮保持可见并可操作。
- 修复资料与平台列表模式在移动端仍使用桌面多列布局的问题；窄屏改为单列卡片，上传、编辑、删除和 AI 操作都保留在视口内。
- 通过学生台账分页、v4.7 视觉契约、真实 Chromium 桌面/移动页面验收；不新增数据迁移，不改变 AI 授权、同步、备份或附件安全边界。

**v4.8.3 发布状态：** 正式 Tag、Release、Windows/macOS/Web 产物和 SHA-256 清单以[v4.8.3 发布收尾记录](./docs/upgrade/release-v4.8.3.md)及 GitHub 私有 Release 为准；v4.8.2 及更早历史资产不移动。

## [4.8.2] · 2026-08-22 · 持久化可靠性维护

本版本是 v4.8.1 的兼容维护发布，不改变 `schema_version: 8`、`data_schema_version: 11`、`sync_protocol_version: 1`、旧导入格式、附件 ID 关联或历史 Tag/Release。

- 局域网客户端状态保存改为串行队列，提供 `waitPersistence()`；异步保存失败会恢复最后一次成功的队列、冲突、游标和连接状态快照，避免“页面显示成功但刷新后队列消失”。
- 客户端连接、配对、拉取、推送、冲突列表和冲突解决等待真实持久化完成；持久化失败返回 `SYNC_STATE_PERSIST_FAILED`，不会被包装成普通网络成功。
- 学生、普通业务和自定义集合仓储的 `put`、`putMany`、`delete` 在底层写入失败时恢复原内存集合；批量内存仓储先验证完整输入，再执行替换，避免坏记录导致已有数据被清空。
- 新增 `tests/v48-sync-persistence.js`，覆盖单次失败回滚、后续成功写入和前序失败不阻断后续快照；同步服务、断网恢复、附件存储、桌面和管理 UI 定向测试通过。
- 不新增迁移；v4.8.1/v4.8.0 工作区可以直接读取。Windows/macOS 未签名、macOS 未公证、真实手机证书信任、WebDAV 服务商互操作、长期多设备压力、浏览器关闭期间后台写文件和数据库级加密仍是明确限制。

**当前 v4.8.2 发布状态：** 正式 Tag、Release、Windows/macOS/Web 产物和 SHA-256 清单以[v4.8.2 发布收尾记录](./docs/upgrade/release-v4.8.2.md)及 GitHub 私有 Release 为准；v4.8.1 及更早历史资产不移动。

## [4.8.1] · 2026-08-22 · 发布后维护收口

本版本是 v4.8.0 的兼容维护发布，不改变 `schema_version: 8`、`data_schema_version: 11`、`sync_protocol_version: 1`、既有导入格式或历史 Tag/Release。

- 证书识别授权弹窗明确显示“确认发送证书图片给模型”，让老师在出站前清楚知道发送对象；仍保持单次授权、单附件范围、失败/取消失效和人工确认后写入获奖档案的边界。
- 桌面恢复向导统一使用至少 12 位恢复口令提示，导出增加二次输入确认、`minlength`、处理中禁用和失败重试；导入错误口令不会替换当前数据。
- 补充管理 UI 回归断言，并完成 `v4.8.1` 构建前的五个视口真实浏览器验收：`1440x920`、`1280x800`、`1024x768`、`390x844`、`360x800`。
- 发布边界保持不变：Windows/macOS 包未签名、macOS 未公证；真实手机 HTTPS 信任、WebDAV 服务商互操作、长时间同步、数据库级加密和云端实时业务同步仍不是默认承诺。

**当前 v4.8.1 发布状态：** 正式 Tag、Release、Windows/macOS/Web 产物和 SHA-256 清单以[v4.8.1 发布收尾记录](./docs/upgrade/release-v4.8.1.md)及 GitHub 私有 Release 为准；v4.8.0 历史资产不移动。

## [4.8.0] · 2026-08-22 · 客户反馈与本地协同增强

本版本在 v4.7.1 基础上完成客户反馈收口与本地协同增强。正式下载以私有 GitHub Release 中的离线 HTML、Windows x64/ARM64 安装包、macOS Universal DMG/ZIP 及 SHA-256 清单为准。

- 收口 AI 建议、通知、敏感授权和批量审阅的真实持久化等待、失败回滚和重试语义。
- 修复已有浏览器 IndexedDB v5 工作区升级后缺少 v4.8 新集合的问题；保留旧记录并增量创建新 stores。
- 修复移动端学生台账表格模式因备用内容缺失导致的空白页面。
- 优化工作区 5,000 条记录替换的重复深拷贝，保留历史版本、危险键校验和持久化隔离，性能断言通过。
- 完成真实浏览器五视口验收、Windows x64/ARM64 与 macOS Universal 打包验证和最终自动化门禁。
- 发布边界：Windows/macOS 包未签名、macOS 未公证；局域网首期只面向同一网络，浏览器关闭期间无法后台写文件或同步；数据库级加密、真实模型质量和云端实时业务同步不在本版本承诺内。

完整事实、证据分层和后续计划见 [当前全量状态与后续提升路线](./docs/upgrade/comprehensive-status-and-roadmap-2026-08-22.md) 和 [当前收口总账](./docs/upgrade/closeout-status-2026-08-22.md)。本节以下的候选过程段落保留为发布前审计历史，不改变当前 v4.8.0 发布状态。

### 发布前历史归档：2026-08-22 IndexedDB 旧工作区兼容修复

- 发现并修复浏览器已有工作区无法使用 AI 通知本地预览的问题：`src/core/v4-runtime.js` 原来一直以物理数据库版本 5 打开，新增 v4.8 集合不会触发 `onupgradeneeded`。
- IndexedDB 物理版本现在增量到 6，与工作区协议 `schema_version:8` 和业务 `data_schema_version:11` 分开维护；升级只创建缺失 object store，不删除旧数据。
- 回归测试真实创建 v5 旧库，确认旧学生记录仍可读取，并确认 AI、同步、备份和内容推送集合会在升级后存在。
- 修复后真实浏览器中通知 AI“本地预览重点”已恢复保存和结果渲染；本条记录的是发布前候选过程，能力已随 v4.8.0 正式 Release 交付。
- 完整状态和下一步路线见[当前全量状态与后续提升路线](./docs/upgrade/comprehensive-status-and-roadmap-2026-08-22.md)。

修复后最终自动化门禁已重新通过：`pnpm test`、`pnpm lint`、`pnpm build:release`、`pnpm check:public`、`pnpm check:secrets`、`pnpm test:release` 和 `git diff --check`。真实桌面、手机局域网、WebDAV、附件恢复和签名安装证据属于 v4.8.0 发布后的已知限制，已在发布收尾记录中列明。

### 发布前历史归档：2026-08-22 当前事实总账

该段是发布前的事实快照。当时 `codex/ai-upgrade` 上的 AI 持久化、局域网队列文案、UI 回归测试、IndexedDB 兼容修复和跨端恢复测试尚在候选阶段；随后 v4.8.0 已完成正式门禁并发布。当前状态详见[当前收口总账](./docs/upgrade/closeout-status-2026-08-22.md)。

本轮候选又修复学生台账卡片模式的多余横向滚动代理，并将参考工作台通知本地预览页面改为等待来源/审计持久化后再渲染；AI 定向回归和交互连续性失败回滚均已通过。通知回滚测试曾错误地在预览阶段注入失败，现已修正为只对确认事务注入失败；正式版本边界仍不改变。

本轮继续收口 AI 公共写入：建议、敏感授权、通知确认和批量审阅结果提供 `ready` 持久化承诺、`persistence_state` 和对应 `*Async` 接口，页面等待对象级保存完成后才显示成功；失败会恢复内存快照并保留重试路径。`pnpm test:cwb-ai` 已通过。该改动已随 v4.8.0 正式 Release 交付。

### 发布前历史归档：2026-08-21 局域网二维码配对

- 桌面端新增二维码配对入口，保留主机地址、配对 ID、配对码和证书指纹的明文复制回退。
- 二维码载荷采用 `cwb://lan-pair` 白名单协议，仅包含短期 HTTPS 连接信息；不会包含设备令牌、主密钥、学生数据、业务记录 ID 或附件 ID。
- 核心服务校验 HTTPS 地址、8 位配对码、证书指纹、版本、未知字段和过期时间；二维码生成失败时不阻断手动配对。
- 扫码不会自动信任证书或启用局域网自动发现，真实手机扫码与证书信任仍是正式发布前的验收项。
- 新增 `tests/v48-pairing-qr.js`，并将其纳入 `test:v48`；二维码能力已随 v4.8.0 正式 Release 交付，自动发现和真实手机证书信任仍是发布后限制。

### 发布前历史归档：v4.8.0 工作区候选（2026-08-21）

客户反馈执行方案已形成长期文档，详见 [v4.8.0 客户反馈执行方案](./docs/upgrade/customer-feedback-execution-plan-2026-08-21.md) 和 [客户反馈逐项审计](./docs/upgrade/customer-feedback-audit-2026-08-21.md)。以下内容记录发布前候选范围；当前正式版本为 `v4.8.0`。

- 已进入候选：Windows/macOS 局域网主机核心、一次性设备配对、HTTPS/证书指纹、幂等和字段级冲突模型。
- 已进入候选：Electron 解密错误诊断、版本化密钥、恢复口令核心、可选安装路径、数据目录迁移核心和桌面运行期间的每分钟备份到期检查。
- 已进入候选：`student_id` 优先、当前/历史学号兼容、合并导入、覆盖确认、恢复点和学生分析服务。
- 本批新增：客户端连接/拉取/推送/离线队列/冲突收件箱 UI、Electron 主仓真实记录适配、学生字段中心、动态分班、工作分类、联合走访、内容推送范围与内容包导入/导出、模板任务草稿、恢复诊断入口，以及逐次授权的敏感语音录音/转写页面和群体最小人数聚合。
- 本批维护收口：内容推送增加 `workspace_admin`、`content_editor`、`teacher`、`viewer` 本地策略角色；页面列表、已读和普通角色导出按当前工作区上下文过滤，管理员可按权限导出全量。工作留痕导出补充来源集合、记录 ID、来源状态、更新时间和顶层 `provenance` 回链，并增加构建产物运行时测试。
- 本批新增存储加固：局域网附件支持分块/断点续传、非连续分块恢复第一个缺口、SHA-256 校验和授权下载；正式附件与临时上传分块均使用 AES-256-GCM 加密落盘，旧候选明文附件兼容读取时先重加密，服务端所有设备路由统一校验 bearer token。备份成功会记录最多 50 条 `v4_backup_runs` 最小运行记录，且不会污染业务变更阈值。
- 本批新增远程备份核心与候选页面：`CWBV48.createRemoteBackupAdapter()` 只允许用户明确触发的 HTTPS/WebDAV 加密 `.cwbk` 传输，限制凭据 URL、重定向、超时和响应大小，不上传密钥；页面支持配置、测试连接、上传、下载预览恢复和删除，Bearer/Basic 凭据只保留当前会话；真实服务商互操作仍待验收。
- 本批新增：受支持 DOCX 模板字段校验、生成前缺失字段预览、单人/批量 Word 工作包和内容控件反向汇总预览/结构化 CSV 导出；任意 Word 自动识别和反向结果自动写回事实不在范围内。
- 发布后限制：手机真实 HTTPS 信任/自动发现与附件真实断网重连验收、真实 WebDAV 服务商互操作、Windows/macOS 实机迁移/恢复回滚、学生批量维护真实大名单验收、多设备长期压力和真实设备逐集合恢复仍需在目标环境抽查。
- 本批安全边界收口：空数据目录参数在解析前拒绝；迁移候选仓激活失败和原仓回滚失败分别返回可诊断错误码，并在回滚失败时停止写入；远程备份端点拒绝绝对 URL、查询参数和原始/编码目录穿越；网络请求不会吞掉本地端点校验错误。
- 本批早期交互补充：局域网桌面配对弹窗可复制主机地址、一次性配对 ID、配对码和证书指纹；后续二维码批次已在本 Unreleased 条目顶部补齐，自动发现仍未启用。
- `tests/v48-cross-platform-recovery.js` 已通过脱敏契约验证浏览器工作区、单文件离线 HTML、加密备份、手机工作包、交换包、自定义集合以及附件 ID/内容回读；这不等同于真实设备或长期同步验收。
- 候选使用 `schema_version:8`、`data_schema_version:11` 和 `sync_protocol_version:1`；v4.7.1 历史 Tag/Release 不覆盖。
- 发布前门槛已在后续完成：全量测试、跨端恢复、密钥扫描、构建、Release 门禁和产物检查的正式证据见 [v4.8.0 发布收尾记录](./docs/upgrade/release-v4.8.0.md)。

2026-08-21 发布前历史归档：同步主机的配对、push、冲突解决和设备撤销在状态持久化失败后恢复内存快照，并在 Electron 仓储中回滚已有/新增业务记录；客户端地址校验错误不再被包装成网络错误；配对码改为 8 位数字并在 5 次错误后限流；新增 `v48-p0-hardening` 和按风险拆分的测试脚本入口。该能力已随 v4.8.0 正式 Release 交付。

2026-08-21 发布前历史归档：客户端配对请求现在可在主机确认后通过请求 ID和设备 ID一次性领取令牌；主机设备支持暂停/恢复/撤销；客户端增加立即同步和 60 秒自动检查，失败时保留队列和错误状态；健康状态和 `ui_state` 不再暴露 `token_hash`或会话令牌。新增 `test:sync-resilience`并补充 HTTP 配对结果、一次性领取和状态脱敏测试；该能力已随 v4.8.0 正式 Release 交付。

2026-08-21 发布前历史归档：成功照片上传只执行一次学生记录持久化，只有旧附件清理失败时才追加保存待清理标记；学生档案编辑不再在照片内部保存成功后重复保存。照片失败仍恢复原引用、清理新附件并保留重试路径。新增 `tests/photo-storage.js` 的单次写入回归断言；该能力已随 v4.8.0 正式 Release 交付。

2026-08-21 发布前历史归档：大名单持久化契约使用 `__cwb_bulk_students__` 内部载荷，业务仓储 reopen 后仍恢复完整逻辑名单；修正 `v8-canonical-idb-browser` 测试，避免把物理 IndexedDB 行数误当学生人数。修正后的完整回归中 10,000 条导入耗时 `4788.5ms`，最大进度间隔 `9.4ms`、事件循环间隔 `176ms`；完整构建、lint、公开面、密钥扫描和 Release 契约已完成，正式发布证据见 v4.8.0 收尾记录。

## [4.7.1] · 2026-08-21 · AI 与工作流交互收口维护

本版本在历史 `v4.7.0` 的参考工作台和三栏 UI 基础上，收口 AI 可用性、保存可靠性、来源链和跨端交互。它不改变工作区协议 `schema_version: 8`、业务 `data_schema_version: 10` 或稳定 `student_id` 关联，也不覆盖历史 Tag/Release。

- AI 页面入口增加模型、用途、当前会话凭据、视觉能力和每日额度的点击前可用性检查；公共运行入口继续二次校验，未配置模型时本地通知预览仍可用。
- 每日额度按模型连接与用途隔离，只统计成功的 `generate`；连接测试、失败、取消和本地通知预览不消耗生成额度，旧审计按连接器与模型名兼容读取。
- 统一保存队列、表单失败保留和重试提示，来源记录与工作留痕草稿的变化/删除状态会一起持久化，避免“按钮已点但数据未落地”或旧草稿误归档。
- 工具结果、科研阶段任务、附件回滚、共享附件保护、证书学生 ID 校验和 Electron 学生卡片模式继续使用稳定来源键与真实档案校验，重复点击不会制造重复记录。
- 首页、学生台账、AI 工作区和移动抽屉完成真实桌面/手机视口复核；AI 页面不再向辅导员展示模型连接等技术配置术语，导航关闭状态具备正确的焦点和无障碍标记。
- 本版本的实现、测试、已知限制和发布证据见 [v4.7.1 发布收尾记录](./docs/upgrade/release-v4.7.1.md) 与 [AI 模型可用性审计](./docs/upgrade/ai-provider-readiness-audit-2026-08-21.md)。

发布前验证已在工作区完成；Windows/macOS 构建、正式 Release、Pages 和最终产物哈希以本版本发布记录的实际证据为准。

2026-08-20 复核：本条保留发布前的 AI 逐项核对过程；当前 AI 状态和剩余风险见[AI 全面收口审计](./docs/upgrade/ai-comprehensive-audit-2026-08-20.md)。

2026-08-21 保存链路维护：基础集合触发工作记录草稿来源复核时，`v4_worklog_drafts` 的 `custom` 变化会与基础记录一起同步到 v8 工作区；来源删除的过期状态使用待同步版本保留到保存成功。统一保存队列和 `awaitTrackedSave()` 让确认框/表单在真实写入成功前不关闭，失败时可重试且不会重复创建记录。来源指纹补充备注、查课/查寝事实、地点、处理措施、优先级和截止日期字段；`updated_at` 明确只作为存储元数据，不参与来源指纹，避免无关批量保存使草稿误报过期。

2026-08-21 定向验证：`pnpm test:interaction`、`pnpm test:v46`、`pnpm test:v47`、`pnpm test:cwb-ai` 和 `pnpm lint` 均通过；完整测试、构建、公开面、密钥扫描、发布契约和差异检查也已在最终代码状态通过。

2026-08-21 启动体验修复：内联启动早于 `defer` 核心脚本时，AI 工作台不再把短暂的未加载状态误报为“缺少治理运行时/集合”。DOM 就绪且 AI、v4.6、v4.7 运行时齐备后自动绑定治理对象并只重绘一次；真实脚本缺失仍会保留健康检查阻断。`pnpm test:v47`、`pnpm lint` 和 `http://127.0.0.1:4173/` 实测通过。

2026-08-21 AI 模型可用性收口：生成和页面内 AI 按钮会在点击前检查模型、用途、当前会话凭据、视觉能力和额度；请求入口仍二次校验。`v4_ai_audit.provider_id` 让每日成功生成额度按模型连接和用途隔离，旧审计按连接器与模型名兼容读取；连接测试、失败、取消和本地通知预览不扣生成额度。详细边界见 [AI 模型可用性审计](./docs/upgrade/ai-provider-readiness-audit-2026-08-21.md)。

2026-08-21 最终门禁验证：修复汇报页不应暴露技术配置语言、证书确认必须匹配真实学生档案后，完整 `pnpm test`、发布构建、公开面检查、密钥扫描、Release 契约和 `git diff --check` 均通过；Tag、Release、Pages 和最终产物哈希在发布完成后回填。

- 首页增加“今天先处理最重要的事”工作台，集中显示逾期、在办、重点关注、待确认草稿和今日查课摘要，并把待办控制在首屏可读范围内。
- v4.7 页面统一使用页面包裹、标题强调线、统计语义线、浅色筛选条、表格密度和焦点可见样式；保留当前品牌蓝，不复制参考产品品牌素材。
- 学生台账将常用搜索、班级、层次和展示模式前置，把更多筛选、排序和列视图收进可展开区域。
- 修正首页同时显示新旧两套工作区的问题：旧 today/KPI 不再重复输出，趋势、职责、学生结构和热力图收进默认收起的数据概览，并增加备份、照片、导入和党团流程维护状态。
- 桌面端继续使用左导航/中央主区/右侧上下文，手机端使用抽屉/单列/底部快捷栏；360px 以下压缩非核心顶栏状态，保留产品名和菜单入口。
- 增加 `tests/v47-polish-ui.js` 的窄屏 CSS、ARIA、抽屉语义和首页交互回归覆盖。
- 收紧 AI 显式上下文：指定学生或班级时会排除跨学生记录，无法解析的 `student_id` 不再被当作“未关联”放行。
- 生成审计会回填建议 ID、草稿 ID、来源 ID、字段级授权、完整工作范围和截断状态；任务、谈话、工作留痕和证书记录继续保留同一来源链。
- 增加不携带业务上下文的模型“测试连接”动作，只记录耗时、模型和安全错误码；AI 临时输出、请求内容和完整来源记录不再写入持久化 `ui_state`。
- AI 出站即使获得身份信息授权也强制移除内部 `student_id` 和本地 `id/record_id`；证书授权、模型调用、草稿和确认审计统一保留同一 `request_id`，任务/谈话/通用记录同步保留 `ai_request_id`。
- AI 本地来源目录现在为 `local` 来源生成稳定来源 ID、保存真实的 `url/source_url/official_url` 回链和业务指纹；资料被修改或删除后，建议状态会标记为需要复核，不能直接转为任务、谈话或工作留痕。
- `pnpm test:cwb-ai` 已覆盖来源目录、来源新鲜度、跨学生范围、出站脱敏、建议转化、通知降级、证书链和 relay 边界；本轮测试通过，这些修复归入 v4.7.1，历史 v4.7.0 不包含。
- 修复明确授权 `contact` 后手机号被通用数字规则二次脱敏的问题；补充只有学号快照的直接 AI 调用范围校验，并保留来源的 `student_number` 兼容快照但强制在出站时移除。
- 工作流来源校验复用完整 IPv6/保留地址判断，拒绝 IPv4-mapped IPv6、文档网段和其他特殊目标；`node tests/cwb-ai-workflow.js`、`node tests/ai-source-integrity.js` 和 `pnpm test:cwb-ai` 均已通过。
- `pnpm test:v47` 与 `pnpm test:optimization` 已通过；首次完整 `pnpm test` 暴露导入闭环测试仍匹配旧事件分发器变量名，更新 `tests/import-loop.js` 后完整测试退出码 0。
- 直连 AI provider 增加 4 MiB 默认、8 MiB 硬上限的 UTF-8 响应保护；超大响应和成功响应的非法 JSON 均返回安全错误码，不保存原始响应。
- 请求取消在响应体读取后再次检查；服务端返回后发生取消也不会继续生成成功审计、AI 草稿或建议。
- 图片请求增加 `supportsVision` 能力门禁；Anthropic provider 将 OpenAI 风格图片消息转换为 `image/source.base64`，公共运行入口过滤未知结构化消息字段和 content 类型。
- 建议详情新增“有帮助 / 需要修改 / 不适用”质量反馈；反馈只写入既有 `v4_ai_audit`，指标支持按最新反馈、反馈类型和用途统计，不新增数据集合。
- 继续收口 AI 治理：每日额度只统计真实 `generate` 完成审计；身份授权按当前学生/班级/实际来源收窄；relay 二次清理来源令牌查询参数；建议转化强制要求人工确认时间和确认方式；停用 provider 与取消连接测试写入明确审计状态；补齐额度、授权、附件、视觉能力、非法 JSON 和响应过大等中文错误提示。
- 继续做 AI 入口逐项对账：课表周视图的 `v4_class_schedules`、住宿当前安排的 `v4_dorm_assignments` 和已保存点名结果的 `v4_roll_call_sessions` 均补齐记录级目标绑定；随机点名上下文日期字段统一为规范化后的 `date`，并已加入跨模块回归测试。
- 收口 AI 用途注册表：工作台和建议中心现在直接展示 canonical 的“预警辅助分析”和“证书识别草稿”，建议筛选会把旧别名归并到规范用途；公共 `CWB.ai.run()` 遇到未登记用途会返回 `AI_PURPOSE_INVALID`，不再静默回退为工作总结。
- 全量测试事实更新：本候选首次完整 `pnpm test` 的失败已定位为测试静态契约问题；修复后完整 `pnpm test`、构建、lint、公开面、密钥扫描和发布契约均在最终代码状态通过。
- 新增模型输出二次脱敏：模型回显的未授权身份、联系方式和本地记录索引不会进入页面结果、AI 草稿或建议；`v4_ai_audit.output_redacted` 记录本地清理是否发生，原始响应不落库。
- 新增只读 `CWB.ai.health()` 与 AI 工作台运行状态卡片，检查治理脚本、用途合同、AI 集合、页面入口、模型、来源和审核队列；缺失页面入口现在会正确报告为接入缺口。
- 新增 `tests/ai-output-health.js` 并纳入 `pnpm test:cwb-ai`；当前 AI 定向门禁包含 15 个用例并实际通过，历史 `v4.7.0` 不包含这些修复。
- 收口业务按钮的连续生命周期：工具结果和科研阶段任务按稳定来源键幂等保存，重复点击不会重复创建任务或留痕；任务记录保留来源集合、来源 ID 和阶段。
- 家校联系、科研阶段和调宿的附件保存失败会回滚未被引用的附件；删除业务记录时检查共享引用，只清理孤儿附件并保留审计。
- 来源记录被删除或修改后，关联工作记录草稿会进入来源已删除/来源已变化状态，不能用旧草稿直接归档；保存失败保留表单输入和附件选择，允许修正后重试。
- 上下文面板补齐焦点进入、`Escape` 关闭、焦点返回和刷新状态恢复；Electron 学生台账卡片模式新增独立 3 条合成学生回归，覆盖照片、宿舍、导师、班主任、家长关系和刷新恢复。
- 新增[交互连续性与按钮闭环审计](./docs/upgrade/interaction-continuity-audit-2026-08-20.md)作为后续按钮、来源链和失败重试的维护验收基线。

本版本准备推送到私有仓库的 `codex/ai-upgrade` 分支；完整需求对照和验证边界见 [全量开发计划对账](./docs/upgrade/full-plan-reconciliation-2026-08-19.md)、[UI 收口审计](./docs/upgrade/ui-redesign-closeout-audit-2026-08-19.md) 与 [AI 实现逐项对账](./docs/upgrade/ai-implementation-reconciliation-2026-08-20.md)。正式发布证据以本版本发布收尾记录为准。

## [4.7.0] · 2026-08-19 · 参考工作台全功能与 UI 重构

历史发布记录曾完成：[v4.7.0 Release](https://github.com/7752777/counselor-desk/releases/tag/v4.7.0) 包含离线 HTML、Windows x64/ARM64、macOS Universal DMG/ZIP 和三份 SHA-256 清单；[发布门禁 #32211212549](https://github.com/7752777/counselor-desk/actions/runs/32211212549) 与 [Pages 部署 #32212075408](https://github.com/7752777/counselor-desk/actions/runs/32212075408) 是历史证据。仓库现已改为私有，原 Pages 当前不可访问；本工作区候选不属于该历史 Release。

- 逐页吸收用户提供的九页参考界面的工作台结构：统计摘要、筛选区、主操作、结果区和桌面右侧上下文区；`<=900px` 退化为导航抽屉、单列主区和底部快捷栏。
- 新增查课看板、课堂随机点名、宿舍查寝与异常、量化考评、工具箱、就业防骗、竞赛资源和学业分析页面；点名使用本地随机种子，AI 不参与随机决定。
- 通知 AI 使用原文/结果双栏，老师主动粘贴或导入后才生成可编辑重点、截止时间、待办和证据片段，确认后才能转任务或工作留痕草稿。
- 业务 `data_schema_version` 增量到 10，新增十个 v10 集合并统一接入浏览器 IndexedDB、离线 HTML、Electron、备份恢复和手机交换包；工作区 `schema_version: 8` 与稳定 `student_id` 兼容边界不变。
- 修复查课有数据时结果表格未闭合的问题；为 1024-1279px 中等桌面增加右侧上下文面板打开入口，并持久化折叠/打开状态。

功能差距、设计规则、安全边界和已知限制见[参考工作台功能与界面审计](./docs/upgrade/reference-product-feature-and-ui-audit-2026-08-19.md)；附件大小、SHA-256、跨端验证和发布证据见[v4.7.0 发布收尾记录](./docs/upgrade/release-v4.7.0.md)。

## v4.6.0 后续维护（已并入 v4.7.0）

本节保留 v4.6.0 公开后发现、并在 v4.7.0 一并发布的维护修复记录。它们没有移动或覆盖 v4.6.0 Tag/Release。

- 学生档案和家校联系默认电话脱敏；完整联系方式要求工作台访问锁和访问审计；班级分析个人明细与指标下钻同样受控。
- 班级分析按记录 `term` 筛选，活动参与按 `student_id + term + activity_id` 去重，学生时间线补齐家校联系、住宿/调宿、班委考核和活动参与。
- 排宿预览提前拦截重复学生，确认时继续复核实时床位和住宿状态；日期工具拒绝不存在的日历日期。
- 奖惩附件删除检查同集合共享引用，只清理孤儿附件；工作记录草稿在来源变化后必须重新核对，来源仍存在时刷新来源哈希，来源已删除时不可归档。
- 补充中文错误提示和重复动作注册清理，减少老师遇到内部错误码或后续维护误覆盖的情况。
- 修复无电话学生档案打开时对不存在联系方式按钮绑定事件的问题，并让 CRUD 烟测跳过文件输入控件赋值；完整回归和发布前门禁均已通过。

详细范围、实际验证和未发布边界见[收尾查漏补缺审计](./docs/upgrade/closeout-audit-2026-08-19.md)。

## [4.6.0] · 2026-08-19 · 日常工作与业务协同增强

本版本已正式公开：发布提交为 [`3db26ec`](https://github.com/7752777/counselor-desk/commit/3db26ec52d40e1779ee15e52a1578c4cba1cbc30)，[发布门禁 #32185919304](https://github.com/7752777/counselor-desk/actions/runs/32185919304) 和 [Pages 部署 #32188118210](https://github.com/7752777/counselor-desk/actions/runs/32188118210) 均成功；[v4.6.0 Release](https://github.com/7752777/counselor-desk/releases/tag/v4.6.0) 提供离线 HTML、Windows x64/ARM64、macOS Universal DMG/ZIP 和三份 SHA-256 清单。附件大小、哈希和未签名/未公证限制见 [v4.6.0 发布收尾记录](./docs/upgrade/release-v4.6.0.md)。

- 新增实用工具页：本地抽签、随机分组、轮值安排、日期计算和名单清理。默认不保存输入，只有明确保存才进入任务、留痕或名单；AI 不参与随机数决定。
- 新增住宿专项：楼栋、房间、床位、住宿批次、排宿预览、容量/性别/床位/重复入住检查、人工确认、调宿历史、导入、打印和导出。
- 扩展班委：工作区岗位字典支持权益、就业、科研等岗位、自定义角色、一人多职、代理、空缺、继任和任期；新增优秀/良好/合格/不合格考核，AI 只生成评价草稿。
- 新增独立家长联系记录和统一待确认工作记录草稿；谈话、家长联系、任务完成、查课查寝、活动/班会和 AI 通知转化均保留来源链，不自动制造正式事实。
- 新增科研课题阶段管理、阶段时间轴、截止提醒和去重任务关联；阶段变更保留历史，AI 只生成清单和提醒草稿。
- 奖励/荣誉记录支持证书图片、PDF 和文档附件，使用附件 ID、哈希去重、缩略图、索引导出和删除审计；识别结果需人工确认。
- 新增班级综合分析和首页紧凑工作摘要；聚合缺失显示“未记录”，敏感指标默认只显示数量，首页默认只列最紧急的 5 条。
- 修复 Electron 桌面端学生卡片模式，补齐 Electron/Pages 的 v4.6 运行时资源清单，并让 `v4.6.*` Tag 进入正式 Release workflow 触发范围。
- schema 从 v8 增量迁移到 v9，新增 10 个集合，不删除旧数据；学生业务继续以稳定 `student_id` 为关联主键，附件二进制继续进入附件仓。

安全边界：住宿分配、班委等级、奖惩事实、科研审批和敏感学生记录不能由 AI 自动修改；重点学生锁仍是防误看而非数据库加密。实现和验收范围见 [v4.6.0 日常协同审计](./docs/upgrade/next-optimization-audit-2026-08-19.md)，发布状态见 [v4.6.0 发布收尾记录](./docs/upgrade/release-v4.6.0.md)。

## [4.5.0] · 2026-08-18 · 老师反馈全量收口

本版本围绕 15 条一线反馈，把原本分散的字段、专题台账、导出和 AI 能力收成可日常使用的闭环：

- 修复党员发展日期默认成今天的问题；统一 `v4_party_cases` 主记录和旧 `party` 兼容镜像，空日期保持为空。
- 学生档案补齐导师、班主任、出生、照片、家长、家庭地址、校内/校外居住和房东联系方式；学号更正保留稳定 `student_id` 与历史学号。
- 学生导入默认合并更新；覆盖导入需要明确确认、显示删除数量并先建立恢复点；模板中心明确两行模板格式和字段说明。
- 资料与平台增加卡片/列表模式；谈心谈话支持姓名、稳定 ID、学号和日期范围筛选。
- 新增本地工作通讯录、班级课表、团员发展和活动参与关系集合，统一接入 IndexedDB、Electron、便携 HTML、备份和手机交换包。
- 活动支持学期、学生选择、照片/附件和按学生/学期去重统计；工作留痕支持查课查寝、走访信息和照片/附件。
- 统一 CSV/XLSX/DOCX/打印 HTML/PDF 导出入口，支持筛选范围、字段列序、封面、目录、页眉页脚、页码和附件索引；浏览器 PDF 走系统打印，Electron 使用 `printToPDF`。
- 手机常用栏支持上移/下移，首次欢迎设置不遮挡导航；修复 `body` 溢出容器导致的 sticky 顶栏失效，`390×844` 和 `360×800` 无横向溢出。
- 新增 AI 通知重点识别：老师主动粘贴/导入后生成重点、截止时间、待办和证据片段草稿，人工确认后才可转任务或工作留痕。

安全边界：重点学生锁明确是“仅防误看，不是加密”；照片和附件进入附件仓并做容量检查；AI 不自动改写学生事实、心理、预警、纪律、资助或党团结论。逐条状态、迁移和限制见[老师反馈全量审计](./docs/upgrade/teacher-feedback-audit-2026-08-18.md)。

本版本已正式公开：发布提交为 [`1b773b3dfaa19745541df9e504f6de160c48a75a`](https://github.com/7752777/counselor-desk/commit/1b773b3dfaa19745541df9e504f6de160c48a75a)，[发布门禁 #32160336549](https://github.com/7752777/counselor-desk/actions/runs/32160336549) 与 [Pages 部署 #32161681464](https://github.com/7752777/counselor-desk/actions/runs/32161681464) 均成功；[v4.5.0 Release](https://github.com/7752777/counselor-desk/releases/tag/v4.5.0) 提供离线 HTML、Windows x64 / ARM64、macOS Universal DMG / ZIP 和三份 SHA-256 清单。附件大小、哈希、HTTP 200 和平台限制见 [v4.5.0 发布收尾记录](./docs/upgrade/release-v4.5.0.md)。

## [4.4.6] · 2026-08-18 · 安全边界、备份新鲜度与容量治理

本版本在 v4.4.5 的 AI 跨模块和移动端基础上，补齐了真实使用中最容易造成误判或数据风险的部分：

- 重点学生档案明确标注为“防误看锁，不是加密”；清除口令改为“清除防误看锁（不恢复口令）”，启用工作台访问锁时需要再次验证，并留下访问审计。
- 首页直接显示距上次加密备份的天数和备份后变更数量；桌面端继续支持按周期或变更量补做备份，网页端明确保持手动备份边界。
- 图片、照片和附件写入前检查容量增量，容量页统计本地记录、IndexedDB 附件数量和大小，70% 提醒、85% 限制高风险大写入、95% 进入危险状态。
- 增加平台交换最小化策略和访问审计入口，默认遮蔽手机号、姓名、学号和危机级别原文，并排除心理、资助、重点档案、AI 凭据、附件正文和工作区历史。
- 补充用户视角综合审查、依赖/单文件发布清单和完整收尾基线，明确真加密、家校沟通、字典中心、Word/PDF 汇报稿、签名公证和此前暴露密钥轮换仍属于独立边界。
- 修复移动导航搜索包含空格时无法匹配完整模块名称的问题，并补充折叠分组下的完整名称回归测试。

本版本已正式公开：发布提交为 [`f32d32a67325cc0177ccdc977e43709bb4391a35`](https://github.com/7752777/counselor-desk/commit/f32d32a67325cc0177ccdc977e43709bb4391a35)，[发布门禁 #32133893252](https://github.com/7752777/counselor-desk/actions/runs/32133893252) 与 [Pages 部署 #32135312775](https://github.com/7752777/counselor-desk/actions/runs/32135312775) 均成功；[v4.4.6 Release](https://github.com/7752777/counselor-desk/releases/tag/v4.4.6) 提供离线 HTML、Windows x64 / ARM64、macOS Universal DMG / ZIP 和三份 SHA-256 清单。文件大小、哈希和平台限制见 [v4.4.6 发布收尾记录](./docs/upgrade/release-v4.4.6.md)。

## [4.4.5] · 2026-08-18 · AI 深度融合、移动体验与正式发布工程

- 把 AI 从独立工作台扩展为跨模块工作流：统一当前学生、当前事项、班级、日期范围和来源上下文，业务页面提供就地动作，学生、谈话、任务、成绩、资助、心理、就业、资料、班团和业务档案均可生成建议或草稿。
- 新增统一建议中心和来源生命周期：支持草稿、审核、查看、采纳、驳回、转任务、转谈话、转工作留痕，保存用途、模型、审计、风险、来源和稳定 `student_id`；批量查看不等于批量采纳。
- 默认 AI 出站上下文继续脱敏，敏感字段授权只对当前请求有效；心理、预警、纪律、资助、奖惩和学生事实不会被 AI 自动改写。
- 完成 AI relay 的来源限制、HTTPS、SSRF 防护、DNS 校验、超时、响应大小限制、错误脱敏和可选令牌；静态 Pages 仍需单独运行受控 HTTPS relay。
- 修复移动端首次欢迎设置和每日问候遮挡导航的问题；抽屉、遮罩、Escape、焦点、折叠搜索、全部展开、恢复导航和窄屏动作区均完成收口。
- 政策与就业资源外链统一进行安全地址校验；非法地址不生成可点击链接，并显示“网址待核验”。
- 新增仓库发布密钥扫描、动态版本 Release 工作流、CHANGELOG 驱动的发布说明、Issue 模板、PR 模板和 Dependabot 配置。
- 继续保持 schema v8、浏览器 IndexedDB、Electron SQLite、离线 HTML、备份、迁移、交换包、手机工作包和稳定 ID 兼容边界。

本版本已正式公开。发布事实如下：提交 [`44c833d1`](https://github.com/7752777/counselor-desk/commit/44c833d1bafbe51df844f81cbcc0d638e7e9621e)，[Release Actions #25](https://github.com/7752777/counselor-desk/actions/runs/32086383154) 与 [Pages 部署 #46](https://github.com/7752777/counselor-desk/actions/runs/32089792020) 均成功，[v4.4.5 Release](https://github.com/7752777/counselor-desk/releases/tag/v4.4.5) 为非 Draft、非 Pre-release 的公开 Latest 版本。Release 包含离线 HTML、Windows x64 / ARM64、macOS Universal DMG / ZIP 和 `Windows-SHA256.txt`、`macOS-SHA256.txt`、`Web-SHA256.txt` 三份校验清单；哈希与签名限制见[发布收尾记录](./docs/upgrade/release-v4.4.5.md)。

## [4.4.4] · 2026-08-17 · 本地 AI、业务档案与工作台可靠性

- 以实际校园学生名册、暑期离校统计、课程成绩和 GPA 汇总表走完浏览器实际预览、提交与学号关联验证。测试过程不改写原始工作簿，报告不保留学生身份或成绩明细。
- 导入日期增加 `2026年7月28日`、`7月27号`、`7/25/26`与同行年份推断，统一规整为 ISO 日期后再校验；不存在的日期仍会被明确标出。
- 成绩导入对教务系统常见的“课程编号 / 课程性质 / 补重学期”辅助列采用精确映射，不再误判为课程或学期重复字段；兼容优、良、中、及格、不及格等等级制成绩与常见的小数点省略写法。
- 已启用 IndexedDB 或桌面端仓储时，大批量专题导入不再由旧 `localStorage` 快照容量预检查拦截；工作区的版本历史与持久化队列继续承担可恢复责任。
- 修正单文件离线 HTML 内嵌 SheetJS 时的历史代码页字节损坏，并将 Excel、备份、ZIP 与图表运行库一并打入离线包。
- 新增本地 AI 用途授权、每日成功额度、视觉能力、调用审计和可取消草稿工作流；AI 结果保持候选、草稿或待确认状态。
- 新增证书识别人工确认、日期范围工作总结和就业资源目录；确认前不写入奖励或工作留痕，资源支持来源、核验状态、收藏与清单维护。
- 新增综合测评、学期学业、处分和资助业务档案，优先使用 `student_id` 关联并兼容旧学号记录；修正处分附件关联边界。
- 新增领导统计视图，支持数值聚合、保存、选择、编辑、删除和 CSV 导出；设置并发写入与 v8 重启恢复均纳入回归。
- 统一新增集合清单，接入浏览器 IndexedDB、便携 HTML、Electron SQLite、迁移、备份、交换包和桌面边界。

`v4.4.4` 已由提交 [`438badd4`](https://github.com/7752777/counselor-desk/commit/438badd4fd1fffd6aff36412912309642f02d389) 发布；完整门禁记录见 [Actions run 32024091313](https://github.com/7752777/counselor-desk/actions/runs/32024091313)，下载附件、SHA-256 清单与未签名/未公证说明见 [v4.4.4 Release](https://github.com/7752777/counselor-desk/releases/tag/v4.4.4)，在线体验见 [GitHub Pages](https://7752777.github.io/counselor-desk/)。

## [4.4.2] · 2026-08-14 · 欢迎体验与发布统一

- 修正首次引导页在桌面端使用错误相对路径时退化为纯色背景的问题；教育工作场景图会随网页、离线 HTML 与桌面包一同交付。
- 每日金句改用温馨书桌图片作为阅读背景，并保持文字在不同主题和窄屏上的清晰度。
- 调整学生分页的“每页 / 数值 / 条”控件与底部摘要，避免换行拆散并减少窄屏拥挤。
- 统一 README、快速开始、安装、迁移、架构和发布指南中的版本、下载与资源说明；保留 v3.8 至 v4.4.1 的完整演进记录。

`v4.4.2` 的网页、离线 HTML、Windows 安装包和 macOS Universal 包只在同一标签完成 Tests、Windows、macOS、网页产物与 Release 门禁后公开；附件、SHA-256 与签名状态以 [v4.4.2 Release](https://github.com/7752777/counselor-desk/releases/tag/v4.4.2) 为准。

## [4.4.1] · 2026-08-14 · 维护发布

- Windows 桌面端移除 Electron 默认的 File / Edit / View 菜单栏，保留产品内的工作入口与 macOS 的系统菜单习惯。
- 更新仓库首页、下载说明、安装指南、迁移说明、发布验收和归档账本，修正“候选、旧 Pages、未发布”等已经失效的说法。
- 把 `v3.8 → v3.9 → v4.0 → v4.1 → v4.2 → v4.3 → v4.4.0` 放回同一张产品历程表，保留每一轮解决的真实一线问题。
- 调整公开面门禁：历史版本可以作为产品演进被介绍，但不能再被误写为当前下载版本或过期安装包。

`v4.4.1` 已按 Tests、Windows、macOS、网页产物、Release 与 Pages 的同一门禁公开；附件、SHA-256 与未签名/未公证说明以 [v4.4.1 Release](https://github.com/7752777/counselor-desk/releases/tag/v4.4.1) 为准。

## [4.4.0] · 2026-08-14 · 正式发布

v4.4.0 是一次面向“日常使用是否真的可靠”的收口：不以代码出现为完成标准，而以恢复、导入、桌面端、性能、公开文档和发布链路是否能经得起验证为标准。发布提交为 [`ed362d73a1c95bded26bdfba811a10eb73b5b2a2`](https://github.com/7752777/counselor-desk/commit/ed362d73a1c95bded26bdfba811a10eb73b5b2a2)，下载、SHA-256 与平台说明见 [v4.4.0 Release](https://github.com/7752777/counselor-desk/releases/tag/v4.4.0)。

### 本次新增与重做

- 学生台账切换为真实 `10 / 20 / 50 / 100` 分页，保存每页条数、当前页、组合筛选、排序和显示模式；删除后保留合理页码。
- 增加当前页与筛选结果选择、批量编辑、批量删除、差异确认和一次撤销；表格加入固定选择/操作列，以及面向大表的横向滚动入口。
- 把社区/书院、培养层次、学生状态、关注类型、危机等级和预警解除状态贯通表单、筛选、导入、导出和统计。
- 增加学生统一时间线、历史学号、疑似重复提示、保存筛选方案、个人列视图、列分组与配置。
- 恢复并明确班团组织、党员发展、成绩与学业帮扶、奖惩、活动、住宿和工作留痕的独立入口。
- 建立 schema v8 工作区：串行写入、保存状态、版本历史、恢复点、失败重试、诊断、迁移和便携模式隔离。
- 重做首次欢迎体验与每日问候：双栏教育场景、五套主题、按本地时段问候、经筛选的中文短句和每日一次控制。
- 补齐八张 2560 × 1440 产品截图、面向使用者的文档中心、公开面扫描和离线包资产检查。

### 数据可靠性与性能

- 加强导入、交换包、加密备份和恢复的失败回滚，覆盖附件同哈希但不同 ID、附件写入失败、历史合并和辅助记录回滚等场景。
- 便携 HTML 使用隔离的数据空间，不读取或污染同一浏览器 `file://` 来源下的普通工作台数据。
- 修正非学生集合的原子替换与桌面存储兼容，防止专题记录在重启后丢失或被错误解码。
- 将 10,000 条导入预览和 5,000 名学生组合筛选放进浏览器性能门禁，记录进度间隔和生命周期回收，而不是只测纯函数。

### 已验证的交付链

- 同一发布提交通过 [Tests、Windows、macOS、网页产物与 Draft Release 门禁](https://github.com/7752777/counselor-desk/actions/runs/31768117637)，随后由 [Pages 部署](https://github.com/7752777/counselor-desk/actions/runs/31768796087) 发布在线体验。
- Release 提供离线 HTML、Windows NSIS `x64 / ARM64` 安装包、macOS Universal `DMG / ZIP` 和三份 SHA-256 清单。
- Windows 运行器完成安装、重装、启动、SQLite、附件、备份、退出保存及两条卸载路径检查；macOS 运行器完成 Universal 架构、DMG 挂载、启动、SQLite、附件、备份恢复与退出保存烟测。

### 使用边界

- macOS 附件当前明确标注为**未签名、未公证**；构建与烟测通过不等于已加入 Apple 信任链。
- 工作台不替代学校正式业务系统、专业心理评估、应急处置或党团审批。敏感数据的访问、导出与备份仍须遵守所在学校制度。
- 任何后续数据一致性或平台问题都会阻断新的发布，不会用旧的绿色记录替代当前版本验证。

## [4.3] · 2026-08-13 · 专题工作从“藏在角落”走向可达

> 开发验证节点，尚未作为公开 Release 发布。

### 工作重点

- 重新梳理班团组织、党员发展、奖惩、活动、成绩、工作留痕等专题入口，减少功能被二级页面掩盖的情况。
- 围绕学生档案汇聚谈话、风险、成绩、住宿、奖惩、活动、组织任职和工作记录，建立可回看的工作脉络。
- 补齐成绩趋势、课程风险、班级对比与学业帮扶的关联展示。
- 继续将附件、导入、导出、筛选与批量操作纳入统一的业务闭环。

### 为什么要做这一轮

有些功能即使存在，也会因为入口深、名称不清楚、数据没有回到学生档案而像“没有”。v4.3 的重点不是单独堆模块，而是把专题工作从二级页面带回可查找、可回看、可交接的位置。

## [4.2] · 2026-08-13 · 让学生台账适合反复使用

> 开发验证节点，尚未作为公开 Release 发布。

### 工作重点

- 推进 10 / 20 / 50 / 100 条分页、当前页与筛选/排序/显示模式记忆，以及删除后的合理页码保持。
- 推进批量选择、全选筛选结果、差异预览、批量编辑、批量删除与一次撤销。
- 为学生扩展社区/书院、培养层次、学生状态、关注类型、危机等级和预警解除等字段，并贯通表单、筛选、导入、导出与统计。
- 推进保存筛选方案、个人列视图、字段分组、列顺序与宽度配置，以及大表固定列、表头与横向滚动体验。

### 为什么要做这一轮

“一次性导入成功”不等于“每天都好用”。当名单变成几百、几千条，老师需要的是能记住工作状态、能安全批量处理、能在删除后继续停在当前任务位置的台账，而不是反复点“加载更多”。

## [4.1] · 2026-08-12 · 给每一次保存留一条退路

> 开发验证节点，尚未作为公开 Release 发布。

### 工作重点

- 推进统一写入队列、保存状态、版本历史、操作日志、失败重试和诊断包。
- 推进 schema v8：兼容既有 JSON、便携 HTML、浏览器本地数据和桌面 SQLite，迁移前创建恢复点，保持内部 ID、附件关联、历史学号与自定义字段不变。
- 明确浏览器本地存储不可用时的兼容模式和应急导出提示，避免静默降级。
- 为备份恢复、附件去重、异常退出、数据库锁定与磁盘不足补充回归覆盖。

### 为什么要做这一轮

学生工作记录最怕的不是少一个按钮，而是“刚刚保存的东西到底落在哪里”“恢复失败会不会覆盖现有数据”。这一轮把可见的保存状态、可重试的写入、可回看的历史和可携带的备份放到同一条可靠性链路上。

## [4.0.0] · 2026-08-07 · 双端本地工作台公开版

### 新增

- 学生台账支持动态字段、常用字段映射、筛选排序、卡片/照片视图与导入导出。
- 谈心谈话、重点关注、请销假、考勤、住宿、奖助、就业、资料库、班团与党团相关工作入口。
- 附件归档、照片管理、分类资料库、常用模板与本地文件引用。
- 网页离线使用、手机工作包交换、Windows/macOS 桌面端构建基础与本地数据路径。
- 备份、恢复、迁移和导入预览的基础流程。

### 改进

- 统一更多常见学校导出字段的识别方式，未知列保留为可追溯数据。
- 增强 CSV 公式注入防护、学号文本处理、日期与枚举校验。
- 更新公开首页、使用说明、截图与下载说明，让普通使用者无需先理解源码结构。

### 已知边界

- 公开版本不能替代学校正式业务系统、专业心理评估、紧急处置或党团审批流程。
- 桌面构建、签名与公证状态必须以 Release 实际附件说明为准。

## [3.9.0] · 2026-08-05 · 本地工作流加固

- 增加首次使用引导、主题设置、界面锁与本地学习资料管理。
- 建立通用导入预览、导入快照与手机交换包的基础能力。
- 持续处理不同学校表头、前导零学号、重复记录、空值与异常数据的兼容问题。

## [3.8.0] · 2026-08-04 · 让表格导入更可靠

- 支持 xls / xlsx / CSV 学生名单导入和常见表头识别。
- 补齐导入校验、未知字段保留、数据合并和回归测试基础。

---

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
